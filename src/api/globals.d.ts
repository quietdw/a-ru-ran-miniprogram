/* tslint:disable */
/* eslint-disable */
/**
 * Gin-Vue-Admin Swagger API接口文档 - version v2.8.6
 *
 * 使用gin+vue进行极速开发的全栈开发基础平台
 *
 * OpenAPI version: 3.0.0
 *
 * Contact:
 *
 * NOTE: This file is auto generated by the alova's vscode plugin.
 *
 * https://alova.js.org/devtools/vscode
 *
 * **Do not edit the file manually.**
 */
import type { Alova, AlovaMethodCreateConfig, AlovaGenerics, Method } from 'alova';
import type { $$userConfigMap, alovaInstance } from '.';
import type apiDefinitions from './apiDefinitions';

type CollapsedAlova = typeof alovaInstance;
type UserMethodConfigMap = typeof $$userConfigMap;

type Alova2MethodConfig<Responded> =
  CollapsedAlova extends Alova<
    AlovaGenerics<
      any,
      any,
      infer RequestConfig,
      infer Response,
      infer ResponseHeader,
      infer L1Cache,
      infer L2Cache,
      infer SE
    >
  >
    ? Omit<
        AlovaMethodCreateConfig<
          AlovaGenerics<Responded, any, RequestConfig, Response, ResponseHeader, L1Cache, L2Cache, SE>,
          any,
          Responded
        >,
        'params'
      >
    : never;

// Extract the return type of transform function that define in $$userConfigMap, if it not exists, use the default type.
type ExtractUserDefinedTransformed<
  DefinitionKey extends keyof typeof apiDefinitions,
  Default
> = DefinitionKey extends keyof UserMethodConfigMap
  ? UserMethodConfigMap[DefinitionKey]['transform'] extends (...args: any[]) => any
    ? Awaited<ReturnType<UserMethodConfigMap[DefinitionKey]['transform']>>
    : Default
  : Default;
type Alova2Method<
  Responded,
  DefinitionKey extends keyof typeof apiDefinitions,
  CurrentConfig extends Alova2MethodConfig<any>
> =
  CollapsedAlova extends Alova<
    AlovaGenerics<
      any,
      any,
      infer RequestConfig,
      infer Response,
      infer ResponseHeader,
      infer L1Cache,
      infer L2Cache,
      infer SE
    >
  >
    ? Method<
        AlovaGenerics<
          CurrentConfig extends undefined
            ? ExtractUserDefinedTransformed<DefinitionKey, Responded>
            : CurrentConfig['transform'] extends (...args: any[]) => any
              ? Awaited<ReturnType<CurrentConfig['transform']>>
              : ExtractUserDefinedTransformed<DefinitionKey, Responded>,
          any,
          RequestConfig,
          Response,
          ResponseHeader,
          L1Cache,
          L2Cache,
          SE
        >
      >
    : never;

export interface System_sysapi {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * api组
   */
  apiGroup?: string;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * api中文描述
   */
  description?: string;
  /**
   * 方法:创建POST(默认)|查看GET|更新PUT|删除DELETE
   */
  method?: string;
  /**
   * api路径
   */
  path?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface Request_idsreq {
  ids?: number[];
}
export interface Request_devicerequest {
  /**
   * 设备代码
   */
  device_code: string;
  /**
   * 设备数据
   */
  device_data?: object;
  /**
   * 设备名称
   */
  device_name: string;
}
export interface Request_getbyid {
  /**
   * 主键ID
   */
  id?: number;
}
export interface Request_searchapiparams {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * api组
   */
  apiGroup?: string;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 排序方式:升序false(默认)|降序true
   */
  desc?: boolean;
  /**
   * api中文描述
   */
  description?: string;
  /**
   * 关键字
   */
  keyword?: string;
  /**
   * 方法:创建POST(默认)|查看GET|更新PUT|删除DELETE
   */
  method?: string;
  /**
   * 排序
   */
  orderKey?: string;
  /**
   * 页码
   */
  page?: number;
  /**
   * 每页大小
   */
  pageSize?: number;
  /**
   * api路径
   */
  path?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface Request_orderrequest {
  /**
   * 配送地址
   */
  delivery_address: string;
  /**
   * 配送备注
   */
  delivery_note?: string;
  /**
   * 商品ID 	//用户ID
   */
  product_id: number;
  /**
   * 收货人姓名
   */
  receiver_name: string;
  /**
   * 用户手机号
   */
  user_phone: string;
}
export interface Request_reservationsrequest {
  /**
   * 预约日期（yyyy-mm-dd的Date类型）
   */
  date?: string;
  /**
   * 备注
   */
  note?: string;
  /**
   * 房间 ID
   */
  roomId?: number;
  /**
   * 时段 ID
   */
  timeSlotId?: number;
}
export interface Request_wechatmingetuserinfo {
  /**
   * @Description 724 微信的 encrypted_data
   */
  encrypted_data: string;
  /**
   * @Description 724 微信的 iv
   */
  iv: string;
}
export interface Request_wechatminauthrequest {
  /**
   * @Description 723 微信登入的code
   */
  code: string;
}
export interface Example_exaattachmentcategory {
  /**
   * 主键ID
   */
  ID?: number;
  children?: Example_exaattachmentcategory[];
  /**
   * 创建时间
   */
  createdAt?: string;
  name?: string;
  pid?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface System_sysbasemenubtn {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  desc?: string;
  name?: string;
  sysBaseMenuID?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface System_meta {
  activeName?: string;
  /**
   * 自动关闭tab
   */
  closeTab?: boolean;
  /**
   * 是否是基础路由（开发中）
   */
  defaultMenu?: boolean;
  /**
   * 菜单图标
   */
  icon?: string;
  /**
   * 是否缓存
   */
  keepAlive?: boolean;
  /**
   * 菜单名
   */
  title?: string;
  /**
   * 路由切换动画
   */
  transitionType?: string;
}
export interface System_sysbasemenuparameter {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 地址栏携带参数的key
   */
  key?: string;
  sysBaseMenuID?: number;
  /**
   * 地址栏携带参数为params还是query
   */
  type?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  /**
   * 地址栏携带参数的值
   */
  value?: string;
}
export interface System_sysbasemenu {
  /**
   * 主键ID
   */
  ID?: number;
  authoritys?: System_sysauthority[];
  children?: System_sysbasemenu[];
  /**
   * 对应前端文件路径
   */
  component?: string;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 是否在列表隐藏
   */
  hidden?: boolean;
  menuBtn?: System_sysbasemenubtn[];
  /**
   * 附加属性
   */
  meta?: System_meta;
  /**
   * 路由name
   */
  name?: string;
  parameters?: System_sysbasemenuparameter[];
  /**
   * 父菜单ID
   */
  parentId?: number;
  /**
   * 路由path
   */
  path?: string;
  /**
   * 排序标记
   */
  sort?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface System_sysauthority {
  /**
   * 角色ID
   */
  authorityId?: number;
  /**
   * 角色名
   */
  authorityName?: string;
  children?: System_sysauthority[];
  /**
   * 创建时间
   */
  createdAt?: string;
  dataAuthorityId?: System_sysauthority[];
  /**
   * 默认菜单(默认dashboard)
   */
  defaultRouter?: string;
  deletedAt?: string;
  menus?: System_sysbasemenu[];
  /**
   * 父角色ID
   */
  parentId?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface Response_sysauthoritycopyresponse {
  authority?: System_sysauthority;
  /**
   * 旧角色ID
   */
  oldAuthorityId?: number;
}
export interface Request_pageinfo {
  /**
   * 关键字
   */
  keyword?: string;
  /**
   * 页码
   */
  page?: number;
  /**
   * 每页大小
   */
  pageSize?: number;
}
export interface Request_sysauthoritybtnreq {
  authorityId?: number;
  menuID?: number;
  selected?: number[];
}
export interface Request_datasource {
  /**
   * 关联关系 1 一对一 2 一对多
   */
  association?: number;
  dbName?: string;
  hasDeletedAt?: boolean;
  label?: string;
  table?: string;
  value?: string;
}
export interface Request_autocodefield {
  /**
   * 是否检查数据源
   */
  checkDataSource?: boolean;
  /**
   * 是否可清空
   */
  clearable?: boolean;
  /**
   * 数据库字段
   */
  columnName?: string;
  /**
   * 数据库字段描述
   */
  comment?: string;
  /**
   * 数据源
   */
  dataSource?: Request_datasource;
  /**
   * 数据库字段长度
   */
  dataTypeLong?: string;
  /**
   * 是否必填
   */
  defaultValue?: string;
  /**
   * 是否前端详情
   */
  desc?: boolean;
  /**
   * 字典
   */
  dictType?: string;
  /**
   * 校验失败文字
   */
  errorText?: string;
  /**
   * 是否导入/导出
   */
  excel?: boolean;
  /**
   * 中文名
   */
  fieldDesc?: string;
  /**
   * 索引类型
   */
  fieldIndexType?: string;
  /**
   * FieldJson
   */
  fieldJson?: string;
  /**
   * Field名
   */
  fieldName?: string;
  /**
   * 是否隐藏查询条件
   */
  fieldSearchHide?: boolean;
  /**
   * 搜索条件
   */
  fieldSearchType?: string;
  /**
   * Field数据类型
   */
  fieldType?: string;
  /**
   * Front           bool        `json:"front"`           // 是否前端可见
   */
  form?: boolean;
  /**
   * 是否主键
   */
  primaryKey?: boolean;
  /**
   * 是否必填
   */
  require?: boolean;
  /**
   * 是否增加排序
   */
  sort?: boolean;
  /**
   * 是否前端表格列
   */
  table?: boolean;
}
export interface Request_autocode {
  /**
   * Struct简称
   */
  abbreviation?: string;
  /**
   * 是否自动创建api
   */
  autoCreateApiToSql?: boolean;
  /**
   * 是否自动创建按钮权限
   */
  autoCreateBtnAuth?: boolean;
  /**
   * 是否自动创建menu
   */
  autoCreateMenuToSql?: boolean;
  /**
   * 是否自动创建资源标识
   */
  autoCreateResource?: boolean;
  /**
   * 是否自动迁移表结构
   */
  autoMigrate?: boolean;
  /**
   * 业务数据库
   */
  businessDB?: string;
  /**
   * Struct中文名称
   */
  description?: string;
  fields?: Request_autocodefield[];
  /**
   * 是否生成server
   */
  generateServer?: boolean;
  /**
   * 是否生成web
   */
  generateWeb?: boolean;
  /**
   * 是否使用gva默认Model
   */
  gvaModel?: boolean;
  /**
   * go文件名称
   */
  humpPackageName?: string;
  /**
   * 是否新增
   */
  isAdd?: boolean;
  /**
   * 是否树形结构
   */
  isTree?: boolean;
  /**
   * 是否只生成模板
   */
  onlyTemplate?: boolean;
  package?: string;
  /**
   * 文件名称
   */
  packageName?: string;
  primaryField?: Request_autocodefield;
  /**
   * Struct名称
   */
  structName?: string;
  /**
   * 表名
   */
  tableName?: string;
  /**
   * 展示的树json字段
   */
  treeJson?: string;
}
export interface Request_sysautocodepackagecreate {
  desc?: string;
  label?: string;
  packageName?: string;
  template?: string;
}
export interface Request_automcptool {
  description: string;
  name: string;
  params?: Array<{
    default?: string;
    description: string;
    name: string;
    required?: boolean;
    /**
     * string, number, boolean, object, array
     */
    type: string;
  }>;
  response?: Array<{
    /**
     * text, image
     */
    type: string;
  }>;
}
export interface Request_sysautohistoryrollback {
  /**
   * 是否删除接口
   */
  deleteApi?: boolean;
  /**
   * 是否删除菜单
   */
  deleteMenu?: boolean;
  /**
   * 是否删除表
   */
  deleteTable?: boolean;
  /**
   * 主键ID
   */
  id?: number;
}
export interface Request_login {
  /**
   * 验证码
   */
  captcha?: string;
  /**
   * 验证码ID
   */
  captchaId?: string;
  /**
   * 密码
   */
  password?: string;
  /**
   * 用户名
   */
  username?: string;
}
export interface Request_casbininfo {
  /**
   * 方法
   */
  method?: string;
  /**
   * 路径
   */
  path?: string;
}
export interface Request_casbininreceive {
  /**
   * 权限id
   */
  authorityId?: number;
  casbinInfos?: Request_casbininfo[];
}
export interface Miniprogram_consumption {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 交易金额
   */
  amount?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  createdBy?: number;
  deletedBy?: number;
  /**
   * 备注
   */
  remark?: string;
  /**
   * 交易类型
   */
  transaction_type: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  updatedBy?: number;
  /**
   * 用户ID
   */
  userId?: number;
  wallet_balance?: number;
}
export type Common_jsonmap = Record<string, any>;
export interface System_sysuser {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 多用户角色
   */
  authorities?: System_sysauthority[];
  /**
   * 用户角色
   */
  authority?: System_sysauthority;
  /**
   * 用户角色ID
   */
  authorityId?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 用户邮箱
   */
  email?: string;
  /**
   * 用户是否被冻结 1正常 2冻结
   */
  enable?: number;
  /**
   * 用户头像
   */
  headerImg?: string;
  /**
   * 用户昵称
   */
  nickName?: string;
  /**
   * 配置
   */
  originSetting?: Common_jsonmap;
  /**
   * 用户手机号
   */
  phone?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  /**
   * 用户登录名
   */
  userName?: string;
  /**
   * 用户UUID
   */
  uuid?: string;
}
export interface Example_exacustomer {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 客户名
   */
  customerName?: string;
  /**
   * 客户手机号
   */
  customerPhoneData?: string;
  /**
   * 管理详情
   */
  sysUser?: System_sysuser;
  /**
   * 管理角色ID
   */
  sysUserAuthorityID?: number;
  /**
   * 管理ID
   */
  sysUserId?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface Miniprogram_device {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 设备代码
   */
  device_code?: string;
  /**
   * 设备数据
   */
  device_data?: object;
  /**
   * 设备名称
   */
  device_name?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  /**
   * 用户id
   */
  user_id?: number;
}
export interface Response_email {
  /**
   * 邮件内容
   */
  body?: string;
  /**
   * 邮件标题
   */
  subject?: string;
  /**
   * 邮件发送给谁
   */
  to?: string;
}
export interface Example_exafileuploadanddownload {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 分类id
   */
  classId?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 编号
   */
  key?: string;
  /**
   * 文件名
   */
  name?: string;
  /**
   * 文件标签
   */
  tag?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  /**
   * 文件地址
   */
  url?: string;
}
export interface Request_exaattachmentcategorysearch {
  classId?: number;
  /**
   * 关键字
   */
  keyword?: string;
  /**
   * 页码
   */
  page?: number;
  /**
   * 每页大小
   */
  pageSize?: number;
}
export interface Miniprogram_floors {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  createdBy?: number;
  deletedBy?: number;
  /**
   * 楼层名称（用户自定义）
   */
  name?: string;
  /**
   * 排序字段（从小到大）
   */
  sort_order?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
  updatedBy?: number;
}
export interface Model_info {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 附件
   */
  attachments?: object[];
  /**
   * 内容
   */
  content?: string;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 标题
   */
  title?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  /**
   * 作者
   */
  userID?: number;
}
export interface Request_initdb {
  adminPassword: string;
  /**
   * 数据库名
   */
  dbName: string;
  /**
   * sqlite数据库文件路径
   */
  dbPath?: string;
  /**
   * 数据库类型
   */
  dbType?: string;
  /**
   * 服务器地址
   */
  host?: string;
  /**
   * 数据库密码
   */
  password?: string;
  /**
   * 数据库连接端口
   */
  port?: string;
  /**
   * postgresql指定template
   */
  template?: string;
  /**
   * 数据库用户名
   */
  userName?: string;
}
export interface Request_addmenuauthorityinfo {
  /**
   * 角色ID
   */
  authorityId?: number;
  menus?: System_sysbasemenu[];
}
export type Request_empty = object;
export interface Request_getauthorityid {
  /**
   * 角色ID
   */
  authorityId?: number;
}
export interface Miniprogram_product {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  createdBy?: number;
  deletedBy?: number;
  /**
   * 商品图片
   */
  image?: string;
  /**
   * 净含量
   */
  net_weight: string;
  /**
   * 产地
   */
  origin: string;
  /**
   * 原价
   */
  original_price: number;
  /**
   * 商品分类
   */
  product_category?: string;
  /**
   * 商品详情
   */
  product_details?: string;
  /**
   * 商品名称
   */
  product_name: string;
  /**
   * 销售数
   */
  sales_count?: number;
  /**
   * 售价
   */
  selling_price: number;
  /**
   * 保质期
   */
  shelfLife: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  updatedBy?: number;
}
export interface Miniprogram_order {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 实际送达时间
   */
  actual_delivery_time?: string;
  /**
   * 创建时间
   */
  createdAt?: string;
  createdBy?: number;
  deletedBy?: number;
  /**
   * 配送地址
   */
  delivery_address?: string;
  /**
   * 配送备注
   */
  delivery_note?: string;
  /**
   * 期望送达时间
   */
  expected_delivery_time?: string;
  /**
   * 下单金额
   */
  order_amount?: number;
  /**
   * 订单编号
   */
  order_number?: string;
  /**
   * 订单状态
   */
  order_status?: string;
  product?: Miniprogram_product;
  /**
   * 商品ID
   */
  product_id?: number;
  /**
   * 收货人姓名
   */
  receiver_name?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  updatedBy?: number;
  /**
   * 用户ID
   */
  user_id?: number;
  /**
   * 用户手机号
   */
  user_phone: string;
}
export interface Miniprogram_rooms {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  createdBy?: number;
  deletedBy?: number;
  /**
   * 所属楼层 ID
   */
  floorId?: number;
  /**
   * 房间名称
   */
  name?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  updatedBy?: number;
}
export interface Miniprogram_reservations {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  createdBy?: number;
  /**
   * 预约日期（yyyy-mm-dd）
   */
  date?: string;
  deletedBy?: number;
  /**
   * 备注
   */
  note?: string;
  product?: Miniprogram_rooms;
  /**
   * 房间 ID
   */
  roomId?: number;
  /**
   * 预约状态
   */
  status?: string;
  /**
   * 时段 ID
   */
  timeSlotId?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
  updatedBy?: number;
  /**
   * 预约者用户 ID（WeChatUsers.id）
   */
  userId?: number;
}
export interface System_sysdictionarydetail {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 子字典详情
   */
  children?: System_sysdictionarydetail[];
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 禁用状态，根据status字段动态计算
   */
  disabled?: boolean;
  /**
   * 扩展值
   */
  extend?: string;
  /**
   * 展示值
   */
  label?: string;
  /**
   * 层级深度，从0开始
   */
  level?: number;
  /**
   * 父级字典详情ID
   */
  parentID?: number;
  /**
   * 层级路径，如 "1,2,3"
   */
  path?: string;
  /**
   * 排序标记
   */
  sort?: number;
  /**
   * 启用状态
   */
  status?: boolean;
  /**
   * 关联标记
   */
  sysDictionaryID?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
  /**
   * 字典值
   */
  value?: string;
}
export interface System_sysdictionary {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 子字典
   */
  children?: System_sysdictionary[];
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 描述
   */
  desc?: string;
  /**
   * 字典名（中）
   */
  name?: string;
  /**
   * 父级字典ID
   */
  parentID?: number;
  /**
   * 状态
   */
  status?: boolean;
  sysDictionaryDetails?: System_sysdictionarydetail[];
  /**
   * 字典名（英）
   */
  type?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface System_condition {
  /**
   * 主键ID
   */
  ID?: number;
  column?: string;
  /**
   * 创建时间
   */
  createdAt?: string;
  from?: string;
  operator?: string;
  templateID?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface System_jointemplate {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  joins?: string;
  on?: string;
  table?: string;
  templateID?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface System_sysexporttemplate {
  /**
   * 主键ID
   */
  ID?: number;
  conditions?: System_condition[];
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 数据库名称
   */
  dbName?: string;
  joinTemplate?: System_jointemplate[];
  limit?: number;
  /**
   * 模板名称
   */
  name?: string;
  order?: string;
  /**
   * 表名称
   */
  tableName?: string;
  /**
   * 模板标识
   */
  templateID?: string;
  /**
   * 模板信息
   */
  templateInfo?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface System_sysoperationrecord {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 代理
   */
  agent?: string;
  /**
   * 请求Body
   */
  body?: string;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 错误信息
   */
  error_message?: string;
  /**
   * 请求ip
   */
  ip?: string;
  /**
   * 延迟
   */
  latency?: string;
  /**
   * 请求方法
   */
  method?: string;
  /**
   * 请求路径
   */
  path?: string;
  /**
   * 响应Body
   */
  resp?: string;
  /**
   * 请求状态
   */
  status?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
  user?: System_sysuser;
  /**
   * 用户id
   */
  user_id?: number;
}
export interface System_sysparams {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 参数说明
   */
  desc?: string;
  /**
   * 参数键
   */
  key: string;
  /**
   * 参数名称
   */
  name: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  /**
   * 参数值
   */
  value: string;
}
export interface System_sysversion {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 版本描述
   */
  description?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  /**
   * 版本号
   */
  versionCode: string;
  /**
   * 版本数据
   */
  versionData?: string;
  /**
   * 版本名称
   */
  versionName: string;
}
export interface Request_exportversionrequest {
  /**
   * 选中的API ID列表
   */
  apiIds?: number[];
  /**
   * 版本描述
   */
  description?: string;
  /**
   * 选中的字典ID列表
   */
  dictIds?: number[];
  /**
   * 选中的菜单ID列表
   */
  menuIds?: number[];
  /**
   * 版本号
   */
  versionCode: string;
  /**
   * 版本名称
   */
  versionName: string;
}
export interface Request_versioninfo {
  /**
   * 版本号
   */
  code: string;
  /**
   * 版本描述
   */
  description?: string;
  /**
   * 导出时间
   */
  exportTime?: string;
  /**
   * 版本名称
   */
  name: string;
}
export interface Request_importversionrequest {
  /**
   * API数据，直接复用SysApi
   */
  apis?: System_sysapi[];
  /**
   * 字典数据，直接复用SysDictionary
   */
  dictionaries?: System_sysdictionary[];
  /**
   * 菜单数据，直接复用SysBaseMenu
   */
  menus?: System_sysbasemenu[];
  /**
   * 版本信息
   */
  version: Request_versioninfo;
}
export interface Config_aliyunoss {
  'access-key-id'?: string;
  'access-key-secret'?: string;
  'base-path'?: string;
  'bucket-name'?: string;
  'bucket-url'?: string;
  endpoint?: string;
}
export interface Config_autocode {
  'ai-path'?: string;
  module?: string;
  root?: string;
  server?: string;
  web?: string;
}
export interface Config_awss3 {
  'base-url'?: string;
  bucket?: string;
  'disable-ssl'?: boolean;
  endpoint?: string;
  'path-prefix'?: string;
  region?: string;
  's3-force-path-style'?: boolean;
  'secret-id'?: string;
  'secret-key'?: string;
}
export interface Config_captcha {
  /**
   * 验证码高度
   */
  'img-height'?: number;
  /**
   * 验证码宽度
   */
  'img-width'?: number;
  /**
   * 验证码长度
   */
  'key-long'?: number;
  /**
   * 防爆破验证码开启此数，0代表每次登录都需要验证码，其他数字代表错误密码次数，如3代表错误三次后出现验证码
   */
  'open-captcha'?: number;
  /**
   * 防爆破验证码超时时间，单位：s(秒)
   */
  'open-captcha-timeout'?: number;
}
export interface Config_cloudflarer2 {
  'access-key-id'?: string;
  'account-id'?: string;
  'base-url'?: string;
  bucket?: string;
  path?: string;
  'secret-access-key'?: string;
}
export interface Config_corswhitelist {
  'allow-credentials'?: boolean;
  'allow-headers'?: string;
  'allow-methods'?: string;
  'allow-origin'?: string;
  'expose-headers'?: string;
}
export interface Config_cors {
  mode?: string;
  whitelist?: Config_corswhitelist[];
}
export interface Config_specializeddb {
  'alias-name'?: string;
  /**
   * 高级配置
   */
  config?: string;
  /**
   * 数据库名
   */
  'db-name'?: string;
  disable?: boolean;
  /**
   * 数据库引擎，默认InnoDB
   */
  engine?: string;
  /**
   * 是否开启Gorm全局日志
   */
  'log-mode'?: string;
  /**
   * 是否通过zap写入日志文件
   */
  'log-zap'?: boolean;
  /**
   * 空闲中的最大连接数
   */
  'max-idle-conns'?: number;
  /**
   * 打开到数据库的最大连接数
   */
  'max-open-conns'?: number;
  /**
   * 数据库密码
   */
  password?: string;
  /**
   * 数据库地址
   */
  path?: string;
  /**
   * 数据库端口
   */
  port?: string;
  /**
   * 数据库前缀
   */
  prefix?: string;
  /**
   * 是否开启全局禁用复数，true表示开启
   */
  singular?: boolean;
  type?: string;
  /**
   * 数据库账号
   */
  username?: string;
}
export interface Config_disklist {
  'mount-point'?: string;
}
export interface Github_com_flipped_aurora_gin_vue_admin_server_config_email {
  /**
   * 发件人  你自己要发邮件的邮箱
   */
  from?: string;
  /**
   * 服务器地址 例如 smtp.qq.com  请前往QQ或者你要发邮件的邮箱查看其smtp协议
   */
  host?: string;
  /**
   * 是否LoginAuth   是否使用LoginAuth认证方式（适用于IBM、微软邮箱服务器等）
   */
  'is-loginauth'?: boolean;
  /**
   * 是否SSL   是否开启SSL
   */
  'is-ssl'?: boolean;
  /**
   * 昵称    发件人昵称 通常为自己的邮箱
   */
  nickname?: string;
  /**
   * 端口     请前往QQ或者你要发邮件的邮箱查看其smtp协议 大多为 465
   */
  port?: number;
  /**
   * 密钥    用于登录的密钥 最好不要用邮箱密码 去邮箱smtp申请一个用于登录的密钥
   */
  secret?: string;
  /**
   * 收件人:多个以英文逗号分隔 例：a@qq.com b@qq.com 正式开发中请把此项目作为参数使用
   */
  to?: string;
}
export interface Config_excel {
  dir?: string;
}
export interface Config_huaweiobs {
  'access-key'?: string;
  bucket?: string;
  endpoint?: string;
  path?: string;
  'secret-key'?: string;
}
export interface Config_jwt {
  /**
   * 缓冲时间
   */
  'buffer-time'?: string;
  /**
   * 过期时间
   */
  'expires-time'?: string;
  /**
   * 签发者
   */
  issuer?: string;
  /**
   * jwt签名
   */
  'signing-key'?: string;
}
export interface Config_local {
  /**
   * 本地文件访问路径
   */
  path?: string;
  /**
   * 本地文件存储路径
   */
  'store-path'?: string;
}
export interface Config_mcp {
  /**
   * 独立MCP服务端口
   */
  addr?: number;
  /**
   * 消息路径
   */
  message_path?: string;
  /**
   * MCP名称
   */
  name?: string;
  /**
   * 是否独立运行MCP服务
   */
  separate?: boolean;
  /**
   * SSE路径
   */
  sse_path?: string;
  /**
   * URL前缀
   */
  url_prefix?: string;
  /**
   * MCP版本
   */
  version?: string;
}
export interface Config_minio {
  'access-key-id'?: string;
  'access-key-secret'?: string;
  'base-path'?: string;
  'bucket-name'?: string;
  'bucket-url'?: string;
  endpoint?: string;
  'use-ssl'?: boolean;
}
export interface Config_mongohost {
  /**
   * ip地址
   */
  host?: string;
  /**
   * 端口
   */
  port?: string;
}
export interface Config_mongo {
  /**
   * 验证数据库
   */
  'auth-source'?: string;
  /**
   * collection name
   */
  coll?: string;
  /**
   * 连接超时时间
   */
  'connect-timeout-ms'?: number;
  /**
   * database name
   */
  database?: string;
  /**
   * 主机列表
   */
  hosts?: Config_mongohost[];
  /**
   * 是否开启zap日志
   */
  'is-zap'?: boolean;
  /**
   * 最大连接池
   */
  'max-pool-size'?: number;
  /**
   * 最小连接池
   */
  'min-pool-size'?: number;
  /**
   * mongodb options
   */
  options?: string;
  /**
   * 密码
   */
  password?: string;
  /**
   * socket超时时间
   */
  'socket-timeout-ms'?: number;
  /**
   * 用户名
   */
  username?: string;
}
export interface Config_mssql {
  /**
   * 高级配置
   */
  config?: string;
  /**
   * 数据库名
   */
  'db-name'?: string;
  /**
   * 数据库引擎，默认InnoDB
   */
  engine?: string;
  /**
   * 是否开启Gorm全局日志
   */
  'log-mode'?: string;
  /**
   * 是否通过zap写入日志文件
   */
  'log-zap'?: boolean;
  /**
   * 空闲中的最大连接数
   */
  'max-idle-conns'?: number;
  /**
   * 打开到数据库的最大连接数
   */
  'max-open-conns'?: number;
  /**
   * 数据库密码
   */
  password?: string;
  /**
   * 数据库地址
   */
  path?: string;
  /**
   * 数据库端口
   */
  port?: string;
  /**
   * 数据库前缀
   */
  prefix?: string;
  /**
   * 是否开启全局禁用复数，true表示开启
   */
  singular?: boolean;
  /**
   * 数据库账号
   */
  username?: string;
}
export interface Config_mysql {
  /**
   * 高级配置
   */
  config?: string;
  /**
   * 数据库名
   */
  'db-name'?: string;
  /**
   * 数据库引擎，默认InnoDB
   */
  engine?: string;
  /**
   * 是否开启Gorm全局日志
   */
  'log-mode'?: string;
  /**
   * 是否通过zap写入日志文件
   */
  'log-zap'?: boolean;
  /**
   * 空闲中的最大连接数
   */
  'max-idle-conns'?: number;
  /**
   * 打开到数据库的最大连接数
   */
  'max-open-conns'?: number;
  /**
   * 数据库密码
   */
  password?: string;
  /**
   * 数据库地址
   */
  path?: string;
  /**
   * 数据库端口
   */
  port?: string;
  /**
   * 数据库前缀
   */
  prefix?: string;
  /**
   * 是否开启全局禁用复数，true表示开启
   */
  singular?: boolean;
  /**
   * 数据库账号
   */
  username?: string;
}
export interface Config_oracle {
  /**
   * 高级配置
   */
  config?: string;
  /**
   * 数据库名
   */
  'db-name'?: string;
  /**
   * 数据库引擎，默认InnoDB
   */
  engine?: string;
  /**
   * 是否开启Gorm全局日志
   */
  'log-mode'?: string;
  /**
   * 是否通过zap写入日志文件
   */
  'log-zap'?: boolean;
  /**
   * 空闲中的最大连接数
   */
  'max-idle-conns'?: number;
  /**
   * 打开到数据库的最大连接数
   */
  'max-open-conns'?: number;
  /**
   * 数据库密码
   */
  password?: string;
  /**
   * 数据库地址
   */
  path?: string;
  /**
   * 数据库端口
   */
  port?: string;
  /**
   * 数据库前缀
   */
  prefix?: string;
  /**
   * 是否开启全局禁用复数，true表示开启
   */
  singular?: boolean;
  /**
   * 数据库账号
   */
  username?: string;
}
export interface Config_pgsql {
  /**
   * 高级配置
   */
  config?: string;
  /**
   * 数据库名
   */
  'db-name'?: string;
  /**
   * 数据库引擎，默认InnoDB
   */
  engine?: string;
  /**
   * 是否开启Gorm全局日志
   */
  'log-mode'?: string;
  /**
   * 是否通过zap写入日志文件
   */
  'log-zap'?: boolean;
  /**
   * 空闲中的最大连接数
   */
  'max-idle-conns'?: number;
  /**
   * 打开到数据库的最大连接数
   */
  'max-open-conns'?: number;
  /**
   * 数据库密码
   */
  password?: string;
  /**
   * 数据库地址
   */
  path?: string;
  /**
   * 数据库端口
   */
  port?: string;
  /**
   * 数据库前缀
   */
  prefix?: string;
  /**
   * 是否开启全局禁用复数，true表示开启
   */
  singular?: boolean;
  /**
   * 数据库账号
   */
  username?: string;
}
export interface Config_qiniu {
  /**
   * 秘钥AK
   */
  'access-key'?: string;
  /**
   * 空间名称
   */
  bucket?: string;
  /**
   * CDN加速域名
   */
  'img-path'?: string;
  /**
   * 秘钥SK
   */
  'secret-key'?: string;
  /**
   * 上传是否使用CDN上传加速
   */
  'use-cdn-domains'?: boolean;
  /**
   * 是否使用https
   */
  'use-https'?: boolean;
  /**
   * 存储区域
   */
  zone?: string;
}
export interface Config_redis {
  /**
   * 服务器地址:端口
   */
  addr?: string;
  /**
   * 集群模式下的节点地址列表
   */
  clusterAddrs?: string[];
  /**
   * 单实例模式下redis的哪个数据库
   */
  db?: number;
  /**
   * 代表当前实例的名字
   */
  name?: string;
  /**
   * 密码
   */
  password?: string;
  /**
   * 是否使用集群模式
   */
  useCluster?: boolean;
}
export interface Config_sqlite {
  /**
   * 高级配置
   */
  config?: string;
  /**
   * 数据库名
   */
  'db-name'?: string;
  /**
   * 数据库引擎，默认InnoDB
   */
  engine?: string;
  /**
   * 是否开启Gorm全局日志
   */
  'log-mode'?: string;
  /**
   * 是否通过zap写入日志文件
   */
  'log-zap'?: boolean;
  /**
   * 空闲中的最大连接数
   */
  'max-idle-conns'?: number;
  /**
   * 打开到数据库的最大连接数
   */
  'max-open-conns'?: number;
  /**
   * 数据库密码
   */
  password?: string;
  /**
   * 数据库地址
   */
  path?: string;
  /**
   * 数据库端口
   */
  port?: string;
  /**
   * 数据库前缀
   */
  prefix?: string;
  /**
   * 是否开启全局禁用复数，true表示开启
   */
  singular?: boolean;
  /**
   * 数据库账号
   */
  username?: string;
}
export interface Config_system {
  /**
   * 端口值
   */
  addr?: number;
  /**
   * 数据库类型:mysql(默认)|sqlite|sqlserver|postgresql
   */
  'db-type'?: string;
  /**
   * 自动迁移数据库表结构，生产环境建议设为false，手动迁移
   */
  'disable-auto-migrate'?: boolean;
  'iplimit-count'?: number;
  'iplimit-time'?: number;
  /**
   * Oss类型
   */
  'oss-type'?: string;
  'router-prefix'?: string;
  /**
   * 使用mongo
   */
  'use-mongo'?: boolean;
  /**
   * 多点登录拦截
   */
  'use-multipoint'?: boolean;
  /**
   * 使用redis
   */
  'use-redis'?: boolean;
  /**
   * 使用树形角色分配模式
   */
  'use-strict-auth'?: boolean;
}
export interface Config_tencentcos {
  'base-url'?: string;
  bucket?: string;
  'path-prefix'?: string;
  region?: string;
  'secret-id'?: string;
  'secret-key'?: string;
}
export interface Config_zap {
  /**
   * 日志文件夹
   */
  director?: string;
  /**
   * 编码级
   */
  'encode-level'?: string;
  /**
   * 输出
   */
  format?: string;
  /**
   * 级别
   */
  level?: string;
  /**
   * 输出控制台
   */
  'log-in-console'?: boolean;
  /**
   * 日志前缀
   */
  prefix?: string;
  /**
   * 日志保留天数
   */
  'retention-day'?: number;
  /**
   * 显示行
   */
  'show-line'?: boolean;
  /**
   * 栈名
   */
  'stacktrace-key'?: string;
}
export interface Config_server {
  'aliyun-oss'?: Config_aliyunoss;
  /**
   * auto
   */
  autocode?: Config_autocode;
  'aws-s3'?: Config_awss3;
  captcha?: Config_captcha;
  'cloudflare-r2'?: Config_cloudflarer2;
  /**
   * 跨域配置
   */
  cors?: Config_cors;
  'db-list'?: Config_specializeddb[];
  'disk-list'?: Config_disklist[];
  email?: Github_com_flipped_aurora_gin_vue_admin_server_config_email;
  excel?: Config_excel;
  'hua-wei-obs'?: Config_huaweiobs;
  jwt?: Config_jwt;
  /**
   * oss
   */
  local?: Config_local;
  /**
   * MCP配置
   */
  mcp?: Config_mcp;
  minio?: Config_minio;
  mongo?: Config_mongo;
  mssql?: Config_mssql;
  /**
   * gorm
   */
  mysql?: Config_mysql;
  oracle?: Config_oracle;
  pgsql?: Config_pgsql;
  qiniu?: Config_qiniu;
  redis?: Config_redis;
  'redis-list'?: Config_redis[];
  sqlite?: Config_sqlite;
  system?: Config_system;
  'tencent-cos'?: Config_tencentcos;
  zap?: Config_zap;
}
export interface System_system {
  config?: Config_server;
}
export interface Miniprogram_timeslots {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  createdBy?: number;
  deletedBy?: number;
  /**
   * 结束时间
   */
  end_time?: string;
  /**
   * 适用楼层 ID，NULL 表示全局时段
   */
  floorId?: number;
  /**
   * 时段标签，例如 10:00-11:00
   */
  label?: string;
  /**
   * 排序
   */
  slot_order?: number;
  /**
   * 起始时间
   */
  startTime?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
  updatedBy?: number;
}
export interface Request_register {
  authorityId?: string;
  authorityIds?: string;
  email?: string;
  enable?: string;
  headerImg?: string;
  nickName?: string;
  passWord?: string;
  phone?: string;
  userName?: string;
}
export interface Request_changepasswordreq {
  /**
   * 新密码
   */
  newPassword?: string;
  /**
   * 密码
   */
  password?: string;
}
export interface Request_getuserlist {
  email?: string;
  /**
   * 关键字
   */
  keyword?: string;
  nickName?: string;
  /**
   * 页码
   */
  page?: number;
  /**
   * 每页大小
   */
  pageSize?: number;
  phone?: string;
  username?: string;
}
export interface Request_setuserauthorities {
  /**
   * 角色ID
   */
  authorityIds?: number[];
  id?: number;
}
export interface Request_setuserauth {
  /**
   * 角色ID
   */
  authorityId?: number;
}
export interface Miniprogram_wechatuser {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 头像URL
   */
  avatar_url?: string;
  bath_count?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  createdBy?: number;
  /**
   * 默认设备ID
   */
  default_device_id?: number;
  deletedBy?: number;
  /**
   * 加解密密钥
   */
  encryption_key?: string;
  /**
   * 性别：0-未知，1-男，2-女
   */
  gender: number;
  /**
   * 身份证反面照URL
   */
  id_car_back?: string;
  /**
   * 身份证号
   */
  id_card: string;
  /**
   * 身份证正面照URL
   */
  id_card_front: string;
  /**
   * 手机号
   */
  mobile: string;
  /**
   * 用户昵称
   */
  nick_name: string;
  /**
   * 微信openid
   */
  open_id?: string;
  /**
   * 用户积分
   */
  points?: number;
  /**
   * 真实姓名
   */
  real_name: string;
  /**
   * 微信会话密钥
   */
  session_key?: string;
  /**
   * 状态：0-正常，1-禁用
   */
  status: string;
  /**
   * 累计消费
   */
  total_consumption?: number;
  /**
   * 累计充值
   */
  total_recharge?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
  updatedBy?: number;
  /**
   * 会员卡id
   */
  vip_card?: string;
  /**
   * 会员卡等级0无会员 1体验会员 2 vip会员 3 银卡会员 4金卡会员 5钻石会员
   */
  vip_level?: number;
  /**
   * 用户卡包余额
   */
  wallet_balance?: number;
}
export interface Response_response {
  code?: number;
  data?: null;
  msg?: string;
}
export interface Response_sysapilistresponse {
  apis?: System_sysapi[];
}
export interface Response_syscaptcharesponse {
  captchaId?: string;
  captchaLength?: number;
  openCaptcha?: boolean;
  picPath?: string;
}
export interface Response_sysconfigresponse {
  config?: Config_server;
}
export interface Transformer_consumptiontransformer {
  /**
   * 交易金额
   */
  amount?: number;
  create_at?: string;
  id?: number;
  /**
   * 备注
   */
  remark?: string;
  /**
   * 交易类型
   */
  transaction_type: string;
  /**
   * 用户卡包余额
   */
  wallet_balance?: number;
}
export interface Transformer_devicetransformer {
  create_at?: string;
  /**
   * 设备代码
   */
  device_code: string;
  /**
   * 设备数据
   */
  device_data?: object;
  /**
   * 设备名称
   */
  device_name: string;
  id?: number;
}
export interface Response_sysapiresponse {
  api?: System_sysapi;
}
export interface Response_pageresult {
  list?: null;
  page?: number;
  pageSize?: number;
  total?: number;
}
export interface Transformer_producttransformer {
  /**
   * id
   */
  id?: number;
  /**
   * 商品图片
   */
  image?: string;
  /**
   * 净含量
   */
  net_weight: string;
  /**
   * 产地
   */
  origin: string;
  /**
   * 原价
   */
  original_price: number;
  /**
   * 商品分类
   */
  product_category?: string;
  /**
   * 商品名称
   */
  product_name: string;
  /**
   * 销售数
   */
  sales_count?: number;
  /**
   * 售价
   */
  selling_price: number;
  /**
   * 保质期
   */
  shelfLife: string;
}
export interface Transformer_reservationstransformer {
  /**
   * 预约日期（yyyy-mm-dd）
   */
  date?: string;
  note?: string;
  room?: {
    /**
     * 所属楼层 ID
     */
    floorId?: number;
    /**
     * 房间名称
     */
    name?: string;
  };
  /**
   * 房间 ID
   */
  roomId?: number;
  /**
   * 预约状态：RESERVED-预约，CANCELLED-取消预约
   */
  status?: string;
  /**
   * 时段 ID
   */
  timeSlotId?: number;
}
export interface Response_sysauthorityresponse {
  authority?: System_sysauthority;
}
export interface Response_sysauthoritybtnres {
  selected?: number[];
}
export interface Response_loginresponse {
  expiresAt?: number;
  token?: string;
  user?: System_sysuser;
}
export interface Response_policypathresponse {
  paths?: Request_casbininfo[];
}
export interface Response_exacustomerresponse {
  customer?: Example_exacustomer;
}
export interface Example_exafilechunk {
  /**
   * 主键ID
   */
  ID?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  exaFileID?: number;
  fileChunkNumber?: number;
  fileChunkPath?: string;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface Example_exafile {
  /**
   * 主键ID
   */
  ID?: number;
  chunkTotal?: number;
  /**
   * 创建时间
   */
  createdAt?: string;
  exaFileChunk?: Example_exafilechunk[];
  fileMd5?: string;
  fileName?: string;
  filePath?: string;
  isFinish?: boolean;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface Response_fileresponse {
  file?: Example_exafile;
}
export interface Response_filepathresponse {
  filePath?: string;
}
export interface Response_exafileresponse {
  file?: Example_exafileuploadanddownload;
}
export interface Response_sysbasemenuresponse {
  menu?: System_sysbasemenu;
}
export interface Response_sysbasemenusresponse {
  menus?: System_sysbasemenu[];
}
export interface System_sysmenu {
  /**
   * 主键ID
   */
  ID?: number;
  authoritys?: System_sysauthority[];
  btns?: Record<string, number>;
  children?: System_sysmenu[];
  /**
   * 对应前端文件路径
   */
  component?: string;
  /**
   * 创建时间
   */
  createdAt?: string;
  /**
   * 是否在列表隐藏
   */
  hidden?: boolean;
  menuBtn?: System_sysbasemenubtn[];
  menuId?: number;
  /**
   * 附加属性
   */
  meta?: System_meta;
  /**
   * 路由name
   */
  name?: string;
  parameters?: System_sysbasemenuparameter[];
  /**
   * 父菜单ID
   */
  parentId?: number;
  /**
   * 路由path
   */
  path?: string;
  /**
   * 排序标记
   */
  sort?: number;
  /**
   * 更新时间
   */
  updatedAt?: string;
}
export interface Response_sysmenusresponse {
  menus?: System_sysmenu[];
}
export interface Response_sysuserresponse {
  user?: System_sysuser;
}
declare global {
  interface Apis {
    general: {
      /**
       * ---
       *
       * [GET] 分页获取消费明细列表
       *
       * **path:** /api/consumption
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   endAmount?: number
       *   // 关键字
       *   keyword?: string
       *   order?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   remark?: string
       *   sort?: string
       *   startAmount?: number
       *   transaction_type?: string
       *   userId?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 交易金额
       *     amount?: number
       *     create_at?: string
       *     id?: number
       *     // 备注
       *     remark?: string
       *     // 交易类型
       *     transaction_type: string
       *     // 用户卡包余额
       *     wallet_balance?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_consumption<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Transformer_consumptiontransformer;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            endAmount?: number;
            /**
             * 关键字
             */
            keyword?: string;
            order?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            remark?: string;
            sort?: string;
            startAmount?: number;
            transaction_type?: string;
            userId?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Transformer_consumptiontransformer;
          msg?: string;
        },
        'general.get_api_consumption',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询消费明细
       *
       * **path:** /api/consumption/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 交易金额
       *     amount?: number
       *     create_at?: string
       *     id?: number
       *     // 备注
       *     remark?: string
       *     // 交易类型
       *     transaction_type: string
       *     // 用户卡包余额
       *     wallet_balance?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_consumption_id<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Transformer_consumptiontransformer;
            msg?: string;
          }
        > & {
          pathParams: {
            /**
             * ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Transformer_consumptiontransformer;
          msg?: string;
        },
        'general.get_api_consumption_id',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取楼层表列表
       *
       * **path:** /api/floors
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   // 关键字
       *   keyword?: string
       *   name?: string
       *   order?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   sort?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = object & {
       *   code?: number
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       id?: number
       *       name?: string
       *     }>
       *     total?: number
       *     page?: number
       *     pageSize?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_floors<
        Config extends Alova2MethodConfig<
          object & {
            code?: number;
            data?: {
              list?: Array<{
                id?: number;
                name?: string;
              }>;
              total?: number;
              page?: number;
              pageSize?: number;
            };
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            /**
             * 关键字
             */
            keyword?: string;
            name?: string;
            order?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            sort?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        object & {
          code?: number;
          data?: {
            list?: Array<{
              id?: number;
              name?: string;
            }>;
            total?: number;
            page?: number;
            pageSize?: number;
          };
          msg?: string;
        },
        'general.get_api_floors',
        Config
      >;
      /**
       * ---
       *
       * [GET]
       *
       * **path:** /api/order/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_order_id<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          pathParams: {
            /**
             * ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'general.get_api_order_id',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取商品列表
       *
       * **path:** /api/product
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   endOriginalPrice?: number
       *   endSalesCount?: number
       *   endSellingPrice?: number
       *   // 关键字
       *   keyword?: string
       *   order?: string
       *   origin?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   product_name?: string
       *   shelfLife?: string
       *   sort?: string
       *   startOriginalPrice?: number
       *   startSalesCount?: number
       *   startSellingPrice?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // id
       *     id?: number
       *     // 商品图片
       *     image?: string
       *     // 净含量
       *     net_weight: string
       *     // 产地
       *     origin: string
       *     // 原价
       *     original_price: number
       *     // 商品分类
       *     product_category?: string
       *     // 商品名称
       *     product_name: string
       *     // 销售数
       *     sales_count?: number
       *     // 售价
       *     selling_price: number
       *     // 保质期
       *     shelfLife: string
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_product<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Transformer_producttransformer;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            endOriginalPrice?: number;
            endSalesCount?: number;
            endSellingPrice?: number;
            /**
             * 关键字
             */
            keyword?: string;
            order?: string;
            origin?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            product_name?: string;
            shelfLife?: string;
            sort?: string;
            startOriginalPrice?: number;
            startSalesCount?: number;
            startSellingPrice?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Transformer_producttransformer;
          msg?: string;
        },
        'general.get_api_product',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取reservations表列表
       *
       * **path:** /api/reservations
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   // [items] start
       *   // [items] end
       *   dateRange?: string[]
       *   // 关键字
       *   keyword?: string
       *   order?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   sort?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 预约日期（yyyy-mm-dd）
       *     date?: string
       *     note?: string
       *     room?: {
       *       // 所属楼层 ID
       *       floorId?: number
       *       // 房间名称
       *       name?: string
       *     }
       *     // 房间 ID
       *     roomId?: number
       *     // 预约状态：RESERVED-预约，CANCELLED-取消预约
       *     status?: string
       *     // 时段 ID
       *     timeSlotId?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_reservations<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Transformer_reservationstransformer;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            dateRange?: string[];
            /**
             * 关键字
             */
            keyword?: string;
            order?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            sort?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Transformer_reservationstransformer;
          msg?: string;
        },
        'general.get_api_reservations',
        Config
      >;
      /**
       * ---
       *
       * [POST] 预定房间的时段
       *
       * **path:** /api/reservations
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 预约日期（yyyy-mm-dd的Date类型）
       *   date?: string
       *   // 备注
       *   note?: string
       *   // 房间 ID
       *   roomId?: number
       *   // 时段 ID
       *   timeSlotId?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_api_reservations<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_reservationsrequest;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'general.post_api_reservations',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 用id取消预约
       *
       * **path:** /api/reservations/cancelled/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 用id查询reservations表
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 预约日期（yyyy-mm-dd）
       *     date?: string
       *     note?: string
       *     room?: {
       *       // 所属楼层 ID
       *       floorId?: number
       *       // 房间名称
       *       name?: string
       *     }
       *     // 房间 ID
       *     roomId?: number
       *     // 预约状态：RESERVED-预约，CANCELLED-取消预约
       *     status?: string
       *     // 时段 ID
       *     timeSlotId?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      put_api_reservations_cancelled_id<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Transformer_reservationstransformer;
            msg?: string;
          }
        > & {
          pathParams: {
            /**
             * 用id查询reservations表
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Transformer_reservationstransformer;
          msg?: string;
        },
        'general.put_api_reservations_cancelled_id',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的reservations表接口
       *
       * **path:** /api/reservations/room/{roomId}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 房间ID
       *   roomId: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 日期(YYYY-MM-DD)
       *   date: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = object & {
       *   code?: number
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     availables?: Array<{
       *       timeSlotId?: number
       *       label?: string
       *       startTime?: string
       *       endTime?: string
       *       available?: boolean
       *     }>
       *     date?: string
       *     roomId?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_reservations_room_roomid<
        Config extends Alova2MethodConfig<
          object & {
            code?: number;
            data?: {
              availables?: Array<{
                timeSlotId?: number;
                label?: string;
                startTime?: string;
                endTime?: string;
                available?: boolean;
              }>;
              date?: string;
              roomId?: number;
            };
            msg?: string;
          }
        > & {
          pathParams: {
            /**
             * 房间ID
             */
            roomId: string;
          };
          params: {
            /**
             * 日期(YYYY-MM-DD)
             */
            date: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        object & {
          code?: number;
          data?: {
            availables?: Array<{
              timeSlotId?: number;
              label?: string;
              startTime?: string;
              endTime?: string;
              available?: boolean;
            }>;
            date?: string;
            roomId?: number;
          };
          msg?: string;
        },
        'general.get_api_reservations_room_roomid',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询预定
       *
       * **path:** /api/reservations/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 用id查询reservations表
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 预约日期（yyyy-mm-dd）
       *     date?: string
       *     note?: string
       *     room?: {
       *       // 所属楼层 ID
       *       floorId?: number
       *       // 房间名称
       *       name?: string
       *     }
       *     // 房间 ID
       *     roomId?: number
       *     // 预约状态：RESERVED-预约，CANCELLED-取消预约
       *     status?: string
       *     // 时段 ID
       *     timeSlotId?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_reservations_id<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Transformer_reservationstransformer;
            msg?: string;
          }
        > & {
          pathParams: {
            /**
             * 用id查询reservations表
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Transformer_reservationstransformer;
          msg?: string;
        },
        'general.get_api_reservations_id',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取rooms表列表
       *
       * **path:** /api/rooms
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   // 所属楼层 ID
       *   floorId?: number
       *   // 关键字
       *   keyword?: string
       *   name?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = object & {
       *   code?: number
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       id?: number
       *       floorId?: number
       *       name?: string
       *     }>
       *     total?: number
       *     page?: number
       *     pageSize?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_rooms<
        Config extends Alova2MethodConfig<
          object & {
            code?: number;
            data?: {
              list?: Array<{
                id?: number;
                floorId?: number;
                name?: string;
              }>;
              total?: number;
              page?: number;
              pageSize?: number;
            };
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            /**
             * 所属楼层 ID
             */
            floorId?: number;
            /**
             * 关键字
             */
            keyword?: string;
            name?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        object & {
          code?: number;
          data?: {
            list?: Array<{
              id?: number;
              floorId?: number;
              name?: string;
            }>;
            total?: number;
            page?: number;
            pageSize?: number;
          };
          msg?: string;
        },
        'general.get_api_rooms',
        Config
      >;
      /**
       * ---
       *
       * [POST] 小程序获取手机号
       *
       * **path:** /api/wechatuser/getphone
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // @Description 724 微信的 encrypted_data
       *   encrypted_data: string
       *   // @Description 724 微信的 iv
       *   iv: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_api_wechatuser_getphone<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_wechatmingetuserinfo;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'general.post_api_wechatuser_getphone',
        Config
      >;
      /**
       * ---
       *
       * [POST] 小程序登入
       *
       * **path:** /api/wechatuser/login
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // @Description 723 微信登入的code
       *   code: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = object & {
       *   code?: number
       *   data?: {
       *     token?: string
       *     userinfo?: {
       *       ID?: number
       *       CreatedAt?: string
       *       UpdatedAt?: string
       *       open_id?: string
       *       nick_name?: object
       *       mobile?: object
       *       real_name?: object
       *       id_card?: object
       *       id_card_front?: object
       *       id_car_back?: object
       *       default_device_id?: number
       *       encryption_key?: object
       *       session_key?: string
       *       avatar_url?: string
       *       gender?: number
       *       status?: string
       *       vip_card?: object
       *       vip_level?: number
       *       points?: number
       *       wallet_balance?: string
       *       total_recharge?: string
       *       total_consumption?: string
       *       bath_count?: number
       *       CreatedBy?: number
       *       UpdatedBy?: number
       *       DeletedBy?: number
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_api_wechatuser_login<
        Config extends Alova2MethodConfig<
          object & {
            code?: number;
            data?: {
              token?: string;
              userinfo?: {
                ID?: number;
                CreatedAt?: string;
                UpdatedAt?: string;
                open_id?: string;
                nick_name?: object;
                mobile?: object;
                real_name?: object;
                id_card?: object;
                id_card_front?: object;
                id_car_back?: object;
                default_device_id?: number;
                encryption_key?: object;
                session_key?: string;
                avatar_url?: string;
                gender?: number;
                status?: string;
                vip_card?: object;
                vip_level?: number;
                points?: number;
                wallet_balance?: string;
                total_recharge?: string;
                total_consumption?: string;
                bath_count?: number;
                CreatedBy?: number;
                UpdatedBy?: number;
                DeletedBy?: number;
              };
            };
            msg?: string;
          }
        > & {
          data: Request_wechatminauthrequest;
        }
      >(
        config: Config
      ): Alova2Method<
        object & {
          code?: number;
          data?: {
            token?: string;
            userinfo?: {
              ID?: number;
              CreatedAt?: string;
              UpdatedAt?: string;
              open_id?: string;
              nick_name?: object;
              mobile?: object;
              real_name?: object;
              id_card?: object;
              id_card_front?: object;
              id_car_back?: object;
              default_device_id?: number;
              encryption_key?: object;
              session_key?: string;
              avatar_url?: string;
              gender?: number;
              status?: string;
              vip_card?: object;
              vip_level?: number;
              points?: number;
              wallet_balance?: string;
              total_recharge?: string;
              total_consumption?: string;
              bath_count?: number;
              CreatedBy?: number;
              UpdatedBy?: number;
              DeletedBy?: number;
            };
          };
          msg?: string;
        },
        'general.post_api_wechatuser_login',
        Config
      >;
    };
    SysApi: {
      /**
       * ---
       *
       * [POST] 创建基础api
       *
       * **path:** /api/createApi
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // api组
       *   apiGroup?: string
       *   // 创建时间
       *   createdAt?: string
       *   // api中文描述
       *   description?: string
       *   // 方法:创建POST(默认)|查看GET|更新PUT|删除DELETE
       *   method?: string
       *   // api路径
       *   path?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_api_createapi<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysapi;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysApi.post_api_createapi',
        Config
      >;
      /**
       * ---
       *
       * [POST] 删除api
       *
       * **path:** /api/deleteApi
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // api组
       *   apiGroup?: string
       *   // 创建时间
       *   createdAt?: string
       *   // api中文描述
       *   description?: string
       *   // 方法:创建POST(默认)|查看GET|更新PUT|删除DELETE
       *   method?: string
       *   // api路径
       *   path?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_api_deleteapi<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysapi;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysApi.post_api_deleteapi',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除选中Api
       *
       * **path:** /api/deleteApisByIds
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // [items] start
       *   // [items] end
       *   ids?: number[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_api_deleteapisbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_idsreq;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysApi.delete_api_deleteapisbyids',
        Config
      >;
      /**
       * ---
       *
       * [POST] 确认同步API
       *
       * **path:** /api/enterSyncApi
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_api_entersyncapi<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysApi.post_api_entersyncapi',
        Config
      >;
      /**
       * ---
       *
       * [GET] 刷新casbin缓存
       *
       * **path:** /api/freshCasbin
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      get_api_freshcasbin<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysApi.get_api_freshcasbin',
        Config
      >;
      /**
       * ---
       *
       * [POST] 获取所有的Api 不分页
       *
       * **path:** /api/getAllApis
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     apis?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // api组
       *       apiGroup?: string
       *       // 创建时间
       *       createdAt?: string
       *       // api中文描述
       *       description?: string
       *       // 方法:创建POST(默认)|查看GET|更新PUT|删除DELETE
       *       method?: string
       *       // api路径
       *       path?: string
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_api_getallapis<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysapilistresponse;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysapilistresponse;
          msg?: string;
        },
        'SysApi.post_api_getallapis',
        Config
      >;
      /**
       * ---
       *
       * [POST] 根据id获取api
       *
       * **path:** /api/getApiById
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     api?: {
       *       // 主键ID
       *       ID?: number
       *       // api组
       *       apiGroup?: string
       *       // 创建时间
       *       createdAt?: string
       *       // api中文描述
       *       description?: string
       *       // 方法:创建POST(默认)|查看GET|更新PUT|删除DELETE
       *       method?: string
       *       // api路径
       *       path?: string
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *   }
       * }
       * ```
       */
      post_api_getapibyid<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysapiresponse;
          }
        > & {
          data: Request_getbyid;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysapiresponse;
        },
        'SysApi.post_api_getapibyid',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取API分组
       *
       * **path:** /api/getApiGroups
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      get_api_getapigroups<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysApi.get_api_getapigroups',
        Config
      >;
      /**
       * ---
       *
       * [POST] 分页获取API列表
       *
       * **path:** /api/getApiList
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // api组
       *   apiGroup?: string
       *   // 创建时间
       *   createdAt?: string
       *   // 排序方式:升序false(默认)|降序true
       *   desc?: boolean
       *   // api中文描述
       *   description?: string
       *   // 关键字
       *   keyword?: string
       *   // 方法:创建POST(默认)|查看GET|更新PUT|删除DELETE
       *   method?: string
       *   // 排序
       *   orderKey?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   // api路径
       *   path?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_api_getapilist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          data: Request_searchapiparams;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'SysApi.post_api_getapilist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 同步API
       *
       * **path:** /api/syncApi
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      get_api_syncapi<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysApi.get_api_syncapi',
        Config
      >;
      /**
       * ---
       *
       * [POST] 修改基础api
       *
       * **path:** /api/updateApi
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // api组
       *   apiGroup?: string
       *   // 创建时间
       *   createdAt?: string
       *   // api中文描述
       *   description?: string
       *   // 方法:创建POST(默认)|查看GET|更新PUT|删除DELETE
       *   method?: string
       *   // api路径
       *   path?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_api_updateapi<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysapi;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysApi.post_api_updateapi',
        Config
      >;
    };
    device: {
      /**
       * ---
       *
       * [GET] 分页获取设备管理列表
       *
       * **path:** /api/device
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   device_code?: string
       *   device_name?: string
       *   // 关键字
       *   keyword?: string
       *   order?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   sort?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     create_at?: string
       *     // 设备代码
       *     device_code: string
       *     // 设备数据
       *     device_data?: object
       *     // 设备名称
       *     device_name: string
       *     id?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_device<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Transformer_devicetransformer;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            device_code?: string;
            device_name?: string;
            /**
             * 关键字
             */
            keyword?: string;
            order?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            sort?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Transformer_devicetransformer;
          msg?: string;
        },
        'device.get_api_device',
        Config
      >;
      /**
       * ---
       *
       * [POST] 创建设备管理
       *
       * **path:** /api/device
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 设备代码
       *   device_code: string
       *   // 设备数据
       *   device_data?: object
       *   // 设备名称
       *   device_name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_api_device<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_devicerequest;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'device.post_api_device',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询设备管理
       *
       * **path:** /api/device/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     create_at?: string
       *     // 设备代码
       *     device_code: string
       *     // 设备数据
       *     device_data?: object
       *     // 设备名称
       *     device_name: string
       *     id?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_device_id<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Transformer_devicetransformer;
            msg?: string;
          }
        > & {
          pathParams: {
            /**
             * ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Transformer_devicetransformer;
          msg?: string;
        },
        'device.get_api_device_id',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新设备管理
       *
       * **path:** /api/device/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 设备代码
       *   device_code: string
       *   // 设备数据
       *   device_data?: object
       *   // 设备名称
       *   device_name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_api_device_id<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          pathParams: {
            /**
             * ID
             */
            id: number;
          };
          data: Request_devicerequest;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'device.put_api_device_id',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除设备管理
       *
       * **path:** /api/device/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_api_device_id<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          pathParams: {
            /**
             * ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'device.delete_api_device_id',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询设备管理
       *
       * **path:** /device/findDevice
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 用id查询设备管理
       *   ID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     // 设备代码
       *     device_code?: string
       *     // 设备数据
       *     device_data?: object
       *     // 设备名称
       *     device_name?: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 用户id
       *     user_id?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_device_finddevice<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Miniprogram_device;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 用id查询设备管理
             */
            ID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Miniprogram_device;
          msg?: string;
        },
        'device.get_device_finddevice',
        Config
      >;
    };
    IgnoreApi: {
      /**
       * ---
       *
       * [POST] 忽略API
       *
       * **path:** /api/ignoreApi
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_api_ignoreapi<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'IgnoreApi.post_api_ignoreapi',
        Config
      >;
    };
    order: {
      /**
       * ---
       *
       * [GET] 分页获取订单列表
       *
       * **path:** /api/order
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   actual_delivery_timeRange?: string[]
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   endOrderAmount?: number
       *   // [items] start
       *   // [items] end
       *   expected_delivery_timeRange?: string[]
       *   // 关键字
       *   keyword?: string
       *   order?: string
       *   order_status?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   receiver_name?: string
       *   sort?: string
       *   startOrderAmount?: number
       *   user_phone?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_api_order<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            actual_delivery_timeRange?: string[];
            createdAtRange?: string[];
            endOrderAmount?: number;
            expected_delivery_timeRange?: string[];
            /**
             * 关键字
             */
            keyword?: string;
            order?: string;
            order_status?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            receiver_name?: string;
            sort?: string;
            startOrderAmount?: number;
            user_phone?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'order.get_api_order',
        Config
      >;
      /**
       * ---
       *
       * [POST] 创建订单
       *
       * **path:** /api/order
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 配送地址
       *   delivery_address: string
       *   // 配送备注
       *   delivery_note?: string
       *   // 商品ID 	//用户ID
       *   product_id: number
       *   // 收货人姓名
       *   receiver_name: string
       *   // 用户手机号
       *   user_phone: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_api_order<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_orderrequest;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'order.post_api_order',
        Config
      >;
      /**
       * ---
       *
       * [POST]
       *
       * **path:** /api/order/pay/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_api_order_pay_id<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          pathParams: {
            /**
             * ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'order.post_api_order_pay_id',
        Config
      >;
    };
    AddCategory: {
      /**
       * ---
       *
       * [POST] 添加媒体库分类
       *
       * **path:** /attachmentCategory/addCategory
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: Example_exaattachmentcategory[]
       *   // 创建时间
       *   createdAt?: string
       *   name?: string
       *   pid?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      post_attachmentcategory_addcategory<
        Config extends Alova2MethodConfig<unknown> & {
          data: Example_exaattachmentcategory;
        }
      >(
        config: Config
      ): Alova2Method<unknown, 'AddCategory.post_attachmentcategory_addcategory', Config>;
    };
    DeleteCategory: {
      /**
       * ---
       *
       * [POST] 删除分类
       *
       * **path:** /attachmentCategory/deleteCategory
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_attachmentcategory_deletecategory<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_getbyid;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'DeleteCategory.post_attachmentcategory_deletecategory',
        Config
      >;
    };
    GetCategoryList: {
      /**
       * ---
       *
       * [GET] 媒体库分类列表
       *
       * **path:** /attachmentCategory/getCategoryList
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // [items] start
       *     // [cycle] $.data
       *     // [items] end
       *     children?: Example_exaattachmentcategory[]
       *     // 创建时间
       *     createdAt?: string
       *     name?: string
       *     pid?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_attachmentcategory_getcategorylist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Example_exaattachmentcategory;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Example_exaattachmentcategory;
          msg?: string;
        },
        'GetCategoryList.get_attachmentcategory_getcategorylist',
        Config
      >;
    };
    Authority: {
      /**
       * ---
       *
       * [POST] 拷贝角色
       *
       * **path:** /authority/copyAuthority
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   authority?: {
       *     // 角色ID
       *     authorityId?: number
       *     // 角色名
       *     authorityName?: string
       *     // [items] start
       *     // [cycle] $.authority
       *     // [items] end
       *     children?: System_sysauthority[]
       *     // 创建时间
       *     createdAt?: string
       *     // [items] start
       *     // [cycle] $.authority
       *     // [items] end
       *     dataAuthorityId?: System_sysauthority[]
       *     // 默认菜单(默认dashboard)
       *     defaultRouter?: string
       *     deletedAt?: string
       *     // [items] start
       *     // [items] end
       *     menus?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // [items] start
       *       // [cycle] $.authority
       *       // [items] end
       *       authoritys?: System_sysauthority[]
       *       // [items] start
       *       // [cycle] $.authority.menus.[]
       *       // [items] end
       *       children?: System_sysbasemenu[]
       *       // 对应前端文件路径
       *       component?: string
       *       // 创建时间
       *       createdAt?: string
       *       // 是否在列表隐藏
       *       hidden?: boolean
       *       // [items] start
       *       // [items] end
       *       menuBtn?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         desc?: string
       *         name?: string
       *         sysBaseMenuID?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 附加属性
       *       meta?: {
       *         activeName?: string
       *         // 自动关闭tab
       *         closeTab?: boolean
       *         // 是否是基础路由（开发中）
       *         defaultMenu?: boolean
       *         // 菜单图标
       *         icon?: string
       *         // 是否缓存
       *         keepAlive?: boolean
       *         // 菜单名
       *         title?: string
       *         // 路由切换动画
       *         transitionType?: string
       *       }
       *       // 路由name
       *       name?: string
       *       // [items] start
       *       // [items] end
       *       parameters?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 地址栏携带参数的key
       *         key?: string
       *         sysBaseMenuID?: number
       *         // 地址栏携带参数为params还是query
       *         type?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 地址栏携带参数的值
       *         value?: string
       *       }>
       *       // 父菜单ID
       *       parentId?: number
       *       // 路由path
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 父角色ID
       *     parentId?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }
       *   // 旧角色ID
       *   oldAuthorityId?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     authority?: {
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.data.authority
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.data.authority
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.data.authority
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.data.authority.menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_authority_copyauthority<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysauthorityresponse;
            msg?: string;
          }
        > & {
          data: Response_sysauthoritycopyresponse;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysauthorityresponse;
          msg?: string;
        },
        'Authority.post_authority_copyauthority',
        Config
      >;
      /**
       * ---
       *
       * [POST] 创建角色
       *
       * **path:** /authority/createAuthority
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 角色ID
       *   authorityId?: number
       *   // 角色名
       *   authorityName?: string
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysauthority[]
       *   // 创建时间
       *   createdAt?: string
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   dataAuthorityId?: System_sysauthority[]
       *   // 默认菜单(默认dashboard)
       *   defaultRouter?: string
       *   deletedAt?: string
       *   // [items] start
       *   // [items] end
       *   menus?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // [items] start
       *     // [cycle] $
       *     // [items] end
       *     authoritys?: System_sysauthority[]
       *     // [items] start
       *     // [cycle] $.menus.[]
       *     // [items] end
       *     children?: System_sysbasemenu[]
       *     // 对应前端文件路径
       *     component?: string
       *     // 创建时间
       *     createdAt?: string
       *     // 是否在列表隐藏
       *     hidden?: boolean
       *     // [items] start
       *     // [items] end
       *     menuBtn?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       desc?: string
       *       name?: string
       *       sysBaseMenuID?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 附加属性
       *     meta?: {
       *       activeName?: string
       *       // 自动关闭tab
       *       closeTab?: boolean
       *       // 是否是基础路由（开发中）
       *       defaultMenu?: boolean
       *       // 菜单图标
       *       icon?: string
       *       // 是否缓存
       *       keepAlive?: boolean
       *       // 菜单名
       *       title?: string
       *       // 路由切换动画
       *       transitionType?: string
       *     }
       *     // 路由name
       *     name?: string
       *     // [items] start
       *     // [items] end
       *     parameters?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       // 地址栏携带参数的key
       *       key?: string
       *       sysBaseMenuID?: number
       *       // 地址栏携带参数为params还是query
       *       type?: string
       *       // 更新时间
       *       updatedAt?: string
       *       // 地址栏携带参数的值
       *       value?: string
       *     }>
       *     // 父菜单ID
       *     parentId?: number
       *     // 路由path
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 父角色ID
       *   parentId?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     authority?: {
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.data.authority
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.data.authority
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.data.authority
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.data.authority.menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_authority_createauthority<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysauthorityresponse;
            msg?: string;
          }
        > & {
          data: System_sysauthority;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysauthorityresponse;
          msg?: string;
        },
        'Authority.post_authority_createauthority',
        Config
      >;
      /**
       * ---
       *
       * [POST] 删除角色
       *
       * **path:** /authority/deleteAuthority
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 角色ID
       *   authorityId?: number
       *   // 角色名
       *   authorityName?: string
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysauthority[]
       *   // 创建时间
       *   createdAt?: string
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   dataAuthorityId?: System_sysauthority[]
       *   // 默认菜单(默认dashboard)
       *   defaultRouter?: string
       *   deletedAt?: string
       *   // [items] start
       *   // [items] end
       *   menus?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // [items] start
       *     // [cycle] $
       *     // [items] end
       *     authoritys?: System_sysauthority[]
       *     // [items] start
       *     // [cycle] $.menus.[]
       *     // [items] end
       *     children?: System_sysbasemenu[]
       *     // 对应前端文件路径
       *     component?: string
       *     // 创建时间
       *     createdAt?: string
       *     // 是否在列表隐藏
       *     hidden?: boolean
       *     // [items] start
       *     // [items] end
       *     menuBtn?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       desc?: string
       *       name?: string
       *       sysBaseMenuID?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 附加属性
       *     meta?: {
       *       activeName?: string
       *       // 自动关闭tab
       *       closeTab?: boolean
       *       // 是否是基础路由（开发中）
       *       defaultMenu?: boolean
       *       // 菜单图标
       *       icon?: string
       *       // 是否缓存
       *       keepAlive?: boolean
       *       // 菜单名
       *       title?: string
       *       // 路由切换动画
       *       transitionType?: string
       *     }
       *     // 路由name
       *     name?: string
       *     // [items] start
       *     // [items] end
       *     parameters?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       // 地址栏携带参数的key
       *       key?: string
       *       sysBaseMenuID?: number
       *       // 地址栏携带参数为params还是query
       *       type?: string
       *       // 更新时间
       *       updatedAt?: string
       *       // 地址栏携带参数的值
       *       value?: string
       *     }>
       *     // 父菜单ID
       *     parentId?: number
       *     // 路由path
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 父角色ID
       *   parentId?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_authority_deleteauthority<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysauthority;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Authority.post_authority_deleteauthority',
        Config
      >;
      /**
       * ---
       *
       * [POST] 分页获取角色列表
       *
       * **path:** /authority/getAuthorityList
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 关键字
       *   keyword?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_authority_getauthoritylist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          data: Request_pageinfo;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'Authority.post_authority_getauthoritylist',
        Config
      >;
      /**
       * ---
       *
       * [POST] 设置角色资源权限
       *
       * **path:** /authority/setDataAuthority
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 角色ID
       *   authorityId?: number
       *   // 角色名
       *   authorityName?: string
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysauthority[]
       *   // 创建时间
       *   createdAt?: string
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   dataAuthorityId?: System_sysauthority[]
       *   // 默认菜单(默认dashboard)
       *   defaultRouter?: string
       *   deletedAt?: string
       *   // [items] start
       *   // [items] end
       *   menus?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // [items] start
       *     // [cycle] $
       *     // [items] end
       *     authoritys?: System_sysauthority[]
       *     // [items] start
       *     // [cycle] $.menus.[]
       *     // [items] end
       *     children?: System_sysbasemenu[]
       *     // 对应前端文件路径
       *     component?: string
       *     // 创建时间
       *     createdAt?: string
       *     // 是否在列表隐藏
       *     hidden?: boolean
       *     // [items] start
       *     // [items] end
       *     menuBtn?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       desc?: string
       *       name?: string
       *       sysBaseMenuID?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 附加属性
       *     meta?: {
       *       activeName?: string
       *       // 自动关闭tab
       *       closeTab?: boolean
       *       // 是否是基础路由（开发中）
       *       defaultMenu?: boolean
       *       // 菜单图标
       *       icon?: string
       *       // 是否缓存
       *       keepAlive?: boolean
       *       // 菜单名
       *       title?: string
       *       // 路由切换动画
       *       transitionType?: string
       *     }
       *     // 路由name
       *     name?: string
       *     // [items] start
       *     // [items] end
       *     parameters?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       // 地址栏携带参数的key
       *       key?: string
       *       sysBaseMenuID?: number
       *       // 地址栏携带参数为params还是query
       *       type?: string
       *       // 更新时间
       *       updatedAt?: string
       *       // 地址栏携带参数的值
       *       value?: string
       *     }>
       *     // 父菜单ID
       *     parentId?: number
       *     // 路由path
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 父角色ID
       *   parentId?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_authority_setdataauthority<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysauthority;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Authority.post_authority_setdataauthority',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新角色信息
       *
       * **path:** /authority/updateAuthority
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 角色ID
       *   authorityId?: number
       *   // 角色名
       *   authorityName?: string
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysauthority[]
       *   // 创建时间
       *   createdAt?: string
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   dataAuthorityId?: System_sysauthority[]
       *   // 默认菜单(默认dashboard)
       *   defaultRouter?: string
       *   deletedAt?: string
       *   // [items] start
       *   // [items] end
       *   menus?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // [items] start
       *     // [cycle] $
       *     // [items] end
       *     authoritys?: System_sysauthority[]
       *     // [items] start
       *     // [cycle] $.menus.[]
       *     // [items] end
       *     children?: System_sysbasemenu[]
       *     // 对应前端文件路径
       *     component?: string
       *     // 创建时间
       *     createdAt?: string
       *     // 是否在列表隐藏
       *     hidden?: boolean
       *     // [items] start
       *     // [items] end
       *     menuBtn?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       desc?: string
       *       name?: string
       *       sysBaseMenuID?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 附加属性
       *     meta?: {
       *       activeName?: string
       *       // 自动关闭tab
       *       closeTab?: boolean
       *       // 是否是基础路由（开发中）
       *       defaultMenu?: boolean
       *       // 菜单图标
       *       icon?: string
       *       // 是否缓存
       *       keepAlive?: boolean
       *       // 菜单名
       *       title?: string
       *       // 路由切换动画
       *       transitionType?: string
       *     }
       *     // 路由name
       *     name?: string
       *     // [items] start
       *     // [items] end
       *     parameters?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       // 地址栏携带参数的key
       *       key?: string
       *       sysBaseMenuID?: number
       *       // 地址栏携带参数为params还是query
       *       type?: string
       *       // 更新时间
       *       updatedAt?: string
       *       // 地址栏携带参数的值
       *       value?: string
       *     }>
       *     // 父菜单ID
       *     parentId?: number
       *     // 路由path
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 父角色ID
       *   parentId?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     authority?: {
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.data.authority
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.data.authority
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.data.authority
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.data.authority.menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      put_authority_updateauthority<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysauthorityresponse;
            msg?: string;
          }
        > & {
          data: System_sysauthority;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysauthorityresponse;
          msg?: string;
        },
        'Authority.put_authority_updateauthority',
        Config
      >;
    };
    AuthorityBtn: {
      /**
       * ---
       *
       * [POST] 设置权限按钮
       *
       * **path:** /authorityBtn/canRemoveAuthorityBtn
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_authoritybtn_canremoveauthoritybtn<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'AuthorityBtn.post_authoritybtn_canremoveauthoritybtn',
        Config
      >;
      /**
       * ---
       *
       * [POST] 获取权限按钮
       *
       * **path:** /authorityBtn/getAuthorityBtn
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   authorityId?: number
       *   menuID?: number
       *   // [items] start
       *   // [items] end
       *   selected?: number[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     selected?: number[]
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_authoritybtn_getauthoritybtn<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysauthoritybtnres;
            msg?: string;
          }
        > & {
          data: Request_sysauthoritybtnreq;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysauthoritybtnres;
          msg?: string;
        },
        'AuthorityBtn.post_authoritybtn_getauthoritybtn',
        Config
      >;
      /**
       * ---
       *
       * [POST] 设置权限按钮
       *
       * **path:** /authorityBtn/setAuthorityBtn
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   authorityId?: number
       *   menuID?: number
       *   // [items] start
       *   // [items] end
       *   selected?: number[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_authoritybtn_setauthoritybtn<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_sysauthoritybtnreq;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'AuthorityBtn.post_authoritybtn_setauthoritybtn',
        Config
      >;
    };
    AddFunc: {
      /**
       * ---
       *
       * [POST] 增加方法
       *
       * **path:** /autoCode/addFunc
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Struct简称
       *   abbreviation?: string
       *   // 是否自动创建api
       *   autoCreateApiToSql?: boolean
       *   // 是否自动创建按钮权限
       *   autoCreateBtnAuth?: boolean
       *   // 是否自动创建menu
       *   autoCreateMenuToSql?: boolean
       *   // 是否自动创建资源标识
       *   autoCreateResource?: boolean
       *   // 是否自动迁移表结构
       *   autoMigrate?: boolean
       *   // 业务数据库
       *   businessDB?: string
       *   // Struct中文名称
       *   description?: string
       *   // [items] start
       *   // [items] end
       *   fields?: Array<{
       *     // 是否检查数据源
       *     checkDataSource?: boolean
       *     // 是否可清空
       *     clearable?: boolean
       *     // 数据库字段
       *     columnName?: string
       *     // 数据库字段描述
       *     comment?: string
       *     // 数据源
       *     dataSource?: {
       *       // 关联关系 1 一对一 2 一对多
       *       association?: number
       *       dbName?: string
       *       hasDeletedAt?: boolean
       *       label?: string
       *       table?: string
       *       value?: string
       *     }
       *     // 数据库字段长度
       *     dataTypeLong?: string
       *     // 是否必填
       *     defaultValue?: string
       *     // 是否前端详情
       *     desc?: boolean
       *     // 字典
       *     dictType?: string
       *     // 校验失败文字
       *     errorText?: string
       *     // 是否导入/导出
       *     excel?: boolean
       *     // 中文名
       *     fieldDesc?: string
       *     // 索引类型
       *     fieldIndexType?: string
       *     // FieldJson
       *     fieldJson?: string
       *     // Field名
       *     fieldName?: string
       *     // 是否隐藏查询条件
       *     fieldSearchHide?: boolean
       *     // 搜索条件
       *     fieldSearchType?: string
       *     // Field数据类型
       *     fieldType?: string
       *     // Front           bool        `json:"front"`           // 是否前端可见
       *     form?: boolean
       *     // 是否主键
       *     primaryKey?: boolean
       *     // 是否必填
       *     require?: boolean
       *     // 是否增加排序
       *     sort?: boolean
       *     // 是否前端表格列
       *     table?: boolean
       *   }>
       *   // 是否生成server
       *   generateServer?: boolean
       *   // 是否生成web
       *   generateWeb?: boolean
       *   // 是否使用gva默认Model
       *   gvaModel?: boolean
       *   // go文件名称
       *   humpPackageName?: string
       *   // 是否新增
       *   isAdd?: boolean
       *   // 是否树形结构
       *   isTree?: boolean
       *   // 是否只生成模板
       *   onlyTemplate?: boolean
       *   package?: string
       *   // 文件名称
       *   packageName?: string
       *   primaryField?: {
       *     // 是否检查数据源
       *     checkDataSource?: boolean
       *     // 是否可清空
       *     clearable?: boolean
       *     // 数据库字段
       *     columnName?: string
       *     // 数据库字段描述
       *     comment?: string
       *     // 数据源
       *     dataSource?: {
       *       // 关联关系 1 一对一 2 一对多
       *       association?: number
       *       dbName?: string
       *       hasDeletedAt?: boolean
       *       label?: string
       *       table?: string
       *       value?: string
       *     }
       *     // 数据库字段长度
       *     dataTypeLong?: string
       *     // 是否必填
       *     defaultValue?: string
       *     // 是否前端详情
       *     desc?: boolean
       *     // 字典
       *     dictType?: string
       *     // 校验失败文字
       *     errorText?: string
       *     // 是否导入/导出
       *     excel?: boolean
       *     // 中文名
       *     fieldDesc?: string
       *     // 索引类型
       *     fieldIndexType?: string
       *     // FieldJson
       *     fieldJson?: string
       *     // Field名
       *     fieldName?: string
       *     // 是否隐藏查询条件
       *     fieldSearchHide?: boolean
       *     // 搜索条件
       *     fieldSearchType?: string
       *     // Field数据类型
       *     fieldType?: string
       *     // Front           bool        `json:"front"`           // 是否前端可见
       *     form?: boolean
       *     // 是否主键
       *     primaryKey?: boolean
       *     // 是否必填
       *     require?: boolean
       *     // 是否增加排序
       *     sort?: boolean
       *     // 是否前端表格列
       *     table?: boolean
       *   }
       *   // Struct名称
       *   structName?: string
       *   // 表名
       *   tableName?: string
       *   // 展示的树json字段
       *   treeJson?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      post_autocode_addfunc<
        Config extends Alova2MethodConfig<string> & {
          data: Request_autocode;
        }
      >(
        config: Config
      ): Alova2Method<string, 'AddFunc.post_autocode_addfunc', Config>;
    };
    AutoCodePackage: {
      /**
       * ---
       *
       * [POST] 创建package
       *
       * **path:** /autoCode/createPackage
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   desc?: string
       *   label?: string
       *   packageName?: string
       *   template?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_autocode_createpackage<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          data: Request_sysautocodepackagecreate;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCodePackage.post_autocode_createpackage',
        Config
      >;
      /**
       * ---
       *
       * [POST] 获取package
       *
       * **path:** /autoCode/getPackage
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_autocode_getpackage<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCodePackage.post_autocode_getpackage',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取package
       *
       * **path:** /autoCode/getTemplates
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      get_autocode_gettemplates<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCodePackage.get_autocode_gettemplates',
        Config
      >;
    };
    AutoCodeTemplate: {
      /**
       * ---
       *
       * [POST] 自动代码模板
       *
       * **path:** /autoCode/createTemp
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Struct简称
       *   abbreviation?: string
       *   // 是否自动创建api
       *   autoCreateApiToSql?: boolean
       *   // 是否自动创建按钮权限
       *   autoCreateBtnAuth?: boolean
       *   // 是否自动创建menu
       *   autoCreateMenuToSql?: boolean
       *   // 是否自动创建资源标识
       *   autoCreateResource?: boolean
       *   // 是否自动迁移表结构
       *   autoMigrate?: boolean
       *   // 业务数据库
       *   businessDB?: string
       *   // Struct中文名称
       *   description?: string
       *   // [items] start
       *   // [items] end
       *   fields?: Array<{
       *     // 是否检查数据源
       *     checkDataSource?: boolean
       *     // 是否可清空
       *     clearable?: boolean
       *     // 数据库字段
       *     columnName?: string
       *     // 数据库字段描述
       *     comment?: string
       *     // 数据源
       *     dataSource?: {
       *       // 关联关系 1 一对一 2 一对多
       *       association?: number
       *       dbName?: string
       *       hasDeletedAt?: boolean
       *       label?: string
       *       table?: string
       *       value?: string
       *     }
       *     // 数据库字段长度
       *     dataTypeLong?: string
       *     // 是否必填
       *     defaultValue?: string
       *     // 是否前端详情
       *     desc?: boolean
       *     // 字典
       *     dictType?: string
       *     // 校验失败文字
       *     errorText?: string
       *     // 是否导入/导出
       *     excel?: boolean
       *     // 中文名
       *     fieldDesc?: string
       *     // 索引类型
       *     fieldIndexType?: string
       *     // FieldJson
       *     fieldJson?: string
       *     // Field名
       *     fieldName?: string
       *     // 是否隐藏查询条件
       *     fieldSearchHide?: boolean
       *     // 搜索条件
       *     fieldSearchType?: string
       *     // Field数据类型
       *     fieldType?: string
       *     // Front           bool        `json:"front"`           // 是否前端可见
       *     form?: boolean
       *     // 是否主键
       *     primaryKey?: boolean
       *     // 是否必填
       *     require?: boolean
       *     // 是否增加排序
       *     sort?: boolean
       *     // 是否前端表格列
       *     table?: boolean
       *   }>
       *   // 是否生成server
       *   generateServer?: boolean
       *   // 是否生成web
       *   generateWeb?: boolean
       *   // 是否使用gva默认Model
       *   gvaModel?: boolean
       *   // go文件名称
       *   humpPackageName?: string
       *   // 是否新增
       *   isAdd?: boolean
       *   // 是否树形结构
       *   isTree?: boolean
       *   // 是否只生成模板
       *   onlyTemplate?: boolean
       *   package?: string
       *   // 文件名称
       *   packageName?: string
       *   primaryField?: {
       *     // 是否检查数据源
       *     checkDataSource?: boolean
       *     // 是否可清空
       *     clearable?: boolean
       *     // 数据库字段
       *     columnName?: string
       *     // 数据库字段描述
       *     comment?: string
       *     // 数据源
       *     dataSource?: {
       *       // 关联关系 1 一对一 2 一对多
       *       association?: number
       *       dbName?: string
       *       hasDeletedAt?: boolean
       *       label?: string
       *       table?: string
       *       value?: string
       *     }
       *     // 数据库字段长度
       *     dataTypeLong?: string
       *     // 是否必填
       *     defaultValue?: string
       *     // 是否前端详情
       *     desc?: boolean
       *     // 字典
       *     dictType?: string
       *     // 校验失败文字
       *     errorText?: string
       *     // 是否导入/导出
       *     excel?: boolean
       *     // 中文名
       *     fieldDesc?: string
       *     // 索引类型
       *     fieldIndexType?: string
       *     // FieldJson
       *     fieldJson?: string
       *     // Field名
       *     fieldName?: string
       *     // 是否隐藏查询条件
       *     fieldSearchHide?: boolean
       *     // 搜索条件
       *     fieldSearchType?: string
       *     // Field数据类型
       *     fieldType?: string
       *     // Front           bool        `json:"front"`           // 是否前端可见
       *     form?: boolean
       *     // 是否主键
       *     primaryKey?: boolean
       *     // 是否必填
       *     require?: boolean
       *     // 是否增加排序
       *     sort?: boolean
       *     // 是否前端表格列
       *     table?: boolean
       *   }
       *   // Struct名称
       *   structName?: string
       *   // 表名
       *   tableName?: string
       *   // 展示的树json字段
       *   treeJson?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      post_autocode_createtemp<
        Config extends Alova2MethodConfig<string> & {
          data: Request_autocode;
        }
      >(
        config: Config
      ): Alova2Method<string, 'AutoCodeTemplate.post_autocode_createtemp', Config>;
      /**
       * ---
       *
       * [POST] 预览创建后的代码
       *
       * **path:** /autoCode/preview
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Struct简称
       *   abbreviation?: string
       *   // 是否自动创建api
       *   autoCreateApiToSql?: boolean
       *   // 是否自动创建按钮权限
       *   autoCreateBtnAuth?: boolean
       *   // 是否自动创建menu
       *   autoCreateMenuToSql?: boolean
       *   // 是否自动创建资源标识
       *   autoCreateResource?: boolean
       *   // 是否自动迁移表结构
       *   autoMigrate?: boolean
       *   // 业务数据库
       *   businessDB?: string
       *   // Struct中文名称
       *   description?: string
       *   // [items] start
       *   // [items] end
       *   fields?: Array<{
       *     // 是否检查数据源
       *     checkDataSource?: boolean
       *     // 是否可清空
       *     clearable?: boolean
       *     // 数据库字段
       *     columnName?: string
       *     // 数据库字段描述
       *     comment?: string
       *     // 数据源
       *     dataSource?: {
       *       // 关联关系 1 一对一 2 一对多
       *       association?: number
       *       dbName?: string
       *       hasDeletedAt?: boolean
       *       label?: string
       *       table?: string
       *       value?: string
       *     }
       *     // 数据库字段长度
       *     dataTypeLong?: string
       *     // 是否必填
       *     defaultValue?: string
       *     // 是否前端详情
       *     desc?: boolean
       *     // 字典
       *     dictType?: string
       *     // 校验失败文字
       *     errorText?: string
       *     // 是否导入/导出
       *     excel?: boolean
       *     // 中文名
       *     fieldDesc?: string
       *     // 索引类型
       *     fieldIndexType?: string
       *     // FieldJson
       *     fieldJson?: string
       *     // Field名
       *     fieldName?: string
       *     // 是否隐藏查询条件
       *     fieldSearchHide?: boolean
       *     // 搜索条件
       *     fieldSearchType?: string
       *     // Field数据类型
       *     fieldType?: string
       *     // Front           bool        `json:"front"`           // 是否前端可见
       *     form?: boolean
       *     // 是否主键
       *     primaryKey?: boolean
       *     // 是否必填
       *     require?: boolean
       *     // 是否增加排序
       *     sort?: boolean
       *     // 是否前端表格列
       *     table?: boolean
       *   }>
       *   // 是否生成server
       *   generateServer?: boolean
       *   // 是否生成web
       *   generateWeb?: boolean
       *   // 是否使用gva默认Model
       *   gvaModel?: boolean
       *   // go文件名称
       *   humpPackageName?: string
       *   // 是否新增
       *   isAdd?: boolean
       *   // 是否树形结构
       *   isTree?: boolean
       *   // 是否只生成模板
       *   onlyTemplate?: boolean
       *   package?: string
       *   // 文件名称
       *   packageName?: string
       *   primaryField?: {
       *     // 是否检查数据源
       *     checkDataSource?: boolean
       *     // 是否可清空
       *     clearable?: boolean
       *     // 数据库字段
       *     columnName?: string
       *     // 数据库字段描述
       *     comment?: string
       *     // 数据源
       *     dataSource?: {
       *       // 关联关系 1 一对一 2 一对多
       *       association?: number
       *       dbName?: string
       *       hasDeletedAt?: boolean
       *       label?: string
       *       table?: string
       *       value?: string
       *     }
       *     // 数据库字段长度
       *     dataTypeLong?: string
       *     // 是否必填
       *     defaultValue?: string
       *     // 是否前端详情
       *     desc?: boolean
       *     // 字典
       *     dictType?: string
       *     // 校验失败文字
       *     errorText?: string
       *     // 是否导入/导出
       *     excel?: boolean
       *     // 中文名
       *     fieldDesc?: string
       *     // 索引类型
       *     fieldIndexType?: string
       *     // FieldJson
       *     fieldJson?: string
       *     // Field名
       *     fieldName?: string
       *     // 是否隐藏查询条件
       *     fieldSearchHide?: boolean
       *     // 搜索条件
       *     fieldSearchType?: string
       *     // Field数据类型
       *     fieldType?: string
       *     // Front           bool        `json:"front"`           // 是否前端可见
       *     form?: boolean
       *     // 是否主键
       *     primaryKey?: boolean
       *     // 是否必填
       *     require?: boolean
       *     // 是否增加排序
       *     sort?: boolean
       *     // 是否前端表格列
       *     table?: boolean
       *   }
       *   // Struct名称
       *   structName?: string
       *   // 表名
       *   tableName?: string
       *   // 展示的树json字段
       *   treeJson?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_autocode_preview<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          data: Request_autocode;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCodeTemplate.post_autocode_preview',
        Config
      >;
    };
    AutoCode: {
      /**
       * ---
       *
       * [POST] 删除package
       *
       * **path:** /autoCode/delPackage
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_autocode_delpackage<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          data: Request_getbyid;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCode.post_autocode_delpackage',
        Config
      >;
      /**
       * ---
       *
       * [POST] 删除回滚记录
       *
       * **path:** /autoCode/delSysHistory
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_autocode_delsyshistory<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_getbyid;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'AutoCode.post_autocode_delsyshistory',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取当前表所有字段
       *
       * **path:** /autoCode/getColumn
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      get_autocode_getcolumn<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCode.get_autocode_getcolumn',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取当前所有数据库
       *
       * **path:** /autoCode/getDB
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      get_autocode_getdb<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCode.get_autocode_getdb',
        Config
      >;
      /**
       * ---
       *
       * [POST] 获取meta信息
       *
       * **path:** /autoCode/getMeta
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_autocode_getmeta<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          data: Request_getbyid;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCode.post_autocode_getmeta',
        Config
      >;
      /**
       * ---
       *
       * [POST] 查询回滚记录
       *
       * **path:** /autoCode/getSysHistory
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 关键字
       *   keyword?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_autocode_getsyshistory<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          data: Request_pageinfo;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'AutoCode.post_autocode_getsyshistory',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取当前数据库所有表
       *
       * **path:** /autoCode/getTables
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      get_autocode_gettables<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCode.get_autocode_gettables',
        Config
      >;
      /**
       * ---
       *
       * [POST] 回滚自动生成代码
       *
       * **path:** /autoCode/rollback
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 是否删除接口
       *   deleteApi?: boolean
       *   // 是否删除菜单
       *   deleteMenu?: boolean
       *   // 是否删除表
       *   deleteTable?: boolean
       *   // 主键ID
       *   id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_autocode_rollback<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_sysautohistoryrollback;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'AutoCode.post_autocode_rollback',
        Config
      >;
    };
    AutoCodePlugin: {
      /**
       * ---
       *
       * [POST] 打包插件
       *
       * **path:** /autoCode/initAPI
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_autocode_initapi<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCodePlugin.post_autocode_initapi',
        Config
      >;
      /**
       * ---
       *
       * [POST] 打包插件
       *
       * **path:** /autoCode/initMenu
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_autocode_initmenu<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCodePlugin.post_autocode_initmenu',
        Config
      >;
      /**
       * ---
       *
       * [POST] 安装插件
       *
       * **path:** /autoCode/installPlugin
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // this is a test file
       *   plug: Blob
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: null[]
       *   msg?: string
       * }
       * ```
       */
      post_autocode_installplugin<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: null[];
            msg?: string;
          }
        > & {
          data: {
            /**
             * this is a test file
             */
            plug: Blob;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: null[];
          msg?: string;
        },
        'AutoCodePlugin.post_autocode_installplugin',
        Config
      >;
      /**
       * ---
       *
       * [POST] 打包插件
       *
       * **path:** /autoCode/pubPlug
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 插件名称
       *   plugName: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_autocode_pubplug<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 插件名称
             */
            plugName: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AutoCodePlugin.post_autocode_pubplug',
        Config
      >;
    };
    mcp: {
      /**
       * ---
       *
       * [POST] 自动McpTool
       *
       * **path:** /autoCode/mcp
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   description: string
       *   name: string
       *   // [items] start
       *   // [items] end
       *   params?: Array<{
       *     default?: string
       *     description: string
       *     name: string
       *     required?: boolean
       *     // string, number, boolean, object, array
       *     type: string
       *   }>
       *   // [items] start
       *   // [items] end
       *   response?: Array<{
       *     // text, image
       *     type: string
       *   }>
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      post_autocode_mcp<
        Config extends Alova2MethodConfig<string> & {
          data: Request_automcptool;
        }
      >(
        config: Config
      ): Alova2Method<string, 'mcp.post_autocode_mcp', Config>;
      /**
       * ---
       *
       * [POST] 自动McpTool
       *
       * **path:** /autoCode/mcpList
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   description: string
       *   name: string
       *   // [items] start
       *   // [items] end
       *   params?: Array<{
       *     default?: string
       *     description: string
       *     name: string
       *     required?: boolean
       *     // string, number, boolean, object, array
       *     type: string
       *   }>
       *   // [items] start
       *   // [items] end
       *   response?: Array<{
       *     // text, image
       *     type: string
       *   }>
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      post_autocode_mcplist<
        Config extends Alova2MethodConfig<string> & {
          data: Request_automcptool;
        }
      >(
        config: Config
      ): Alova2Method<string, 'mcp.post_autocode_mcplist', Config>;
      /**
       * ---
       *
       * [POST] 测试McpTool
       *
       * **path:** /autoCode/mcpTest
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = object
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * }
       * ```
       */
      post_autocode_mcptest<
        Config extends Alova2MethodConfig<Response_response> & {
          data: object;
        }
      >(
        config: Config
      ): Alova2Method<Response_response, 'mcp.post_autocode_mcptest', Config>;
    };
    Base: {
      /**
       * ---
       *
       * [POST] 生成验证码
       *
       * **path:** /base/captcha
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     captchaId?: string
       *     captchaLength?: number
       *     openCaptcha?: boolean
       *     picPath?: string
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_base_captcha<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_syscaptcharesponse;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_syscaptcharesponse;
          msg?: string;
        },
        'Base.post_base_captcha',
        Config
      >;
      /**
       * ---
       *
       * [POST] 用户登录
       *
       * **path:** /base/login
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 验证码
       *   captcha?: string
       *   // 验证码ID
       *   captchaId?: string
       *   // 密码
       *   password?: string
       *   // 用户名
       *   username?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     expiresAt?: number
       *     token?: string
       *     user?: {
       *       // 主键ID
       *       ID?: number
       *       // 多用户角色
       *       // [items] start
       *       // [items] end
       *       authorities?: Array<{
       *         // 角色ID
       *         authorityId?: number
       *         // 角色名
       *         authorityName?: string
       *         // [items] start
       *         // [cycle] $.data.user.authorities.[]
       *         // [items] end
       *         children?: System_sysauthority[]
       *         // 创建时间
       *         createdAt?: string
       *         // [items] start
       *         // [cycle] $.data.user.authorities.[]
       *         // [items] end
       *         dataAuthorityId?: System_sysauthority[]
       *         // 默认菜单(默认dashboard)
       *         defaultRouter?: string
       *         deletedAt?: string
       *         // [items] start
       *         // [items] end
       *         menus?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // [items] start
       *           // [cycle] $.data.user.authorities.[]
       *           // [items] end
       *           authoritys?: System_sysauthority[]
       *           // [items] start
       *           // [cycle] $.data.user.authorities.[].menus.[]
       *           // [items] end
       *           children?: System_sysbasemenu[]
       *           // 对应前端文件路径
       *           component?: string
       *           // 创建时间
       *           createdAt?: string
       *           // 是否在列表隐藏
       *           hidden?: boolean
       *           // [items] start
       *           // [items] end
       *           menuBtn?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // 创建时间
       *             createdAt?: string
       *             desc?: string
       *             name?: string
       *             sysBaseMenuID?: number
       *             // 更新时间
       *             updatedAt?: string
       *           }>
       *           // 附加属性
       *           meta?: {
       *             activeName?: string
       *             // 自动关闭tab
       *             closeTab?: boolean
       *             // 是否是基础路由（开发中）
       *             defaultMenu?: boolean
       *             // 菜单图标
       *             icon?: string
       *             // 是否缓存
       *             keepAlive?: boolean
       *             // 菜单名
       *             title?: string
       *             // 路由切换动画
       *             transitionType?: string
       *           }
       *           // 路由name
       *           name?: string
       *           // [items] start
       *           // [items] end
       *           parameters?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // 创建时间
       *             createdAt?: string
       *             // 地址栏携带参数的key
       *             key?: string
       *             sysBaseMenuID?: number
       *             // 地址栏携带参数为params还是query
       *             type?: string
       *             // 更新时间
       *             updatedAt?: string
       *             // 地址栏携带参数的值
       *             value?: string
       *           }>
       *           // 父菜单ID
       *           parentId?: number
       *           // 路由path
       *           path?: string
       *           // 排序标记
       *           sort?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 父角色ID
       *         parentId?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 用户角色
       *       authority?: {
       *         // 角色ID
       *         authorityId?: number
       *         // 角色名
       *         authorityName?: string
       *         // [items] start
       *         // [cycle] $.data.user.authority
       *         // [items] end
       *         children?: System_sysauthority[]
       *         // 创建时间
       *         createdAt?: string
       *         // [items] start
       *         // [cycle] $.data.user.authority
       *         // [items] end
       *         dataAuthorityId?: System_sysauthority[]
       *         // 默认菜单(默认dashboard)
       *         defaultRouter?: string
       *         deletedAt?: string
       *         // [items] start
       *         // [items] end
       *         menus?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // [items] start
       *           // [cycle] $.data.user.authority
       *           // [items] end
       *           authoritys?: System_sysauthority[]
       *           // [items] start
       *           // [cycle] $.data.user.authority.menus.[]
       *           // [items] end
       *           children?: System_sysbasemenu[]
       *           // 对应前端文件路径
       *           component?: string
       *           // 创建时间
       *           createdAt?: string
       *           // 是否在列表隐藏
       *           hidden?: boolean
       *           // [items] start
       *           // [items] end
       *           menuBtn?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // 创建时间
       *             createdAt?: string
       *             desc?: string
       *             name?: string
       *             sysBaseMenuID?: number
       *             // 更新时间
       *             updatedAt?: string
       *           }>
       *           // 附加属性
       *           meta?: {
       *             activeName?: string
       *             // 自动关闭tab
       *             closeTab?: boolean
       *             // 是否是基础路由（开发中）
       *             defaultMenu?: boolean
       *             // 菜单图标
       *             icon?: string
       *             // 是否缓存
       *             keepAlive?: boolean
       *             // 菜单名
       *             title?: string
       *             // 路由切换动画
       *             transitionType?: string
       *           }
       *           // 路由name
       *           name?: string
       *           // [items] start
       *           // [items] end
       *           parameters?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // 创建时间
       *             createdAt?: string
       *             // 地址栏携带参数的key
       *             key?: string
       *             sysBaseMenuID?: number
       *             // 地址栏携带参数为params还是query
       *             type?: string
       *             // 更新时间
       *             updatedAt?: string
       *             // 地址栏携带参数的值
       *             value?: string
       *           }>
       *           // 父菜单ID
       *           parentId?: number
       *           // 路由path
       *           path?: string
       *           // 排序标记
       *           sort?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 父角色ID
       *         parentId?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }
       *       // 用户角色ID
       *       authorityId?: number
       *       // 创建时间
       *       createdAt?: string
       *       // 用户邮箱
       *       email?: string
       *       // 用户是否被冻结 1正常 2冻结
       *       enable?: number
       *       // 用户头像
       *       headerImg?: string
       *       // 用户昵称
       *       nickName?: string
       *       // 配置
       *       originSetting?: Record<string, any>
       *       // 用户手机号
       *       phone?: string
       *       // 更新时间
       *       updatedAt?: string
       *       // 用户登录名
       *       userName?: string
       *       // 用户UUID
       *       uuid?: string
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_base_login<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_loginresponse;
            msg?: string;
          }
        > & {
          data: Request_login;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_loginresponse;
          msg?: string;
        },
        'Base.post_base_login',
        Config
      >;
    };
    Casbin: {
      /**
       * ---
       *
       * [POST] 更新角色api权限
       *
       * **path:** /casbin/UpdateCasbin
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 权限id
       *   authorityId?: number
       *   // [items] start
       *   // [items] end
       *   casbinInfos?: Array<{
       *     // 方法
       *     method?: string
       *     // 路径
       *     path?: string
       *   }>
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_casbin_updatecasbin<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_casbininreceive;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Casbin.post_casbin_updatecasbin',
        Config
      >;
      /**
       * ---
       *
       * [POST] 获取权限列表
       *
       * **path:** /casbin/getPolicyPathByAuthorityId
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 权限id
       *   authorityId?: number
       *   // [items] start
       *   // [items] end
       *   casbinInfos?: Array<{
       *     // 方法
       *     method?: string
       *     // 路径
       *     path?: string
       *   }>
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     paths?: Array<{
       *       // 方法
       *       method?: string
       *       // 路径
       *       path?: string
       *     }>
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_casbin_getpolicypathbyauthorityid<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_policypathresponse;
            msg?: string;
          }
        > & {
          data: Request_casbininreceive;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_policypathresponse;
          msg?: string;
        },
        'Casbin.post_casbin_getpolicypathbyauthorityid',
        Config
      >;
    };
    Consumption: {
      /**
       * ---
       *
       * [POST] 创建消费明细
       *
       * **path:** /consumption/createConsumption
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 交易金额
       *   amount?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 备注
       *   remark?: string
       *   // 交易类型
       *   transaction_type: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 用户ID
       *   userId?: number
       *   wallet_balance?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_consumption_createconsumption<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_consumption;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Consumption.post_consumption_createconsumption',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除消费明细
       *
       * **path:** /consumption/deleteConsumption
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 交易金额
       *   amount?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 备注
       *   remark?: string
       *   // 交易类型
       *   transaction_type: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 用户ID
       *   userId?: number
       *   wallet_balance?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_consumption_deleteconsumption<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_consumption;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Consumption.delete_consumption_deleteconsumption',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除消费明细
       *
       * **path:** /consumption/deleteConsumptionByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_consumption_deleteconsumptionbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Consumption.delete_consumption_deleteconsumptionbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询消费明细
       *
       * **path:** /consumption/findConsumption
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 用id查询消费明细
       *   ID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 交易金额
       *     amount?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 备注
       *     remark?: string
       *     // 交易类型
       *     transaction_type: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *     // 用户ID
       *     userId?: number
       *     wallet_balance?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_consumption_findconsumption<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Miniprogram_consumption;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 用id查询消费明细
             */
            ID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Miniprogram_consumption;
          msg?: string;
        },
        'Consumption.get_consumption_findconsumption',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取消费明细列表
       *
       * **path:** /consumption/getConsumptionList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   endAmount?: number
       *   // 关键字
       *   keyword?: string
       *   order?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   remark?: string
       *   sort?: string
       *   startAmount?: number
       *   transaction_type?: string
       *   userId?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_consumption_getconsumptionlist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            endAmount?: number;
            /**
             * 关键字
             */
            keyword?: string;
            order?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            remark?: string;
            sort?: string;
            startAmount?: number;
            transaction_type?: string;
            userId?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'Consumption.get_consumption_getconsumptionlist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的消费明细接口
       *
       * **path:** /consumption/getConsumptionPublic
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_consumption_getconsumptionpublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'Consumption.get_consumption_getconsumptionpublic',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新消费明细
       *
       * **path:** /consumption/updateConsumption
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 交易金额
       *   amount?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 备注
       *   remark?: string
       *   // 交易类型
       *   transaction_type: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 用户ID
       *   userId?: number
       *   wallet_balance?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_consumption_updateconsumption<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_consumption;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Consumption.put_consumption_updateconsumption',
        Config
      >;
    };
    ExaCustomer: {
      /**
       * ---
       *
       * [GET] 获取单一客户信息
       *
       * **path:** /customer/customer
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 客户名
       *   customerName?: string
       *   // 客户手机号
       *   customerPhoneData?: string
       *   // 管理角色ID
       *   sysUserAuthorityID?: number
       *   // 管理ID
       *   sysUserId?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     customer?: {
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       // 客户名
       *       customerName?: string
       *       // 客户手机号
       *       customerPhoneData?: string
       *       // 管理详情
       *       sysUser?: {
       *         // 主键ID
       *         ID?: number
       *         // 多用户角色
       *         // [items] start
       *         // [items] end
       *         authorities?: Array<{
       *           // 角色ID
       *           authorityId?: number
       *           // 角色名
       *           authorityName?: string
       *           // [items] start
       *           // [cycle] $.data.customer.sysUser.authorities.[]
       *           // [items] end
       *           children?: System_sysauthority[]
       *           // 创建时间
       *           createdAt?: string
       *           // [items] start
       *           // [cycle] $.data.customer.sysUser.authorities.[]
       *           // [items] end
       *           dataAuthorityId?: System_sysauthority[]
       *           // 默认菜单(默认dashboard)
       *           defaultRouter?: string
       *           deletedAt?: string
       *           // [items] start
       *           // [items] end
       *           menus?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // [items] start
       *             // [cycle] $.data.customer.sysUser.authorities.[]
       *             // [items] end
       *             authoritys?: System_sysauthority[]
       *             // [items] start
       *             // [cycle] $.data.customer.sysUser.authorities.[].menus.[]
       *             // [items] end
       *             children?: System_sysbasemenu[]
       *             // 对应前端文件路径
       *             component?: string
       *             // 创建时间
       *             createdAt?: string
       *             // 是否在列表隐藏
       *             hidden?: boolean
       *             // [items] start
       *             // [items] end
       *             menuBtn?: Array<{
       *               // 主键ID
       *               ID?: number
       *               // 创建时间
       *               createdAt?: string
       *               desc?: string
       *               name?: string
       *               sysBaseMenuID?: number
       *               // 更新时间
       *               updatedAt?: string
       *             }>
       *             // 附加属性
       *             meta?: {
       *               activeName?: string
       *               // 自动关闭tab
       *               closeTab?: boolean
       *               // 是否是基础路由（开发中）
       *               defaultMenu?: boolean
       *               // 菜单图标
       *               icon?: string
       *               // 是否缓存
       *               keepAlive?: boolean
       *               // 菜单名
       *               title?: string
       *               // 路由切换动画
       *               transitionType?: string
       *             }
       *             // 路由name
       *             name?: string
       *             // [items] start
       *             // [items] end
       *             parameters?: Array<{
       *               // 主键ID
       *               ID?: number
       *               // 创建时间
       *               createdAt?: string
       *               // 地址栏携带参数的key
       *               key?: string
       *               sysBaseMenuID?: number
       *               // 地址栏携带参数为params还是query
       *               type?: string
       *               // 更新时间
       *               updatedAt?: string
       *               // 地址栏携带参数的值
       *               value?: string
       *             }>
       *             // 父菜单ID
       *             parentId?: number
       *             // 路由path
       *             path?: string
       *             // 排序标记
       *             sort?: number
       *             // 更新时间
       *             updatedAt?: string
       *           }>
       *           // 父角色ID
       *           parentId?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 用户角色
       *         authority?: {
       *           // 角色ID
       *           authorityId?: number
       *           // 角色名
       *           authorityName?: string
       *           // [items] start
       *           // [cycle] $.data.customer.sysUser.authority
       *           // [items] end
       *           children?: System_sysauthority[]
       *           // 创建时间
       *           createdAt?: string
       *           // [items] start
       *           // [cycle] $.data.customer.sysUser.authority
       *           // [items] end
       *           dataAuthorityId?: System_sysauthority[]
       *           // 默认菜单(默认dashboard)
       *           defaultRouter?: string
       *           deletedAt?: string
       *           // [items] start
       *           // [items] end
       *           menus?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // [items] start
       *             // [cycle] $.data.customer.sysUser.authority
       *             // [items] end
       *             authoritys?: System_sysauthority[]
       *             // [items] start
       *             // [cycle] $.data.customer.sysUser.authority.menus.[]
       *             // [items] end
       *             children?: System_sysbasemenu[]
       *             // 对应前端文件路径
       *             component?: string
       *             // 创建时间
       *             createdAt?: string
       *             // 是否在列表隐藏
       *             hidden?: boolean
       *             // [items] start
       *             // [items] end
       *             menuBtn?: Array<{
       *               // 主键ID
       *               ID?: number
       *               // 创建时间
       *               createdAt?: string
       *               desc?: string
       *               name?: string
       *               sysBaseMenuID?: number
       *               // 更新时间
       *               updatedAt?: string
       *             }>
       *             // 附加属性
       *             meta?: {
       *               activeName?: string
       *               // 自动关闭tab
       *               closeTab?: boolean
       *               // 是否是基础路由（开发中）
       *               defaultMenu?: boolean
       *               // 菜单图标
       *               icon?: string
       *               // 是否缓存
       *               keepAlive?: boolean
       *               // 菜单名
       *               title?: string
       *               // 路由切换动画
       *               transitionType?: string
       *             }
       *             // 路由name
       *             name?: string
       *             // [items] start
       *             // [items] end
       *             parameters?: Array<{
       *               // 主键ID
       *               ID?: number
       *               // 创建时间
       *               createdAt?: string
       *               // 地址栏携带参数的key
       *               key?: string
       *               sysBaseMenuID?: number
       *               // 地址栏携带参数为params还是query
       *               type?: string
       *               // 更新时间
       *               updatedAt?: string
       *               // 地址栏携带参数的值
       *               value?: string
       *             }>
       *             // 父菜单ID
       *             parentId?: number
       *             // 路由path
       *             path?: string
       *             // 排序标记
       *             sort?: number
       *             // 更新时间
       *             updatedAt?: string
       *           }>
       *           // 父角色ID
       *           parentId?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }
       *         // 用户角色ID
       *         authorityId?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 用户邮箱
       *         email?: string
       *         // 用户是否被冻结 1正常 2冻结
       *         enable?: number
       *         // 用户头像
       *         headerImg?: string
       *         // 用户昵称
       *         nickName?: string
       *         // 配置
       *         originSetting?: Record<string, any>
       *         // 用户手机号
       *         phone?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 用户登录名
       *         userName?: string
       *         // 用户UUID
       *         uuid?: string
       *       }
       *       // 管理角色ID
       *       sysUserAuthorityID?: number
       *       // 管理ID
       *       sysUserId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_customer_customer<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_exacustomerresponse;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 主键ID
             */
            ID?: number;
            /**
             * 创建时间
             */
            createdAt?: string;
            /**
             * 客户名
             */
            customerName?: string;
            /**
             * 客户手机号
             */
            customerPhoneData?: string;
            /**
             * 管理角色ID
             */
            sysUserAuthorityID?: number;
            /**
             * 管理ID
             */
            sysUserId?: number;
            /**
             * 更新时间
             */
            updatedAt?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_exacustomerresponse;
          msg?: string;
        },
        'ExaCustomer.get_customer_customer',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新客户信息
       *
       * **path:** /customer/customer
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 客户名
       *   customerName?: string
       *   // 客户手机号
       *   customerPhoneData?: string
       *   // 管理详情
       *   sysUser?: {
       *     // 主键ID
       *     ID?: number
       *     // 多用户角色
       *     // [items] start
       *     // [items] end
       *     authorities?: Array<{
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authorities.[]
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authorities.[]
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.sysUser.authorities.[]
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.sysUser.authorities.[].menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 用户角色
       *     authority?: {
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authority
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authority
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.sysUser.authority
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.sysUser.authority.menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *     // 用户角色ID
       *     authorityId?: number
       *     // 创建时间
       *     createdAt?: string
       *     // 用户邮箱
       *     email?: string
       *     // 用户是否被冻结 1正常 2冻结
       *     enable?: number
       *     // 用户头像
       *     headerImg?: string
       *     // 用户昵称
       *     nickName?: string
       *     // 配置
       *     originSetting?: Record<string, any>
       *     // 用户手机号
       *     phone?: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 用户登录名
       *     userName?: string
       *     // 用户UUID
       *     uuid?: string
       *   }
       *   // 管理角色ID
       *   sysUserAuthorityID?: number
       *   // 管理ID
       *   sysUserId?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_customer_customer<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Example_exacustomer;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'ExaCustomer.put_customer_customer',
        Config
      >;
      /**
       * ---
       *
       * [POST] 创建客户
       *
       * **path:** /customer/customer
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 客户名
       *   customerName?: string
       *   // 客户手机号
       *   customerPhoneData?: string
       *   // 管理详情
       *   sysUser?: {
       *     // 主键ID
       *     ID?: number
       *     // 多用户角色
       *     // [items] start
       *     // [items] end
       *     authorities?: Array<{
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authorities.[]
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authorities.[]
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.sysUser.authorities.[]
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.sysUser.authorities.[].menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 用户角色
       *     authority?: {
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authority
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authority
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.sysUser.authority
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.sysUser.authority.menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *     // 用户角色ID
       *     authorityId?: number
       *     // 创建时间
       *     createdAt?: string
       *     // 用户邮箱
       *     email?: string
       *     // 用户是否被冻结 1正常 2冻结
       *     enable?: number
       *     // 用户头像
       *     headerImg?: string
       *     // 用户昵称
       *     nickName?: string
       *     // 配置
       *     originSetting?: Record<string, any>
       *     // 用户手机号
       *     phone?: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 用户登录名
       *     userName?: string
       *     // 用户UUID
       *     uuid?: string
       *   }
       *   // 管理角色ID
       *   sysUserAuthorityID?: number
       *   // 管理ID
       *   sysUserId?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_customer_customer<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Example_exacustomer;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'ExaCustomer.post_customer_customer',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除客户
       *
       * **path:** /customer/customer
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 客户名
       *   customerName?: string
       *   // 客户手机号
       *   customerPhoneData?: string
       *   // 管理详情
       *   sysUser?: {
       *     // 主键ID
       *     ID?: number
       *     // 多用户角色
       *     // [items] start
       *     // [items] end
       *     authorities?: Array<{
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authorities.[]
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authorities.[]
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.sysUser.authorities.[]
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.sysUser.authorities.[].menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 用户角色
       *     authority?: {
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authority
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.sysUser.authority
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.sysUser.authority
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.sysUser.authority.menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *     // 用户角色ID
       *     authorityId?: number
       *     // 创建时间
       *     createdAt?: string
       *     // 用户邮箱
       *     email?: string
       *     // 用户是否被冻结 1正常 2冻结
       *     enable?: number
       *     // 用户头像
       *     headerImg?: string
       *     // 用户昵称
       *     nickName?: string
       *     // 配置
       *     originSetting?: Record<string, any>
       *     // 用户手机号
       *     phone?: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 用户登录名
       *     userName?: string
       *     // 用户UUID
       *     uuid?: string
       *   }
       *   // 管理角色ID
       *   sysUserAuthorityID?: number
       *   // 管理ID
       *   sysUserId?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_customer_customer<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Example_exacustomer;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'ExaCustomer.delete_customer_customer',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取权限客户列表
       *
       * **path:** /customer/customerList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 关键字
       *   keyword?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_customer_customerlist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 关键字
             */
            keyword?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'ExaCustomer.get_customer_customerlist',
        Config
      >;
    };
    Device: {
      /**
       * ---
       *
       * [POST] 创建设备管理
       *
       * **path:** /device/createDevice
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 设备代码
       *   device_code?: string
       *   // 设备数据
       *   device_data?: object
       *   // 设备名称
       *   device_name?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 用户id
       *   user_id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_device_createdevice<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_device;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Device.post_device_createdevice',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除设备管理
       *
       * **path:** /device/deleteDevice
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 设备代码
       *   device_code?: string
       *   // 设备数据
       *   device_data?: object
       *   // 设备名称
       *   device_name?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 用户id
       *   user_id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_device_deletedevice<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_device;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Device.delete_device_deletedevice',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除设备管理
       *
       * **path:** /device/deleteDeviceByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_device_deletedevicebyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Device.delete_device_deletedevicebyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取设备管理列表
       *
       * **path:** /device/getDeviceList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   device_code?: string
       *   device_name?: string
       *   // 关键字
       *   keyword?: string
       *   order?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   sort?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_device_getdevicelist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            device_code?: string;
            device_name?: string;
            /**
             * 关键字
             */
            keyword?: string;
            order?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            sort?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'Device.get_device_getdevicelist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的设备管理接口
       *
       * **path:** /device/getDevicePublic
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_device_getdevicepublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'Device.get_device_getdevicepublic',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新设备管理
       *
       * **path:** /device/updateDevice
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 设备代码
       *   device_code?: string
       *   // 设备数据
       *   device_data?: object
       *   // 设备名称
       *   device_name?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 用户id
       *   user_id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_device_updatedevice<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_device;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Device.put_device_updatedevice',
        Config
      >;
    };
    System: {
      /**
       * ---
       *
       * [POST] 发送测试邮件
       *
       * **path:** /email/emailTest
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      post_email_emailtest<Config extends Alova2MethodConfig<string>>(
        config?: Config
      ): Alova2Method<string, 'System.post_email_emailtest', Config>;
      /**
       * ---
       *
       * [POST] 发送邮件
       *
       * **path:** /email/sendEmail
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 邮件内容
       *   body?: string
       *   // 邮件标题
       *   subject?: string
       *   // 邮件发送给谁
       *   to?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      post_email_sendemail<
        Config extends Alova2MethodConfig<string> & {
          data: Response_email;
        }
      >(
        config: Config
      ): Alova2Method<string, 'System.post_email_sendemail', Config>;
      /**
       * ---
       *
       * [POST] 获取服务器信息
       *
       * **path:** /system/getServerInfo
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_system_getserverinfo<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'System.post_system_getserverinfo',
        Config
      >;
      /**
       * ---
       *
       * [POST] 获取配置文件内容
       *
       * **path:** /system/getSystemConfig
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     config?: {
       *       'aliyun-oss'?: {
       *         'access-key-id'?: string
       *         'access-key-secret'?: string
       *         'base-path'?: string
       *         'bucket-name'?: string
       *         'bucket-url'?: string
       *         endpoint?: string
       *       }
       *       // auto
       *       autocode?: {
       *         'ai-path'?: string
       *         module?: string
       *         root?: string
       *         server?: string
       *         web?: string
       *       }
       *       'aws-s3'?: {
       *         'base-url'?: string
       *         bucket?: string
       *         'disable-ssl'?: boolean
       *         endpoint?: string
       *         'path-prefix'?: string
       *         region?: string
       *         's3-force-path-style'?: boolean
       *         'secret-id'?: string
       *         'secret-key'?: string
       *       }
       *       captcha?: {
       *         // 验证码高度
       *         'img-height'?: number
       *         // 验证码宽度
       *         'img-width'?: number
       *         // 验证码长度
       *         'key-long'?: number
       *         // 防爆破验证码开启此数，0代表每次登录都需要验证码，其他数字代表错误密码次数，如3代表错误三次后出现验证码
       *         'open-captcha'?: number
       *         // 防爆破验证码超时时间，单位：s(秒)
       *         'open-captcha-timeout'?: number
       *       }
       *       'cloudflare-r2'?: {
       *         'access-key-id'?: string
       *         'account-id'?: string
       *         'base-url'?: string
       *         bucket?: string
       *         path?: string
       *         'secret-access-key'?: string
       *       }
       *       // 跨域配置
       *       cors?: {
       *         mode?: string
       *         // [items] start
       *         // [items] end
       *         whitelist?: Array<{
       *           'allow-credentials'?: boolean
       *           'allow-headers'?: string
       *           'allow-methods'?: string
       *           'allow-origin'?: string
       *           'expose-headers'?: string
       *         }>
       *       }
       *       // [items] start
       *       // [items] end
       *       'db-list'?: Array<{
       *         'alias-name'?: string
       *         // 高级配置
       *         config?: string
       *         // 数据库名
       *         'db-name'?: string
       *         disable?: boolean
       *         // 数据库引擎，默认InnoDB
       *         engine?: string
       *         // 是否开启Gorm全局日志
       *         'log-mode'?: string
       *         // 是否通过zap写入日志文件
       *         'log-zap'?: boolean
       *         // 空闲中的最大连接数
       *         'max-idle-conns'?: number
       *         // 打开到数据库的最大连接数
       *         'max-open-conns'?: number
       *         // 数据库密码
       *         password?: string
       *         // 数据库地址
       *         path?: string
       *         // 数据库端口
       *         port?: string
       *         // 数据库前缀
       *         prefix?: string
       *         // 是否开启全局禁用复数，true表示开启
       *         singular?: boolean
       *         type?: string
       *         // 数据库账号
       *         username?: string
       *       }>
       *       // [items] start
       *       // [items] end
       *       'disk-list'?: Array<{
       *         'mount-point'?: string
       *       }>
       *       email?: {
       *         // 发件人  你自己要发邮件的邮箱
       *         from?: string
       *         // 服务器地址 例如 smtp.qq.com  请前往QQ或者你要发邮件的邮箱查看其smtp协议
       *         host?: string
       *         // 是否LoginAuth   是否使用LoginAuth认证方式（适用于IBM、微软邮箱服务器等）
       *         'is-loginauth'?: boolean
       *         // 是否SSL   是否开启SSL
       *         'is-ssl'?: boolean
       *         // 昵称    发件人昵称 通常为自己的邮箱
       *         nickname?: string
       *         // 端口     请前往QQ或者你要发邮件的邮箱查看其smtp协议 大多为 465
       *         port?: number
       *         // 密钥    用于登录的密钥 最好不要用邮箱密码 去邮箱smtp申请一个用于登录的密钥
       *         secret?: string
       *         // 收件人:多个以英文逗号分隔 例：a@qq.com b@qq.com 正式开发中请把此项目作为参数使用
       *         to?: string
       *       }
       *       excel?: {
       *         dir?: string
       *       }
       *       'hua-wei-obs'?: {
       *         'access-key'?: string
       *         bucket?: string
       *         endpoint?: string
       *         path?: string
       *         'secret-key'?: string
       *       }
       *       jwt?: {
       *         // 缓冲时间
       *         'buffer-time'?: string
       *         // 过期时间
       *         'expires-time'?: string
       *         // 签发者
       *         issuer?: string
       *         // jwt签名
       *         'signing-key'?: string
       *       }
       *       // oss
       *       local?: {
       *         // 本地文件访问路径
       *         path?: string
       *         // 本地文件存储路径
       *         'store-path'?: string
       *       }
       *       // MCP配置
       *       mcp?: {
       *         // 独立MCP服务端口
       *         addr?: number
       *         // 消息路径
       *         message_path?: string
       *         // MCP名称
       *         name?: string
       *         // 是否独立运行MCP服务
       *         separate?: boolean
       *         // SSE路径
       *         sse_path?: string
       *         // URL前缀
       *         url_prefix?: string
       *         // MCP版本
       *         version?: string
       *       }
       *       minio?: {
       *         'access-key-id'?: string
       *         'access-key-secret'?: string
       *         'base-path'?: string
       *         'bucket-name'?: string
       *         'bucket-url'?: string
       *         endpoint?: string
       *         'use-ssl'?: boolean
       *       }
       *       mongo?: {
       *         // 验证数据库
       *         'auth-source'?: string
       *         // collection name
       *         coll?: string
       *         // 连接超时时间
       *         'connect-timeout-ms'?: number
       *         // database name
       *         database?: string
       *         // 主机列表
       *         // [items] start
       *         // [items] end
       *         hosts?: Array<{
       *           // ip地址
       *           host?: string
       *           // 端口
       *           port?: string
       *         }>
       *         // 是否开启zap日志
       *         'is-zap'?: boolean
       *         // 最大连接池
       *         'max-pool-size'?: number
       *         // 最小连接池
       *         'min-pool-size'?: number
       *         // mongodb options
       *         options?: string
       *         // 密码
       *         password?: string
       *         // socket超时时间
       *         'socket-timeout-ms'?: number
       *         // 用户名
       *         username?: string
       *       }
       *       mssql?: {
       *         // 高级配置
       *         config?: string
       *         // 数据库名
       *         'db-name'?: string
       *         // 数据库引擎，默认InnoDB
       *         engine?: string
       *         // 是否开启Gorm全局日志
       *         'log-mode'?: string
       *         // 是否通过zap写入日志文件
       *         'log-zap'?: boolean
       *         // 空闲中的最大连接数
       *         'max-idle-conns'?: number
       *         // 打开到数据库的最大连接数
       *         'max-open-conns'?: number
       *         // 数据库密码
       *         password?: string
       *         // 数据库地址
       *         path?: string
       *         // 数据库端口
       *         port?: string
       *         // 数据库前缀
       *         prefix?: string
       *         // 是否开启全局禁用复数，true表示开启
       *         singular?: boolean
       *         // 数据库账号
       *         username?: string
       *       }
       *       // gorm
       *       mysql?: {
       *         // 高级配置
       *         config?: string
       *         // 数据库名
       *         'db-name'?: string
       *         // 数据库引擎，默认InnoDB
       *         engine?: string
       *         // 是否开启Gorm全局日志
       *         'log-mode'?: string
       *         // 是否通过zap写入日志文件
       *         'log-zap'?: boolean
       *         // 空闲中的最大连接数
       *         'max-idle-conns'?: number
       *         // 打开到数据库的最大连接数
       *         'max-open-conns'?: number
       *         // 数据库密码
       *         password?: string
       *         // 数据库地址
       *         path?: string
       *         // 数据库端口
       *         port?: string
       *         // 数据库前缀
       *         prefix?: string
       *         // 是否开启全局禁用复数，true表示开启
       *         singular?: boolean
       *         // 数据库账号
       *         username?: string
       *       }
       *       oracle?: {
       *         // 高级配置
       *         config?: string
       *         // 数据库名
       *         'db-name'?: string
       *         // 数据库引擎，默认InnoDB
       *         engine?: string
       *         // 是否开启Gorm全局日志
       *         'log-mode'?: string
       *         // 是否通过zap写入日志文件
       *         'log-zap'?: boolean
       *         // 空闲中的最大连接数
       *         'max-idle-conns'?: number
       *         // 打开到数据库的最大连接数
       *         'max-open-conns'?: number
       *         // 数据库密码
       *         password?: string
       *         // 数据库地址
       *         path?: string
       *         // 数据库端口
       *         port?: string
       *         // 数据库前缀
       *         prefix?: string
       *         // 是否开启全局禁用复数，true表示开启
       *         singular?: boolean
       *         // 数据库账号
       *         username?: string
       *       }
       *       pgsql?: {
       *         // 高级配置
       *         config?: string
       *         // 数据库名
       *         'db-name'?: string
       *         // 数据库引擎，默认InnoDB
       *         engine?: string
       *         // 是否开启Gorm全局日志
       *         'log-mode'?: string
       *         // 是否通过zap写入日志文件
       *         'log-zap'?: boolean
       *         // 空闲中的最大连接数
       *         'max-idle-conns'?: number
       *         // 打开到数据库的最大连接数
       *         'max-open-conns'?: number
       *         // 数据库密码
       *         password?: string
       *         // 数据库地址
       *         path?: string
       *         // 数据库端口
       *         port?: string
       *         // 数据库前缀
       *         prefix?: string
       *         // 是否开启全局禁用复数，true表示开启
       *         singular?: boolean
       *         // 数据库账号
       *         username?: string
       *       }
       *       qiniu?: {
       *         // 秘钥AK
       *         'access-key'?: string
       *         // 空间名称
       *         bucket?: string
       *         // CDN加速域名
       *         'img-path'?: string
       *         // 秘钥SK
       *         'secret-key'?: string
       *         // 上传是否使用CDN上传加速
       *         'use-cdn-domains'?: boolean
       *         // 是否使用https
       *         'use-https'?: boolean
       *         // 存储区域
       *         zone?: string
       *       }
       *       redis?: {
       *         // 服务器地址:端口
       *         addr?: string
       *         // 集群模式下的节点地址列表
       *         // [items] start
       *         // [items] end
       *         clusterAddrs?: string[]
       *         // 单实例模式下redis的哪个数据库
       *         db?: number
       *         // 代表当前实例的名字
       *         name?: string
       *         // 密码
       *         password?: string
       *         // 是否使用集群模式
       *         useCluster?: boolean
       *       }
       *       // [items] start
       *       // [items] end
       *       'redis-list'?: Array<{
       *         // 服务器地址:端口
       *         addr?: string
       *         // 集群模式下的节点地址列表
       *         // [items] start
       *         // [items] end
       *         clusterAddrs?: string[]
       *         // 单实例模式下redis的哪个数据库
       *         db?: number
       *         // 代表当前实例的名字
       *         name?: string
       *         // 密码
       *         password?: string
       *         // 是否使用集群模式
       *         useCluster?: boolean
       *       }>
       *       sqlite?: {
       *         // 高级配置
       *         config?: string
       *         // 数据库名
       *         'db-name'?: string
       *         // 数据库引擎，默认InnoDB
       *         engine?: string
       *         // 是否开启Gorm全局日志
       *         'log-mode'?: string
       *         // 是否通过zap写入日志文件
       *         'log-zap'?: boolean
       *         // 空闲中的最大连接数
       *         'max-idle-conns'?: number
       *         // 打开到数据库的最大连接数
       *         'max-open-conns'?: number
       *         // 数据库密码
       *         password?: string
       *         // 数据库地址
       *         path?: string
       *         // 数据库端口
       *         port?: string
       *         // 数据库前缀
       *         prefix?: string
       *         // 是否开启全局禁用复数，true表示开启
       *         singular?: boolean
       *         // 数据库账号
       *         username?: string
       *       }
       *       system?: {
       *         // 端口值
       *         addr?: number
       *         // 数据库类型:mysql(默认)|sqlite|sqlserver|postgresql
       *         'db-type'?: string
       *         // 自动迁移数据库表结构，生产环境建议设为false，手动迁移
       *         'disable-auto-migrate'?: boolean
       *         'iplimit-count'?: number
       *         'iplimit-time'?: number
       *         // Oss类型
       *         'oss-type'?: string
       *         'router-prefix'?: string
       *         // 使用mongo
       *         'use-mongo'?: boolean
       *         // 多点登录拦截
       *         'use-multipoint'?: boolean
       *         // 使用redis
       *         'use-redis'?: boolean
       *         // 使用树形角色分配模式
       *         'use-strict-auth'?: boolean
       *       }
       *       'tencent-cos'?: {
       *         'base-url'?: string
       *         bucket?: string
       *         'path-prefix'?: string
       *         region?: string
       *         'secret-id'?: string
       *         'secret-key'?: string
       *       }
       *       zap?: {
       *         // 日志文件夹
       *         director?: string
       *         // 编码级
       *         'encode-level'?: string
       *         // 输出
       *         format?: string
       *         // 级别
       *         level?: string
       *         // 输出控制台
       *         'log-in-console'?: boolean
       *         // 日志前缀
       *         prefix?: string
       *         // 日志保留天数
       *         'retention-day'?: number
       *         // 显示行
       *         'show-line'?: boolean
       *         // 栈名
       *         'stacktrace-key'?: string
       *       }
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_system_getsystemconfig<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysconfigresponse;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysconfigresponse;
          msg?: string;
        },
        'System.post_system_getsystemconfig',
        Config
      >;
      /**
       * ---
       *
       * [POST] 重载系统
       *
       * **path:** /system/reloadSystem
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_system_reloadsystem<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'System.post_system_reloadsystem',
        Config
      >;
      /**
       * ---
       *
       * [POST] 设置配置文件内容
       *
       * **path:** /system/setSystemConfig
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   config?: {
       *     'aliyun-oss'?: {
       *       'access-key-id'?: string
       *       'access-key-secret'?: string
       *       'base-path'?: string
       *       'bucket-name'?: string
       *       'bucket-url'?: string
       *       endpoint?: string
       *     }
       *     // auto
       *     autocode?: {
       *       'ai-path'?: string
       *       module?: string
       *       root?: string
       *       server?: string
       *       web?: string
       *     }
       *     'aws-s3'?: {
       *       'base-url'?: string
       *       bucket?: string
       *       'disable-ssl'?: boolean
       *       endpoint?: string
       *       'path-prefix'?: string
       *       region?: string
       *       's3-force-path-style'?: boolean
       *       'secret-id'?: string
       *       'secret-key'?: string
       *     }
       *     captcha?: {
       *       // 验证码高度
       *       'img-height'?: number
       *       // 验证码宽度
       *       'img-width'?: number
       *       // 验证码长度
       *       'key-long'?: number
       *       // 防爆破验证码开启此数，0代表每次登录都需要验证码，其他数字代表错误密码次数，如3代表错误三次后出现验证码
       *       'open-captcha'?: number
       *       // 防爆破验证码超时时间，单位：s(秒)
       *       'open-captcha-timeout'?: number
       *     }
       *     'cloudflare-r2'?: {
       *       'access-key-id'?: string
       *       'account-id'?: string
       *       'base-url'?: string
       *       bucket?: string
       *       path?: string
       *       'secret-access-key'?: string
       *     }
       *     // 跨域配置
       *     cors?: {
       *       mode?: string
       *       // [items] start
       *       // [items] end
       *       whitelist?: Array<{
       *         'allow-credentials'?: boolean
       *         'allow-headers'?: string
       *         'allow-methods'?: string
       *         'allow-origin'?: string
       *         'expose-headers'?: string
       *       }>
       *     }
       *     // [items] start
       *     // [items] end
       *     'db-list'?: Array<{
       *       'alias-name'?: string
       *       // 高级配置
       *       config?: string
       *       // 数据库名
       *       'db-name'?: string
       *       disable?: boolean
       *       // 数据库引擎，默认InnoDB
       *       engine?: string
       *       // 是否开启Gorm全局日志
       *       'log-mode'?: string
       *       // 是否通过zap写入日志文件
       *       'log-zap'?: boolean
       *       // 空闲中的最大连接数
       *       'max-idle-conns'?: number
       *       // 打开到数据库的最大连接数
       *       'max-open-conns'?: number
       *       // 数据库密码
       *       password?: string
       *       // 数据库地址
       *       path?: string
       *       // 数据库端口
       *       port?: string
       *       // 数据库前缀
       *       prefix?: string
       *       // 是否开启全局禁用复数，true表示开启
       *       singular?: boolean
       *       type?: string
       *       // 数据库账号
       *       username?: string
       *     }>
       *     // [items] start
       *     // [items] end
       *     'disk-list'?: Array<{
       *       'mount-point'?: string
       *     }>
       *     email?: {
       *       // 发件人  你自己要发邮件的邮箱
       *       from?: string
       *       // 服务器地址 例如 smtp.qq.com  请前往QQ或者你要发邮件的邮箱查看其smtp协议
       *       host?: string
       *       // 是否LoginAuth   是否使用LoginAuth认证方式（适用于IBM、微软邮箱服务器等）
       *       'is-loginauth'?: boolean
       *       // 是否SSL   是否开启SSL
       *       'is-ssl'?: boolean
       *       // 昵称    发件人昵称 通常为自己的邮箱
       *       nickname?: string
       *       // 端口     请前往QQ或者你要发邮件的邮箱查看其smtp协议 大多为 465
       *       port?: number
       *       // 密钥    用于登录的密钥 最好不要用邮箱密码 去邮箱smtp申请一个用于登录的密钥
       *       secret?: string
       *       // 收件人:多个以英文逗号分隔 例：a@qq.com b@qq.com 正式开发中请把此项目作为参数使用
       *       to?: string
       *     }
       *     excel?: {
       *       dir?: string
       *     }
       *     'hua-wei-obs'?: {
       *       'access-key'?: string
       *       bucket?: string
       *       endpoint?: string
       *       path?: string
       *       'secret-key'?: string
       *     }
       *     jwt?: {
       *       // 缓冲时间
       *       'buffer-time'?: string
       *       // 过期时间
       *       'expires-time'?: string
       *       // 签发者
       *       issuer?: string
       *       // jwt签名
       *       'signing-key'?: string
       *     }
       *     // oss
       *     local?: {
       *       // 本地文件访问路径
       *       path?: string
       *       // 本地文件存储路径
       *       'store-path'?: string
       *     }
       *     // MCP配置
       *     mcp?: {
       *       // 独立MCP服务端口
       *       addr?: number
       *       // 消息路径
       *       message_path?: string
       *       // MCP名称
       *       name?: string
       *       // 是否独立运行MCP服务
       *       separate?: boolean
       *       // SSE路径
       *       sse_path?: string
       *       // URL前缀
       *       url_prefix?: string
       *       // MCP版本
       *       version?: string
       *     }
       *     minio?: {
       *       'access-key-id'?: string
       *       'access-key-secret'?: string
       *       'base-path'?: string
       *       'bucket-name'?: string
       *       'bucket-url'?: string
       *       endpoint?: string
       *       'use-ssl'?: boolean
       *     }
       *     mongo?: {
       *       // 验证数据库
       *       'auth-source'?: string
       *       // collection name
       *       coll?: string
       *       // 连接超时时间
       *       'connect-timeout-ms'?: number
       *       // database name
       *       database?: string
       *       // 主机列表
       *       // [items] start
       *       // [items] end
       *       hosts?: Array<{
       *         // ip地址
       *         host?: string
       *         // 端口
       *         port?: string
       *       }>
       *       // 是否开启zap日志
       *       'is-zap'?: boolean
       *       // 最大连接池
       *       'max-pool-size'?: number
       *       // 最小连接池
       *       'min-pool-size'?: number
       *       // mongodb options
       *       options?: string
       *       // 密码
       *       password?: string
       *       // socket超时时间
       *       'socket-timeout-ms'?: number
       *       // 用户名
       *       username?: string
       *     }
       *     mssql?: {
       *       // 高级配置
       *       config?: string
       *       // 数据库名
       *       'db-name'?: string
       *       // 数据库引擎，默认InnoDB
       *       engine?: string
       *       // 是否开启Gorm全局日志
       *       'log-mode'?: string
       *       // 是否通过zap写入日志文件
       *       'log-zap'?: boolean
       *       // 空闲中的最大连接数
       *       'max-idle-conns'?: number
       *       // 打开到数据库的最大连接数
       *       'max-open-conns'?: number
       *       // 数据库密码
       *       password?: string
       *       // 数据库地址
       *       path?: string
       *       // 数据库端口
       *       port?: string
       *       // 数据库前缀
       *       prefix?: string
       *       // 是否开启全局禁用复数，true表示开启
       *       singular?: boolean
       *       // 数据库账号
       *       username?: string
       *     }
       *     // gorm
       *     mysql?: {
       *       // 高级配置
       *       config?: string
       *       // 数据库名
       *       'db-name'?: string
       *       // 数据库引擎，默认InnoDB
       *       engine?: string
       *       // 是否开启Gorm全局日志
       *       'log-mode'?: string
       *       // 是否通过zap写入日志文件
       *       'log-zap'?: boolean
       *       // 空闲中的最大连接数
       *       'max-idle-conns'?: number
       *       // 打开到数据库的最大连接数
       *       'max-open-conns'?: number
       *       // 数据库密码
       *       password?: string
       *       // 数据库地址
       *       path?: string
       *       // 数据库端口
       *       port?: string
       *       // 数据库前缀
       *       prefix?: string
       *       // 是否开启全局禁用复数，true表示开启
       *       singular?: boolean
       *       // 数据库账号
       *       username?: string
       *     }
       *     oracle?: {
       *       // 高级配置
       *       config?: string
       *       // 数据库名
       *       'db-name'?: string
       *       // 数据库引擎，默认InnoDB
       *       engine?: string
       *       // 是否开启Gorm全局日志
       *       'log-mode'?: string
       *       // 是否通过zap写入日志文件
       *       'log-zap'?: boolean
       *       // 空闲中的最大连接数
       *       'max-idle-conns'?: number
       *       // 打开到数据库的最大连接数
       *       'max-open-conns'?: number
       *       // 数据库密码
       *       password?: string
       *       // 数据库地址
       *       path?: string
       *       // 数据库端口
       *       port?: string
       *       // 数据库前缀
       *       prefix?: string
       *       // 是否开启全局禁用复数，true表示开启
       *       singular?: boolean
       *       // 数据库账号
       *       username?: string
       *     }
       *     pgsql?: {
       *       // 高级配置
       *       config?: string
       *       // 数据库名
       *       'db-name'?: string
       *       // 数据库引擎，默认InnoDB
       *       engine?: string
       *       // 是否开启Gorm全局日志
       *       'log-mode'?: string
       *       // 是否通过zap写入日志文件
       *       'log-zap'?: boolean
       *       // 空闲中的最大连接数
       *       'max-idle-conns'?: number
       *       // 打开到数据库的最大连接数
       *       'max-open-conns'?: number
       *       // 数据库密码
       *       password?: string
       *       // 数据库地址
       *       path?: string
       *       // 数据库端口
       *       port?: string
       *       // 数据库前缀
       *       prefix?: string
       *       // 是否开启全局禁用复数，true表示开启
       *       singular?: boolean
       *       // 数据库账号
       *       username?: string
       *     }
       *     qiniu?: {
       *       // 秘钥AK
       *       'access-key'?: string
       *       // 空间名称
       *       bucket?: string
       *       // CDN加速域名
       *       'img-path'?: string
       *       // 秘钥SK
       *       'secret-key'?: string
       *       // 上传是否使用CDN上传加速
       *       'use-cdn-domains'?: boolean
       *       // 是否使用https
       *       'use-https'?: boolean
       *       // 存储区域
       *       zone?: string
       *     }
       *     redis?: {
       *       // 服务器地址:端口
       *       addr?: string
       *       // 集群模式下的节点地址列表
       *       // [items] start
       *       // [items] end
       *       clusterAddrs?: string[]
       *       // 单实例模式下redis的哪个数据库
       *       db?: number
       *       // 代表当前实例的名字
       *       name?: string
       *       // 密码
       *       password?: string
       *       // 是否使用集群模式
       *       useCluster?: boolean
       *     }
       *     // [items] start
       *     // [items] end
       *     'redis-list'?: Array<{
       *       // 服务器地址:端口
       *       addr?: string
       *       // 集群模式下的节点地址列表
       *       // [items] start
       *       // [items] end
       *       clusterAddrs?: string[]
       *       // 单实例模式下redis的哪个数据库
       *       db?: number
       *       // 代表当前实例的名字
       *       name?: string
       *       // 密码
       *       password?: string
       *       // 是否使用集群模式
       *       useCluster?: boolean
       *     }>
       *     sqlite?: {
       *       // 高级配置
       *       config?: string
       *       // 数据库名
       *       'db-name'?: string
       *       // 数据库引擎，默认InnoDB
       *       engine?: string
       *       // 是否开启Gorm全局日志
       *       'log-mode'?: string
       *       // 是否通过zap写入日志文件
       *       'log-zap'?: boolean
       *       // 空闲中的最大连接数
       *       'max-idle-conns'?: number
       *       // 打开到数据库的最大连接数
       *       'max-open-conns'?: number
       *       // 数据库密码
       *       password?: string
       *       // 数据库地址
       *       path?: string
       *       // 数据库端口
       *       port?: string
       *       // 数据库前缀
       *       prefix?: string
       *       // 是否开启全局禁用复数，true表示开启
       *       singular?: boolean
       *       // 数据库账号
       *       username?: string
       *     }
       *     system?: {
       *       // 端口值
       *       addr?: number
       *       // 数据库类型:mysql(默认)|sqlite|sqlserver|postgresql
       *       'db-type'?: string
       *       // 自动迁移数据库表结构，生产环境建议设为false，手动迁移
       *       'disable-auto-migrate'?: boolean
       *       'iplimit-count'?: number
       *       'iplimit-time'?: number
       *       // Oss类型
       *       'oss-type'?: string
       *       'router-prefix'?: string
       *       // 使用mongo
       *       'use-mongo'?: boolean
       *       // 多点登录拦截
       *       'use-multipoint'?: boolean
       *       // 使用redis
       *       'use-redis'?: boolean
       *       // 使用树形角色分配模式
       *       'use-strict-auth'?: boolean
       *     }
       *     'tencent-cos'?: {
       *       'base-url'?: string
       *       bucket?: string
       *       'path-prefix'?: string
       *       region?: string
       *       'secret-id'?: string
       *       'secret-key'?: string
       *     }
       *     zap?: {
       *       // 日志文件夹
       *       director?: string
       *       // 编码级
       *       'encode-level'?: string
       *       // 输出
       *       format?: string
       *       // 级别
       *       level?: string
       *       // 输出控制台
       *       'log-in-console'?: boolean
       *       // 日志前缀
       *       prefix?: string
       *       // 日志保留天数
       *       'retention-day'?: number
       *       // 显示行
       *       'show-line'?: boolean
       *       // 栈名
       *       'stacktrace-key'?: string
       *     }
       *   }
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: string
       * }
       * ```
       */
      post_system_setsystemconfig<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: string;
          }
        > & {
          data: System_system;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: string;
        },
        'System.post_system_setsystemconfig',
        Config
      >;
    };
    ExaFileUploadAndDownload: {
      /**
       * ---
       *
       * [POST] 断点续传到服务器
       *
       * **path:** /fileUploadAndDownload/breakpointContinue
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // an example for breakpoint resume, 断点续传示例
       *   file: Blob
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_fileuploadanddownload_breakpointcontinue<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: {
            /**
             * an example for breakpoint resume, 断点续传示例
             */
            file: Blob;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'ExaFileUploadAndDownload.post_fileuploadanddownload_breakpointcontinue',
        Config
      >;
      /**
       * ---
       *
       * [POST] 删除文件
       *
       * **path:** /fileUploadAndDownload/deleteFile
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 分类id
       *   classId?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 编号
       *   key?: string
       *   // 文件名
       *   name?: string
       *   // 文件标签
       *   tag?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 文件地址
       *   url?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_fileuploadanddownload_deletefile<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Example_exafileuploadanddownload;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'ExaFileUploadAndDownload.post_fileuploadanddownload_deletefile',
        Config
      >;
      /**
       * ---
       *
       * [GET] 查找文件
       *
       * **path:** /fileUploadAndDownload/findFile
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Find the file, 查找文件
       *   file: Blob
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     file?: {
       *       // 主键ID
       *       ID?: number
       *       chunkTotal?: number
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [items] end
       *       exaFileChunk?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         exaFileID?: number
       *         fileChunkNumber?: number
       *         fileChunkPath?: string
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       fileMd5?: string
       *       fileName?: string
       *       filePath?: string
       *       isFinish?: boolean
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_fileuploadanddownload_findfile<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_fileresponse;
            msg?: string;
          }
        > & {
          data: {
            /**
             * Find the file, 查找文件
             */
            file: Blob;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_fileresponse;
          msg?: string;
        },
        'ExaFileUploadAndDownload.get_fileuploadanddownload_findfile',
        Config
      >;
      /**
       * ---
       *
       * [POST] 创建文件
       *
       * **path:** /fileUploadAndDownload/findFile
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 上传文件完成
       *   file: Blob
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     filePath?: string
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_fileuploadanddownload_findfile<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_filepathresponse;
            msg?: string;
          }
        > & {
          data: {
            /**
             * 上传文件完成
             */
            file: Blob;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_filepathresponse;
          msg?: string;
        },
        'ExaFileUploadAndDownload.post_fileuploadanddownload_findfile',
        Config
      >;
      /**
       * ---
       *
       * [POST] 分页文件列表
       *
       * **path:** /fileUploadAndDownload/getFileList
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   classId?: number
       *   // 关键字
       *   keyword?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_fileuploadanddownload_getfilelist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          data: Request_exaattachmentcategorysearch;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'ExaFileUploadAndDownload.post_fileuploadanddownload_getfilelist',
        Config
      >;
      /**
       * ---
       *
       * [POST] 导入URL
       *
       * **path:** /fileUploadAndDownload/importURL
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 分类id
       *   classId?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 编号
       *   key?: string
       *   // 文件名
       *   name?: string
       *   // 文件标签
       *   tag?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 文件地址
       *   url?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_fileuploadanddownload_importurl<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Example_exafileuploadanddownload;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'ExaFileUploadAndDownload.post_fileuploadanddownload_importurl',
        Config
      >;
      /**
       * ---
       *
       * [POST] 删除切片
       *
       * **path:** /fileUploadAndDownload/removeChunk
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 删除缓存切片
       *   file: Blob
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_fileuploadanddownload_removechunk<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: {
            /**
             * 删除缓存切片
             */
            file: Blob;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'ExaFileUploadAndDownload.post_fileuploadanddownload_removechunk',
        Config
      >;
      /**
       * ---
       *
       * [POST] 上传文件示例
       *
       * **path:** /fileUploadAndDownload/upload
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 上传文件示例
       *   file: Blob
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     file?: {
       *       // 主键ID
       *       ID?: number
       *       // 分类id
       *       classId?: number
       *       // 创建时间
       *       createdAt?: string
       *       // 编号
       *       key?: string
       *       // 文件名
       *       name?: string
       *       // 文件标签
       *       tag?: string
       *       // 更新时间
       *       updatedAt?: string
       *       // 文件地址
       *       url?: string
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_fileuploadanddownload_upload<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_exafileresponse;
            msg?: string;
          }
        > & {
          data: {
            /**
             * 上传文件示例
             */
            file: Blob;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_exafileresponse;
          msg?: string;
        },
        'ExaFileUploadAndDownload.post_fileuploadanddownload_upload',
        Config
      >;
    };
    Floors: {
      /**
       * ---
       *
       * [POST] 创建楼层表
       *
       * **path:** /floors/createFloors
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 楼层名称（用户自定义）
       *   name?: string
       *   // 排序字段（从小到大）
       *   sort_order?: number
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_floors_createfloors<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_floors;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Floors.post_floors_createfloors',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除楼层表
       *
       * **path:** /floors/deleteFloors
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 楼层名称（用户自定义）
       *   name?: string
       *   // 排序字段（从小到大）
       *   sort_order?: number
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_floors_deletefloors<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_floors;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Floors.delete_floors_deletefloors',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除楼层表
       *
       * **path:** /floors/deleteFloorsByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_floors_deletefloorsbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Floors.delete_floors_deletefloorsbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询楼层表
       *
       * **path:** /floors/findFloors
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 用id查询楼层表
       *   ID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 楼层名称（用户自定义）
       *     name?: string
       *     // 排序字段（从小到大）
       *     sort_order?: number
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_floors_findfloors<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Miniprogram_floors;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 用id查询楼层表
             */
            ID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Miniprogram_floors;
          msg?: string;
        },
        'Floors.get_floors_findfloors',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取楼层表列表
       *
       * **path:** /floors/getFloorsList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   // 关键字
       *   keyword?: string
       *   name?: string
       *   order?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   sort?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_floors_getfloorslist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            /**
             * 关键字
             */
            keyword?: string;
            name?: string;
            order?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            sort?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'Floors.get_floors_getfloorslist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的楼层表接口
       *
       * **path:** /floors/getFloorsPublic
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_floors_getfloorspublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'Floors.get_floors_getfloorspublic',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新楼层表
       *
       * **path:** /floors/updateFloors
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 楼层名称（用户自定义）
       *   name?: string
       *   // 排序字段（从小到大）
       *   sort_order?: number
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_floors_updatefloors<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_floors;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Floors.put_floors_updatefloors',
        Config
      >;
    };
    Info: {
      /**
       * ---
       *
       * [POST] 创建公告
       *
       * **path:** /info/createInfo
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 附件
       *   // [items] start
       *   // [items] end
       *   attachments?: object[]
       *   // 内容
       *   content?: string
       *   // 创建时间
       *   createdAt?: string
       *   // 标题
       *   title?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 作者
       *   userID?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_info_createinfo<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Model_info;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Info.post_info_createinfo',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除公告
       *
       * **path:** /info/deleteInfo
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 附件
       *   // [items] start
       *   // [items] end
       *   attachments?: object[]
       *   // 内容
       *   content?: string
       *   // 创建时间
       *   createdAt?: string
       *   // 标题
       *   title?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 作者
       *   userID?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_info_deleteinfo<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Model_info;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Info.delete_info_deleteinfo',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除公告
       *
       * **path:** /info/deleteInfoByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_info_deleteinfobyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Info.delete_info_deleteinfobyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询公告
       *
       * **path:** /info/findInfo
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 主键ID
       *   ID?: number
       *   // 内容
       *   content?: string
       *   // 创建时间
       *   createdAt?: string
       *   // 标题
       *   title?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 作者
       *   userID?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 附件
       *     // [items] start
       *     // [items] end
       *     attachments?: object[]
       *     // 内容
       *     content?: string
       *     // 创建时间
       *     createdAt?: string
       *     // 标题
       *     title?: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 作者
       *     userID?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_info_findinfo<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Model_info;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 主键ID
             */
            ID?: number;
            /**
             * 内容
             */
            content?: string;
            /**
             * 创建时间
             */
            createdAt?: string;
            /**
             * 标题
             */
            title?: string;
            /**
             * 更新时间
             */
            updatedAt?: string;
            /**
             * 作者
             */
            userID?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Model_info;
          msg?: string;
        },
        'Info.get_info_findinfo',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取Info的数据源
       *
       * **path:** /info/getInfoDataSource
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_info_getinfodatasource<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'Info.get_info_getinfodatasource',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取公告列表
       *
       * **path:** /info/getInfoList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   endCreatedAt?: string
       *   // 关键字
       *   keyword?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   startCreatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_info_getinfolist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            endCreatedAt?: string;
            /**
             * 关键字
             */
            keyword?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            startCreatedAt?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'Info.get_info_getinfolist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的公告接口
       *
       * **path:** /info/getInfoPublic
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   endCreatedAt?: string
       *   // 关键字
       *   keyword?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   startCreatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_info_getinfopublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        > & {
          params: {
            endCreatedAt?: string;
            /**
             * 关键字
             */
            keyword?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            startCreatedAt?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'Info.get_info_getinfopublic',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新公告
       *
       * **path:** /info/updateInfo
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 附件
       *   // [items] start
       *   // [items] end
       *   attachments?: object[]
       *   // 内容
       *   content?: string
       *   // 创建时间
       *   createdAt?: string
       *   // 标题
       *   title?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 作者
       *   userID?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_info_updateinfo<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Model_info;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Info.put_info_updateinfo',
        Config
      >;
    };
    CheckDB: {
      /**
       * ---
       *
       * [POST] 初始化用户数据库
       *
       * **path:** /init/checkdb
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_init_checkdb<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'CheckDB.post_init_checkdb',
        Config
      >;
    };
    InitDB: {
      /**
       * ---
       *
       * [POST] 初始化用户数据库
       *
       * **path:** /init/initdb
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   adminPassword: string
       *   // 数据库名
       *   dbName: string
       *   // sqlite数据库文件路径
       *   dbPath?: string
       *   // 数据库类型
       *   dbType?: string
       *   // 服务器地址
       *   host?: string
       *   // 数据库密码
       *   password?: string
       *   // 数据库连接端口
       *   port?: string
       *   // postgresql指定template
       *   template?: string
       *   // 数据库用户名
       *   userName?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: string
       * }
       * ```
       */
      post_init_initdb<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: string;
          }
        > & {
          data: Request_initdb;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: string;
        },
        'InitDB.post_init_initdb',
        Config
      >;
    };
    Jwt: {
      /**
       * ---
       *
       * [POST] jwt加入黑名单
       *
       * **path:** /jwt/jsonInBlacklist
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_jwt_jsoninblacklist<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Jwt.post_jwt_jsoninblacklist',
        Config
      >;
    };
    Menu: {
      /**
       * ---
       *
       * [POST] 新增菜单
       *
       * **path:** /menu/addBaseMenu
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // [items] start
       *   // [items] end
       *   authoritys?: Array<{
       *     // 角色ID
       *     authorityId?: number
       *     // 角色名
       *     authorityName?: string
       *     // [items] start
       *     // [cycle] $.authoritys.[]
       *     // [items] end
       *     children?: System_sysauthority[]
       *     // 创建时间
       *     createdAt?: string
       *     // [items] start
       *     // [cycle] $.authoritys.[]
       *     // [items] end
       *     dataAuthorityId?: System_sysauthority[]
       *     // 默认菜单(默认dashboard)
       *     defaultRouter?: string
       *     deletedAt?: string
       *     // [items] start
       *     // [cycle] $
       *     // [items] end
       *     menus?: System_sysbasemenu[]
       *     // 父角色ID
       *     parentId?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysbasemenu[]
       *   // 对应前端文件路径
       *   component?: string
       *   // 创建时间
       *   createdAt?: string
       *   // 是否在列表隐藏
       *   hidden?: boolean
       *   // [items] start
       *   // [items] end
       *   menuBtn?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     desc?: string
       *     name?: string
       *     sysBaseMenuID?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 附加属性
       *   meta?: {
       *     activeName?: string
       *     // 自动关闭tab
       *     closeTab?: boolean
       *     // 是否是基础路由（开发中）
       *     defaultMenu?: boolean
       *     // 菜单图标
       *     icon?: string
       *     // 是否缓存
       *     keepAlive?: boolean
       *     // 菜单名
       *     title?: string
       *     // 路由切换动画
       *     transitionType?: string
       *   }
       *   // 路由name
       *   name?: string
       *   // [items] start
       *   // [items] end
       *   parameters?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     // 地址栏携带参数的key
       *     key?: string
       *     sysBaseMenuID?: number
       *     // 地址栏携带参数为params还是query
       *     type?: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 地址栏携带参数的值
       *     value?: string
       *   }>
       *   // 父菜单ID
       *   parentId?: number
       *   // 路由path
       *   path?: string
       *   // 排序标记
       *   sort?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_menu_addbasemenu<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysbasemenu;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Menu.post_menu_addbasemenu',
        Config
      >;
      /**
       * ---
       *
       * [POST] 删除菜单
       *
       * **path:** /menu/deleteBaseMenu
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_menu_deletebasemenu<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_getbyid;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Menu.post_menu_deletebasemenu',
        Config
      >;
      /**
       * ---
       *
       * [POST] 根据id获取菜单
       *
       * **path:** /menu/getBaseMenuById
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     menu?: {
       *       // 主键ID
       *       ID?: number
       *       // [items] start
       *       // [items] end
       *       authoritys?: Array<{
       *         // 角色ID
       *         authorityId?: number
       *         // 角色名
       *         authorityName?: string
       *         // [items] start
       *         // [cycle] $.data.menu.authoritys.[]
       *         // [items] end
       *         children?: System_sysauthority[]
       *         // 创建时间
       *         createdAt?: string
       *         // [items] start
       *         // [cycle] $.data.menu.authoritys.[]
       *         // [items] end
       *         dataAuthorityId?: System_sysauthority[]
       *         // 默认菜单(默认dashboard)
       *         defaultRouter?: string
       *         deletedAt?: string
       *         // [items] start
       *         // [cycle] $.data.menu
       *         // [items] end
       *         menus?: System_sysbasemenu[]
       *         // 父角色ID
       *         parentId?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // [items] start
       *       // [cycle] $.data.menu
       *       // [items] end
       *       children?: System_sysbasemenu[]
       *       // 对应前端文件路径
       *       component?: string
       *       // 创建时间
       *       createdAt?: string
       *       // 是否在列表隐藏
       *       hidden?: boolean
       *       // [items] start
       *       // [items] end
       *       menuBtn?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         desc?: string
       *         name?: string
       *         sysBaseMenuID?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 附加属性
       *       meta?: {
       *         activeName?: string
       *         // 自动关闭tab
       *         closeTab?: boolean
       *         // 是否是基础路由（开发中）
       *         defaultMenu?: boolean
       *         // 菜单图标
       *         icon?: string
       *         // 是否缓存
       *         keepAlive?: boolean
       *         // 菜单名
       *         title?: string
       *         // 路由切换动画
       *         transitionType?: string
       *       }
       *       // 路由name
       *       name?: string
       *       // [items] start
       *       // [items] end
       *       parameters?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 地址栏携带参数的key
       *         key?: string
       *         sysBaseMenuID?: number
       *         // 地址栏携带参数为params还是query
       *         type?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 地址栏携带参数的值
       *         value?: string
       *       }>
       *       // 父菜单ID
       *       parentId?: number
       *       // 路由path
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_menu_getbasemenubyid<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysbasemenuresponse;
            msg?: string;
          }
        > & {
          data: Request_getbyid;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysbasemenuresponse;
          msg?: string;
        },
        'Menu.post_menu_getbasemenubyid',
        Config
      >;
      /**
       * ---
       *
       * [POST] 分页获取基础menu列表
       *
       * **path:** /menu/getMenuList
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 关键字
       *   keyword?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_menu_getmenulist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          data: Request_pageinfo;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'Menu.post_menu_getmenulist',
        Config
      >;
      /**
       * ---
       *
       * [POST] 更新菜单
       *
       * **path:** /menu/updateBaseMenu
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // [items] start
       *   // [items] end
       *   authoritys?: Array<{
       *     // 角色ID
       *     authorityId?: number
       *     // 角色名
       *     authorityName?: string
       *     // [items] start
       *     // [cycle] $.authoritys.[]
       *     // [items] end
       *     children?: System_sysauthority[]
       *     // 创建时间
       *     createdAt?: string
       *     // [items] start
       *     // [cycle] $.authoritys.[]
       *     // [items] end
       *     dataAuthorityId?: System_sysauthority[]
       *     // 默认菜单(默认dashboard)
       *     defaultRouter?: string
       *     deletedAt?: string
       *     // [items] start
       *     // [cycle] $
       *     // [items] end
       *     menus?: System_sysbasemenu[]
       *     // 父角色ID
       *     parentId?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysbasemenu[]
       *   // 对应前端文件路径
       *   component?: string
       *   // 创建时间
       *   createdAt?: string
       *   // 是否在列表隐藏
       *   hidden?: boolean
       *   // [items] start
       *   // [items] end
       *   menuBtn?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     desc?: string
       *     name?: string
       *     sysBaseMenuID?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 附加属性
       *   meta?: {
       *     activeName?: string
       *     // 自动关闭tab
       *     closeTab?: boolean
       *     // 是否是基础路由（开发中）
       *     defaultMenu?: boolean
       *     // 菜单图标
       *     icon?: string
       *     // 是否缓存
       *     keepAlive?: boolean
       *     // 菜单名
       *     title?: string
       *     // 路由切换动画
       *     transitionType?: string
       *   }
       *   // 路由name
       *   name?: string
       *   // [items] start
       *   // [items] end
       *   parameters?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     // 地址栏携带参数的key
       *     key?: string
       *     sysBaseMenuID?: number
       *     // 地址栏携带参数为params还是query
       *     type?: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 地址栏携带参数的值
       *     value?: string
       *   }>
       *   // 父菜单ID
       *   parentId?: number
       *   // 路由path
       *   path?: string
       *   // 排序标记
       *   sort?: number
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_menu_updatebasemenu<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysbasemenu;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Menu.post_menu_updatebasemenu',
        Config
      >;
    };
    AuthorityMenu: {
      /**
       * ---
       *
       * [POST] 增加menu和角色关联关系
       *
       * **path:** /menu/addMenuAuthority
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 角色ID
       *   authorityId?: number
       *   // [items] start
       *   // [items] end
       *   menus?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // [items] start
       *     // [items] end
       *     authoritys?: Array<{
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.menus.[].authoritys.[]
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.menus.[].authoritys.[]
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [cycle] $.menus.[]
       *       // [items] end
       *       menus?: System_sysbasemenu[]
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // [items] start
       *     // [cycle] $.menus.[]
       *     // [items] end
       *     children?: System_sysbasemenu[]
       *     // 对应前端文件路径
       *     component?: string
       *     // 创建时间
       *     createdAt?: string
       *     // 是否在列表隐藏
       *     hidden?: boolean
       *     // [items] start
       *     // [items] end
       *     menuBtn?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       desc?: string
       *       name?: string
       *       sysBaseMenuID?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 附加属性
       *     meta?: {
       *       activeName?: string
       *       // 自动关闭tab
       *       closeTab?: boolean
       *       // 是否是基础路由（开发中）
       *       defaultMenu?: boolean
       *       // 菜单图标
       *       icon?: string
       *       // 是否缓存
       *       keepAlive?: boolean
       *       // 菜单名
       *       title?: string
       *       // 路由切换动画
       *       transitionType?: string
       *     }
       *     // 路由name
       *     name?: string
       *     // [items] start
       *     // [items] end
       *     parameters?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       // 地址栏携带参数的key
       *       key?: string
       *       sysBaseMenuID?: number
       *       // 地址栏携带参数为params还是query
       *       type?: string
       *       // 更新时间
       *       updatedAt?: string
       *       // 地址栏携带参数的值
       *       value?: string
       *     }>
       *     // 父菜单ID
       *     parentId?: number
       *     // 路由path
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_menu_addmenuauthority<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_addmenuauthorityinfo;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'AuthorityMenu.post_menu_addmenuauthority',
        Config
      >;
      /**
       * ---
       *
       * [POST] 获取用户动态路由
       *
       * **path:** /menu/getBaseMenuTree
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = object
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     menus?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // [items] start
       *       // [items] end
       *       authoritys?: Array<{
       *         // 角色ID
       *         authorityId?: number
       *         // 角色名
       *         authorityName?: string
       *         // [items] start
       *         // [cycle] $.data.menus.[].authoritys.[]
       *         // [items] end
       *         children?: System_sysauthority[]
       *         // 创建时间
       *         createdAt?: string
       *         // [items] start
       *         // [cycle] $.data.menus.[].authoritys.[]
       *         // [items] end
       *         dataAuthorityId?: System_sysauthority[]
       *         // 默认菜单(默认dashboard)
       *         defaultRouter?: string
       *         deletedAt?: string
       *         // [items] start
       *         // [cycle] $.data.menus.[]
       *         // [items] end
       *         menus?: System_sysbasemenu[]
       *         // 父角色ID
       *         parentId?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // [items] start
       *       // [cycle] $.data.menus.[]
       *       // [items] end
       *       children?: System_sysbasemenu[]
       *       // 对应前端文件路径
       *       component?: string
       *       // 创建时间
       *       createdAt?: string
       *       // 是否在列表隐藏
       *       hidden?: boolean
       *       // [items] start
       *       // [items] end
       *       menuBtn?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         desc?: string
       *         name?: string
       *         sysBaseMenuID?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 附加属性
       *       meta?: {
       *         activeName?: string
       *         // 自动关闭tab
       *         closeTab?: boolean
       *         // 是否是基础路由（开发中）
       *         defaultMenu?: boolean
       *         // 菜单图标
       *         icon?: string
       *         // 是否缓存
       *         keepAlive?: boolean
       *         // 菜单名
       *         title?: string
       *         // 路由切换动画
       *         transitionType?: string
       *       }
       *       // 路由name
       *       name?: string
       *       // [items] start
       *       // [items] end
       *       parameters?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 地址栏携带参数的key
       *         key?: string
       *         sysBaseMenuID?: number
       *         // 地址栏携带参数为params还是query
       *         type?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 地址栏携带参数的值
       *         value?: string
       *       }>
       *       // 父菜单ID
       *       parentId?: number
       *       // 路由path
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_menu_getbasemenutree<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysbasemenusresponse;
            msg?: string;
          }
        > & {
          data: Request_empty;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysbasemenusresponse;
          msg?: string;
        },
        'AuthorityMenu.post_menu_getbasemenutree',
        Config
      >;
      /**
       * ---
       *
       * [POST] 获取用户动态路由
       *
       * **path:** /menu/getMenu
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = object
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     menus?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // [items] start
       *       // [items] end
       *       authoritys?: Array<{
       *         // 角色ID
       *         authorityId?: number
       *         // 角色名
       *         authorityName?: string
       *         // [items] start
       *         // [cycle] $.data.menus.[].authoritys.[]
       *         // [items] end
       *         children?: System_sysauthority[]
       *         // 创建时间
       *         createdAt?: string
       *         // [items] start
       *         // [cycle] $.data.menus.[].authoritys.[]
       *         // [items] end
       *         dataAuthorityId?: System_sysauthority[]
       *         // 默认菜单(默认dashboard)
       *         defaultRouter?: string
       *         deletedAt?: string
       *         // [items] start
       *         // [items] end
       *         menus?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // [items] start
       *           // [cycle] $.data.menus.[].authoritys.[]
       *           // [items] end
       *           authoritys?: System_sysauthority[]
       *           // [items] start
       *           // [cycle] $.data.menus.[].authoritys.[].menus.[]
       *           // [items] end
       *           children?: System_sysbasemenu[]
       *           // 对应前端文件路径
       *           component?: string
       *           // 创建时间
       *           createdAt?: string
       *           // 是否在列表隐藏
       *           hidden?: boolean
       *           // [items] start
       *           // [items] end
       *           menuBtn?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // 创建时间
       *             createdAt?: string
       *             desc?: string
       *             name?: string
       *             sysBaseMenuID?: number
       *             // 更新时间
       *             updatedAt?: string
       *           }>
       *           // 附加属性
       *           meta?: {
       *             activeName?: string
       *             // 自动关闭tab
       *             closeTab?: boolean
       *             // 是否是基础路由（开发中）
       *             defaultMenu?: boolean
       *             // 菜单图标
       *             icon?: string
       *             // 是否缓存
       *             keepAlive?: boolean
       *             // 菜单名
       *             title?: string
       *             // 路由切换动画
       *             transitionType?: string
       *           }
       *           // 路由name
       *           name?: string
       *           // [items] start
       *           // [items] end
       *           parameters?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // 创建时间
       *             createdAt?: string
       *             // 地址栏携带参数的key
       *             key?: string
       *             sysBaseMenuID?: number
       *             // 地址栏携带参数为params还是query
       *             type?: string
       *             // 更新时间
       *             updatedAt?: string
       *             // 地址栏携带参数的值
       *             value?: string
       *           }>
       *           // 父菜单ID
       *           parentId?: number
       *           // 路由path
       *           path?: string
       *           // 排序标记
       *           sort?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 父角色ID
       *         parentId?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       btns?: Record<string, number>
       *       // [items] start
       *       // [cycle] $.data.menus.[]
       *       // [items] end
       *       children?: System_sysmenu[]
       *       // 对应前端文件路径
       *       component?: string
       *       // 创建时间
       *       createdAt?: string
       *       // 是否在列表隐藏
       *       hidden?: boolean
       *       // [items] start
       *       // [items] end
       *       menuBtn?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         desc?: string
       *         name?: string
       *         sysBaseMenuID?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       menuId?: number
       *       // 附加属性
       *       meta?: {
       *         activeName?: string
       *         // 自动关闭tab
       *         closeTab?: boolean
       *         // 是否是基础路由（开发中）
       *         defaultMenu?: boolean
       *         // 菜单图标
       *         icon?: string
       *         // 是否缓存
       *         keepAlive?: boolean
       *         // 菜单名
       *         title?: string
       *         // 路由切换动画
       *         transitionType?: string
       *       }
       *       // 路由name
       *       name?: string
       *       // [items] start
       *       // [items] end
       *       parameters?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 地址栏携带参数的key
       *         key?: string
       *         sysBaseMenuID?: number
       *         // 地址栏携带参数为params还是query
       *         type?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 地址栏携带参数的值
       *         value?: string
       *       }>
       *       // 父菜单ID
       *       parentId?: number
       *       // 路由path
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_menu_getmenu<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysmenusresponse;
            msg?: string;
          }
        > & {
          data: Request_empty;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysmenusresponse;
          msg?: string;
        },
        'AuthorityMenu.post_menu_getmenu',
        Config
      >;
      /**
       * ---
       *
       * [POST] 获取指定角色menu
       *
       * **path:** /menu/getMenuAuthority
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 角色ID
       *   authorityId?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      post_menu_getmenuauthority<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          data: Request_getauthorityid;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'AuthorityMenu.post_menu_getmenuauthority',
        Config
      >;
    };
    Order: {
      /**
       * ---
       *
       * [POST] 创建订单
       *
       * **path:** /order/createOrder
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 实际送达时间
       *   actual_delivery_time?: string
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 配送地址
       *   delivery_address?: string
       *   // 配送备注
       *   delivery_note?: string
       *   // 期望送达时间
       *   expected_delivery_time?: string
       *   // 下单金额
       *   order_amount?: number
       *   // 订单编号
       *   order_number?: string
       *   // 订单状态
       *   order_status?: string
       *   product?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 商品图片
       *     image?: string
       *     // 净含量
       *     net_weight: string
       *     // 产地
       *     origin: string
       *     // 原价
       *     original_price: number
       *     // 商品分类
       *     product_category?: string
       *     // 商品详情
       *     product_details?: string
       *     // 商品名称
       *     product_name: string
       *     // 销售数
       *     sales_count?: number
       *     // 售价
       *     selling_price: number
       *     // 保质期
       *     shelfLife: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *   }
       *   // 商品ID
       *   product_id?: number
       *   // 收货人姓名
       *   receiver_name?: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 用户ID
       *   user_id?: number
       *   // 用户手机号
       *   user_phone: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_order_createorder<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_order;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Order.post_order_createorder',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除订单
       *
       * **path:** /order/deleteOrder
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 实际送达时间
       *   actual_delivery_time?: string
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 配送地址
       *   delivery_address?: string
       *   // 配送备注
       *   delivery_note?: string
       *   // 期望送达时间
       *   expected_delivery_time?: string
       *   // 下单金额
       *   order_amount?: number
       *   // 订单编号
       *   order_number?: string
       *   // 订单状态
       *   order_status?: string
       *   product?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 商品图片
       *     image?: string
       *     // 净含量
       *     net_weight: string
       *     // 产地
       *     origin: string
       *     // 原价
       *     original_price: number
       *     // 商品分类
       *     product_category?: string
       *     // 商品详情
       *     product_details?: string
       *     // 商品名称
       *     product_name: string
       *     // 销售数
       *     sales_count?: number
       *     // 售价
       *     selling_price: number
       *     // 保质期
       *     shelfLife: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *   }
       *   // 商品ID
       *   product_id?: number
       *   // 收货人姓名
       *   receiver_name?: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 用户ID
       *   user_id?: number
       *   // 用户手机号
       *   user_phone: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_order_deleteorder<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_order;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Order.delete_order_deleteorder',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除订单
       *
       * **path:** /order/deleteOrderByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_order_deleteorderbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Order.delete_order_deleteorderbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询订单
       *
       * **path:** /order/findOrder
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 用id查询订单
       *   ID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 实际送达时间
       *     actual_delivery_time?: string
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 配送地址
       *     delivery_address?: string
       *     // 配送备注
       *     delivery_note?: string
       *     // 期望送达时间
       *     expected_delivery_time?: string
       *     // 下单金额
       *     order_amount?: number
       *     // 订单编号
       *     order_number?: string
       *     // 订单状态
       *     order_status?: string
       *     product?: {
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       createdBy?: number
       *       deletedBy?: number
       *       // 商品图片
       *       image?: string
       *       // 净含量
       *       net_weight: string
       *       // 产地
       *       origin: string
       *       // 原价
       *       original_price: number
       *       // 商品分类
       *       product_category?: string
       *       // 商品详情
       *       product_details?: string
       *       // 商品名称
       *       product_name: string
       *       // 销售数
       *       sales_count?: number
       *       // 售价
       *       selling_price: number
       *       // 保质期
       *       shelfLife: string
       *       // 更新时间
       *       updatedAt?: string
       *       updatedBy?: number
       *     }
       *     // 商品ID
       *     product_id?: number
       *     // 收货人姓名
       *     receiver_name?: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *     // 用户ID
       *     user_id?: number
       *     // 用户手机号
       *     user_phone: string
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_order_findorder<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Miniprogram_order;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 用id查询订单
             */
            ID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Miniprogram_order;
          msg?: string;
        },
        'Order.get_order_findorder',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取订单列表
       *
       * **path:** /order/getOrderList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   actual_delivery_timeRange?: string[]
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   endOrderAmount?: number
       *   // [items] start
       *   // [items] end
       *   expected_delivery_timeRange?: string[]
       *   // 关键字
       *   keyword?: string
       *   order?: string
       *   order_status?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   receiver_name?: string
       *   sort?: string
       *   startOrderAmount?: number
       *   user_phone?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_order_getorderlist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            actual_delivery_timeRange?: string[];
            createdAtRange?: string[];
            endOrderAmount?: number;
            expected_delivery_timeRange?: string[];
            /**
             * 关键字
             */
            keyword?: string;
            order?: string;
            order_status?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            receiver_name?: string;
            sort?: string;
            startOrderAmount?: number;
            user_phone?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'Order.get_order_getorderlist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的订单接口
       *
       * **path:** /order/getOrderPublic
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_order_getorderpublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'Order.get_order_getorderpublic',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新订单
       *
       * **path:** /order/updateOrder
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 实际送达时间
       *   actual_delivery_time?: string
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 配送地址
       *   delivery_address?: string
       *   // 配送备注
       *   delivery_note?: string
       *   // 期望送达时间
       *   expected_delivery_time?: string
       *   // 下单金额
       *   order_amount?: number
       *   // 订单编号
       *   order_number?: string
       *   // 订单状态
       *   order_status?: string
       *   product?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 商品图片
       *     image?: string
       *     // 净含量
       *     net_weight: string
       *     // 产地
       *     origin: string
       *     // 原价
       *     original_price: number
       *     // 商品分类
       *     product_category?: string
       *     // 商品详情
       *     product_details?: string
       *     // 商品名称
       *     product_name: string
       *     // 销售数
       *     sales_count?: number
       *     // 售价
       *     selling_price: number
       *     // 保质期
       *     shelfLife: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *   }
       *   // 商品ID
       *   product_id?: number
       *   // 收货人姓名
       *   receiver_name?: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 用户ID
       *   user_id?: number
       *   // 用户手机号
       *   user_phone: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_order_updateorder<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_order;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Order.put_order_updateorder',
        Config
      >;
    };
    Product: {
      /**
       * ---
       *
       * [POST] 创建商品
       *
       * **path:** /product/createProduct
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 商品图片
       *   image?: string
       *   // 净含量
       *   net_weight: string
       *   // 产地
       *   origin: string
       *   // 原价
       *   original_price: number
       *   // 商品分类
       *   product_category?: string
       *   // 商品详情
       *   product_details?: string
       *   // 商品名称
       *   product_name: string
       *   // 销售数
       *   sales_count?: number
       *   // 售价
       *   selling_price: number
       *   // 保质期
       *   shelfLife: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_product_createproduct<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_product;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Product.post_product_createproduct',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除商品
       *
       * **path:** /product/deleteProduct
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 商品图片
       *   image?: string
       *   // 净含量
       *   net_weight: string
       *   // 产地
       *   origin: string
       *   // 原价
       *   original_price: number
       *   // 商品分类
       *   product_category?: string
       *   // 商品详情
       *   product_details?: string
       *   // 商品名称
       *   product_name: string
       *   // 销售数
       *   sales_count?: number
       *   // 售价
       *   selling_price: number
       *   // 保质期
       *   shelfLife: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_product_deleteproduct<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_product;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Product.delete_product_deleteproduct',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除商品
       *
       * **path:** /product/deleteProductByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_product_deleteproductbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Product.delete_product_deleteproductbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询商品
       *
       * **path:** /product/findProduct
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 用id查询商品
       *   ID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 商品图片
       *     image?: string
       *     // 净含量
       *     net_weight: string
       *     // 产地
       *     origin: string
       *     // 原价
       *     original_price: number
       *     // 商品分类
       *     product_category?: string
       *     // 商品详情
       *     product_details?: string
       *     // 商品名称
       *     product_name: string
       *     // 销售数
       *     sales_count?: number
       *     // 售价
       *     selling_price: number
       *     // 保质期
       *     shelfLife: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_product_findproduct<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Miniprogram_product;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 用id查询商品
             */
            ID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Miniprogram_product;
          msg?: string;
        },
        'Product.get_product_findproduct',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取商品列表
       *
       * **path:** /product/getProductList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   endOriginalPrice?: number
       *   endSalesCount?: number
       *   endSellingPrice?: number
       *   // 关键字
       *   keyword?: string
       *   order?: string
       *   origin?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   product_name?: string
       *   shelfLife?: string
       *   sort?: string
       *   startOriginalPrice?: number
       *   startSalesCount?: number
       *   startSellingPrice?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_product_getproductlist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            endOriginalPrice?: number;
            endSalesCount?: number;
            endSellingPrice?: number;
            /**
             * 关键字
             */
            keyword?: string;
            order?: string;
            origin?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            product_name?: string;
            shelfLife?: string;
            sort?: string;
            startOriginalPrice?: number;
            startSalesCount?: number;
            startSellingPrice?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'Product.get_product_getproductlist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的商品接口
       *
       * **path:** /product/getProductPublic
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_product_getproductpublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'Product.get_product_getproductpublic',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新商品
       *
       * **path:** /product/updateProduct
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 商品图片
       *   image?: string
       *   // 净含量
       *   net_weight: string
       *   // 产地
       *   origin: string
       *   // 原价
       *   original_price: number
       *   // 商品分类
       *   product_category?: string
       *   // 商品详情
       *   product_details?: string
       *   // 商品名称
       *   product_name: string
       *   // 销售数
       *   sales_count?: number
       *   // 售价
       *   selling_price: number
       *   // 保质期
       *   shelfLife: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_product_updateproduct<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_product;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Product.put_product_updateproduct',
        Config
      >;
    };
    Reservations: {
      /**
       * ---
       *
       * [POST] 创建reservations表
       *
       * **path:** /reservations/createReservations
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   // 预约日期（yyyy-mm-dd）
       *   date?: string
       *   deletedBy?: number
       *   // 备注
       *   note?: string
       *   product?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 所属楼层 ID
       *     floorId?: number
       *     // 房间名称
       *     name?: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *   }
       *   // 房间 ID
       *   roomId?: number
       *   // 预约状态
       *   status?: string
       *   // 时段 ID
       *   timeSlotId?: number
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 预约者用户 ID（WeChatUsers.id）
       *   userId?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_reservations_createreservations<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_reservations;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Reservations.post_reservations_createreservations',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除reservations表
       *
       * **path:** /reservations/deleteReservations
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   // 预约日期（yyyy-mm-dd）
       *   date?: string
       *   deletedBy?: number
       *   // 备注
       *   note?: string
       *   product?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 所属楼层 ID
       *     floorId?: number
       *     // 房间名称
       *     name?: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *   }
       *   // 房间 ID
       *   roomId?: number
       *   // 预约状态
       *   status?: string
       *   // 时段 ID
       *   timeSlotId?: number
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 预约者用户 ID（WeChatUsers.id）
       *   userId?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_reservations_deletereservations<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_reservations;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Reservations.delete_reservations_deletereservations',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除reservations表
       *
       * **path:** /reservations/deleteReservationsByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_reservations_deletereservationsbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Reservations.delete_reservations_deletereservationsbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询reservations表
       *
       * **path:** /reservations/findReservations
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 用id查询reservations表
       *   ID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     // 预约日期（yyyy-mm-dd）
       *     date?: string
       *     deletedBy?: number
       *     // 备注
       *     note?: string
       *     product?: {
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       createdBy?: number
       *       deletedBy?: number
       *       // 所属楼层 ID
       *       floorId?: number
       *       // 房间名称
       *       name?: string
       *       // 更新时间
       *       updatedAt?: string
       *       updatedBy?: number
       *     }
       *     // 房间 ID
       *     roomId?: number
       *     // 预约状态
       *     status?: string
       *     // 时段 ID
       *     timeSlotId?: number
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *     // 预约者用户 ID（WeChatUsers.id）
       *     userId?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_reservations_findreservations<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Miniprogram_reservations;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 用id查询reservations表
             */
            ID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Miniprogram_reservations;
          msg?: string;
        },
        'Reservations.get_reservations_findreservations',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取reservations表列表
       *
       * **path:** /reservations/getReservationsList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   // [items] start
       *   // [items] end
       *   dateRange?: string[]
       *   // 关键字
       *   keyword?: string
       *   order?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   sort?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_reservations_getreservationslist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            dateRange?: string[];
            /**
             * 关键字
             */
            keyword?: string;
            order?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            sort?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'Reservations.get_reservations_getreservationslist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的reservations表接口
       *
       * **path:** /reservations/getReservationsPublic
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_reservations_getreservationspublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'Reservations.get_reservations_getreservationspublic',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新reservations表
       *
       * **path:** /reservations/updateReservations
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   // 预约日期（yyyy-mm-dd）
       *   date?: string
       *   deletedBy?: number
       *   // 备注
       *   note?: string
       *   product?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 所属楼层 ID
       *     floorId?: number
       *     // 房间名称
       *     name?: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *   }
       *   // 房间 ID
       *   roomId?: number
       *   // 预约状态
       *   status?: string
       *   // 时段 ID
       *   timeSlotId?: number
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 预约者用户 ID（WeChatUsers.id）
       *   userId?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_reservations_updatereservations<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_reservations;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Reservations.put_reservations_updatereservations',
        Config
      >;
    };
    Rooms: {
      /**
       * ---
       *
       * [POST] 创建rooms表
       *
       * **path:** /rooms/createRooms
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 所属楼层 ID
       *   floorId?: number
       *   // 房间名称
       *   name?: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_rooms_createrooms<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_rooms;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Rooms.post_rooms_createrooms',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除rooms表
       *
       * **path:** /rooms/deleteRooms
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 所属楼层 ID
       *   floorId?: number
       *   // 房间名称
       *   name?: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_rooms_deleterooms<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_rooms;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Rooms.delete_rooms_deleterooms',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除rooms表
       *
       * **path:** /rooms/deleteRoomsByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_rooms_deleteroomsbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Rooms.delete_rooms_deleteroomsbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询rooms表
       *
       * **path:** /rooms/findRooms
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 用id查询rooms表
       *   ID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 所属楼层 ID
       *     floorId?: number
       *     // 房间名称
       *     name?: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_rooms_findrooms<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Miniprogram_rooms;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 用id查询rooms表
             */
            ID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Miniprogram_rooms;
          msg?: string;
        },
        'Rooms.get_rooms_findrooms',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取rooms表列表
       *
       * **path:** /rooms/getRoomsList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   // 所属楼层 ID
       *   floorId?: number
       *   // 关键字
       *   keyword?: string
       *   name?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_rooms_getroomslist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            /**
             * 所属楼层 ID
             */
            floorId?: number;
            /**
             * 关键字
             */
            keyword?: string;
            name?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'Rooms.get_rooms_getroomslist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的rooms表接口
       *
       * **path:** /rooms/getRoomsPublic
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_rooms_getroomspublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'Rooms.get_rooms_getroomspublic',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新rooms表
       *
       * **path:** /rooms/updateRooms
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 所属楼层 ID
       *   floorId?: number
       *   // 房间名称
       *   name?: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_rooms_updaterooms<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_rooms;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'Rooms.put_rooms_updaterooms',
        Config
      >;
    };
    SysDictionary: {
      /**
       * ---
       *
       * [POST] 创建SysDictionary
       *
       * **path:** /sysDictionary/createSysDictionary
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 子字典
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysdictionary[]
       *   // 创建时间
       *   createdAt?: string
       *   // 描述
       *   desc?: string
       *   // 字典名（中）
       *   name?: string
       *   // 父级字典ID
       *   parentID?: number
       *   // 状态
       *   status?: boolean
       *   // [items] start
       *   // [items] end
       *   sysDictionaryDetails?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 子字典详情
       *     // [items] start
       *     // [cycle] $.sysDictionaryDetails.[]
       *     // [items] end
       *     children?: System_sysdictionarydetail[]
       *     // 创建时间
       *     createdAt?: string
       *     // 禁用状态，根据status字段动态计算
       *     disabled?: boolean
       *     // 扩展值
       *     extend?: string
       *     // 展示值
       *     label?: string
       *     // 层级深度，从0开始
       *     level?: number
       *     // 父级字典详情ID
       *     parentID?: number
       *     // 层级路径，如 "1,2,3"
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 启用状态
       *     status?: boolean
       *     // 关联标记
       *     sysDictionaryID?: number
       *     // 更新时间
       *     updatedAt?: string
       *     // 字典值
       *     value?: string
       *   }>
       *   // 字典名（英）
       *   type?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_sysdictionary_createsysdictionary<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysdictionary;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysDictionary.post_sysdictionary_createsysdictionary',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除SysDictionary
       *
       * **path:** /sysDictionary/deleteSysDictionary
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 子字典
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysdictionary[]
       *   // 创建时间
       *   createdAt?: string
       *   // 描述
       *   desc?: string
       *   // 字典名（中）
       *   name?: string
       *   // 父级字典ID
       *   parentID?: number
       *   // 状态
       *   status?: boolean
       *   // [items] start
       *   // [items] end
       *   sysDictionaryDetails?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 子字典详情
       *     // [items] start
       *     // [cycle] $.sysDictionaryDetails.[]
       *     // [items] end
       *     children?: System_sysdictionarydetail[]
       *     // 创建时间
       *     createdAt?: string
       *     // 禁用状态，根据status字段动态计算
       *     disabled?: boolean
       *     // 扩展值
       *     extend?: string
       *     // 展示值
       *     label?: string
       *     // 层级深度，从0开始
       *     level?: number
       *     // 父级字典详情ID
       *     parentID?: number
       *     // 层级路径，如 "1,2,3"
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 启用状态
       *     status?: boolean
       *     // 关联标记
       *     sysDictionaryID?: number
       *     // 更新时间
       *     updatedAt?: string
       *     // 字典值
       *     value?: string
       *   }>
       *   // 字典名（英）
       *   type?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_sysdictionary_deletesysdictionary<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysdictionary;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysDictionary.delete_sysdictionary_deletesysdictionary',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询SysDictionary
       *
       * **path:** /sysDictionary/findSysDictionary
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 描述
       *   desc?: string
       *   // 字典名（中）
       *   name?: string
       *   // 父级字典ID
       *   parentID?: number
       *   // 状态
       *   status?: boolean
       *   // 字典名（英）
       *   type?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      get_sysdictionary_findsysdictionary<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 主键ID
             */
            ID?: number;
            /**
             * 创建时间
             */
            createdAt?: string;
            /**
             * 描述
             */
            desc?: string;
            /**
             * 字典名（中）
             */
            name?: string;
            /**
             * 父级字典ID
             */
            parentID?: number;
            /**
             * 状态
             */
            status?: boolean;
            /**
             * 字典名（英）
             */
            type?: string;
            /**
             * 更新时间
             */
            updatedAt?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'SysDictionary.get_sysdictionary_findsysdictionary',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取SysDictionary列表
       *
       * **path:** /sysDictionary/getSysDictionaryList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 字典名（中）
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_sysdictionary_getsysdictionarylist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 字典名（中）
             */
            name?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'SysDictionary.get_sysdictionary_getsysdictionarylist',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新SysDictionary
       *
       * **path:** /sysDictionary/updateSysDictionary
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 子字典
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysdictionary[]
       *   // 创建时间
       *   createdAt?: string
       *   // 描述
       *   desc?: string
       *   // 字典名（中）
       *   name?: string
       *   // 父级字典ID
       *   parentID?: number
       *   // 状态
       *   status?: boolean
       *   // [items] start
       *   // [items] end
       *   sysDictionaryDetails?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 子字典详情
       *     // [items] start
       *     // [cycle] $.sysDictionaryDetails.[]
       *     // [items] end
       *     children?: System_sysdictionarydetail[]
       *     // 创建时间
       *     createdAt?: string
       *     // 禁用状态，根据status字段动态计算
       *     disabled?: boolean
       *     // 扩展值
       *     extend?: string
       *     // 展示值
       *     label?: string
       *     // 层级深度，从0开始
       *     level?: number
       *     // 父级字典详情ID
       *     parentID?: number
       *     // 层级路径，如 "1,2,3"
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 启用状态
       *     status?: boolean
       *     // 关联标记
       *     sysDictionaryID?: number
       *     // 更新时间
       *     updatedAt?: string
       *     // 字典值
       *     value?: string
       *   }>
       *   // 字典名（英）
       *   type?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_sysdictionary_updatesysdictionary<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysdictionary;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysDictionary.put_sysdictionary_updatesysdictionary',
        Config
      >;
    };
    SysDictionaryDetail: {
      /**
       * ---
       *
       * [POST] 创建SysDictionaryDetail
       *
       * **path:** /sysDictionaryDetail/createSysDictionaryDetail
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 子字典详情
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysdictionarydetail[]
       *   // 创建时间
       *   createdAt?: string
       *   // 禁用状态，根据status字段动态计算
       *   disabled?: boolean
       *   // 扩展值
       *   extend?: string
       *   // 展示值
       *   label?: string
       *   // 层级深度，从0开始
       *   level?: number
       *   // 父级字典详情ID
       *   parentID?: number
       *   // 层级路径，如 "1,2,3"
       *   path?: string
       *   // 排序标记
       *   sort?: number
       *   // 启用状态
       *   status?: boolean
       *   // 关联标记
       *   sysDictionaryID?: number
       *   // 更新时间
       *   updatedAt?: string
       *   // 字典值
       *   value?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_sysdictionarydetail_createsysdictionarydetail<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysdictionarydetail;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysDictionaryDetail.post_sysdictionarydetail_createsysdictionarydetail',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除SysDictionaryDetail
       *
       * **path:** /sysDictionaryDetail/deleteSysDictionaryDetail
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 子字典详情
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysdictionarydetail[]
       *   // 创建时间
       *   createdAt?: string
       *   // 禁用状态，根据status字段动态计算
       *   disabled?: boolean
       *   // 扩展值
       *   extend?: string
       *   // 展示值
       *   label?: string
       *   // 层级深度，从0开始
       *   level?: number
       *   // 父级字典详情ID
       *   parentID?: number
       *   // 层级路径，如 "1,2,3"
       *   path?: string
       *   // 排序标记
       *   sort?: number
       *   // 启用状态
       *   status?: boolean
       *   // 关联标记
       *   sysDictionaryID?: number
       *   // 更新时间
       *   updatedAt?: string
       *   // 字典值
       *   value?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_sysdictionarydetail_deletesysdictionarydetail<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysdictionarydetail;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysDictionaryDetail.delete_sysdictionarydetail_deletesysdictionarydetail',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询SysDictionaryDetail
       *
       * **path:** /sysDictionaryDetail/findSysDictionaryDetail
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 禁用状态，根据status字段动态计算
       *   disabled?: boolean
       *   // 扩展值
       *   extend?: string
       *   // 展示值
       *   label?: string
       *   // 层级深度，从0开始
       *   level?: number
       *   // 父级字典详情ID
       *   parentID?: number
       *   // 层级路径，如 "1,2,3"
       *   path?: string
       *   // 排序标记
       *   sort?: number
       *   // 启用状态
       *   status?: boolean
       *   // 关联标记
       *   sysDictionaryID?: number
       *   // 更新时间
       *   updatedAt?: string
       *   // 字典值
       *   value?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      get_sysdictionarydetail_findsysdictionarydetail<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 主键ID
             */
            ID?: number;
            /**
             * 创建时间
             */
            createdAt?: string;
            /**
             * 禁用状态，根据status字段动态计算
             */
            disabled?: boolean;
            /**
             * 扩展值
             */
            extend?: string;
            /**
             * 展示值
             */
            label?: string;
            /**
             * 层级深度，从0开始
             */
            level?: number;
            /**
             * 父级字典详情ID
             */
            parentID?: number;
            /**
             * 层级路径，如 "1,2,3"
             */
            path?: string;
            /**
             * 排序标记
             */
            sort?: number;
            /**
             * 启用状态
             */
            status?: boolean;
            /**
             * 关联标记
             */
            sysDictionaryID?: number;
            /**
             * 更新时间
             */
            updatedAt?: string;
            /**
             * 字典值
             */
            value?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'SysDictionaryDetail.get_sysdictionarydetail_findsysdictionarydetail',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据父级ID获取字典详情
       *
       * **path:** /sysDictionaryDetail/getDictionaryDetailsByParent
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 是否包含子级数据
       *   includeChildren?: boolean
       *   // 父级字典详情ID，为空时获取顶级
       *   parentID?: number
       *   // 字典ID
       *   sysDictionaryID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 子字典详情
       *     // [items] start
       *     // [cycle] $.data.[]
       *     // [items] end
       *     children?: System_sysdictionarydetail[]
       *     // 创建时间
       *     createdAt?: string
       *     // 禁用状态，根据status字段动态计算
       *     disabled?: boolean
       *     // 扩展值
       *     extend?: string
       *     // 展示值
       *     label?: string
       *     // 层级深度，从0开始
       *     level?: number
       *     // 父级字典详情ID
       *     parentID?: number
       *     // 层级路径，如 "1,2,3"
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 启用状态
       *     status?: boolean
       *     // 关联标记
       *     sysDictionaryID?: number
       *     // 更新时间
       *     updatedAt?: string
       *     // 字典值
       *     value?: string
       *   }>
       *   msg?: string
       * }
       * ```
       */
      get_sysdictionarydetail_getdictionarydetailsbyparent<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: System_sysdictionarydetail[];
            msg?: string;
          }
        > & {
          params: {
            /**
             * 是否包含子级数据
             */
            includeChildren?: boolean;
            /**
             * 父级字典详情ID，为空时获取顶级
             */
            parentID?: number;
            /**
             * 字典ID
             */
            sysDictionaryID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: System_sysdictionarydetail[];
          msg?: string;
        },
        'SysDictionaryDetail.get_sysdictionarydetail_getdictionarydetailsbyparent',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取字典详情的完整路径
       *
       * **path:** /sysDictionaryDetail/getDictionaryPath
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 字典详情ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 子字典详情
       *     // [items] start
       *     // [cycle] $.data.[]
       *     // [items] end
       *     children?: System_sysdictionarydetail[]
       *     // 创建时间
       *     createdAt?: string
       *     // 禁用状态，根据status字段动态计算
       *     disabled?: boolean
       *     // 扩展值
       *     extend?: string
       *     // 展示值
       *     label?: string
       *     // 层级深度，从0开始
       *     level?: number
       *     // 父级字典详情ID
       *     parentID?: number
       *     // 层级路径，如 "1,2,3"
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 启用状态
       *     status?: boolean
       *     // 关联标记
       *     sysDictionaryID?: number
       *     // 更新时间
       *     updatedAt?: string
       *     // 字典值
       *     value?: string
       *   }>
       *   msg?: string
       * }
       * ```
       */
      get_sysdictionarydetail_getdictionarypath<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: System_sysdictionarydetail[];
            msg?: string;
          }
        > & {
          params: {
            /**
             * 字典详情ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: System_sysdictionarydetail[];
          msg?: string;
        },
        'SysDictionaryDetail.get_sysdictionarydetail_getdictionarypath',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取字典详情树形结构
       *
       * **path:** /sysDictionaryDetail/getDictionaryTreeList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 字典ID
       *   sysDictionaryID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 子字典详情
       *     // [items] start
       *     // [cycle] $.data.[]
       *     // [items] end
       *     children?: System_sysdictionarydetail[]
       *     // 创建时间
       *     createdAt?: string
       *     // 禁用状态，根据status字段动态计算
       *     disabled?: boolean
       *     // 扩展值
       *     extend?: string
       *     // 展示值
       *     label?: string
       *     // 层级深度，从0开始
       *     level?: number
       *     // 父级字典详情ID
       *     parentID?: number
       *     // 层级路径，如 "1,2,3"
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 启用状态
       *     status?: boolean
       *     // 关联标记
       *     sysDictionaryID?: number
       *     // 更新时间
       *     updatedAt?: string
       *     // 字典值
       *     value?: string
       *   }>
       *   msg?: string
       * }
       * ```
       */
      get_sysdictionarydetail_getdictionarytreelist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: System_sysdictionarydetail[];
            msg?: string;
          }
        > & {
          params: {
            /**
             * 字典ID
             */
            sysDictionaryID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: System_sysdictionarydetail[];
          msg?: string;
        },
        'SysDictionaryDetail.get_sysdictionarydetail_getdictionarytreelist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据字典类型获取字典详情树形结构
       *
       * **path:** /sysDictionaryDetail/getDictionaryTreeListByType
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 字典类型
       *   type: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 子字典详情
       *     // [items] start
       *     // [cycle] $.data.[]
       *     // [items] end
       *     children?: System_sysdictionarydetail[]
       *     // 创建时间
       *     createdAt?: string
       *     // 禁用状态，根据status字段动态计算
       *     disabled?: boolean
       *     // 扩展值
       *     extend?: string
       *     // 展示值
       *     label?: string
       *     // 层级深度，从0开始
       *     level?: number
       *     // 父级字典详情ID
       *     parentID?: number
       *     // 层级路径，如 "1,2,3"
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 启用状态
       *     status?: boolean
       *     // 关联标记
       *     sysDictionaryID?: number
       *     // 更新时间
       *     updatedAt?: string
       *     // 字典值
       *     value?: string
       *   }>
       *   msg?: string
       * }
       * ```
       */
      get_sysdictionarydetail_getdictionarytreelistbytype<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: System_sysdictionarydetail[];
            msg?: string;
          }
        > & {
          params: {
            /**
             * 字典类型
             */
            type: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: System_sysdictionarydetail[];
          msg?: string;
        },
        'SysDictionaryDetail.get_sysdictionarydetail_getdictionarytreelistbytype',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取SysDictionaryDetail列表
       *
       * **path:** /sysDictionaryDetail/getSysDictionaryDetailList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 禁用状态，根据status字段动态计算
       *   disabled?: boolean
       *   // 扩展值
       *   extend?: string
       *   // 关键字
       *   keyword?: string
       *   // 展示值
       *   label?: string
       *   // 层级深度，用于查询指定层级的数据
       *   level?: number
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   // 父级字典详情ID，用于查询指定父级下的子项
       *   parentID?: number
       *   // 层级路径，如 "1,2,3"
       *   path?: string
       *   // 排序标记
       *   sort?: number
       *   // 启用状态
       *   status?: boolean
       *   // 关联标记
       *   sysDictionaryID?: number
       *   // 更新时间
       *   updatedAt?: string
       *   // 字典值
       *   value?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_sysdictionarydetail_getsysdictionarydetaillist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 主键ID
             */
            ID?: number;
            /**
             * 创建时间
             */
            createdAt?: string;
            /**
             * 禁用状态，根据status字段动态计算
             */
            disabled?: boolean;
            /**
             * 扩展值
             */
            extend?: string;
            /**
             * 关键字
             */
            keyword?: string;
            /**
             * 展示值
             */
            label?: string;
            /**
             * 层级深度，用于查询指定层级的数据
             */
            level?: number;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            /**
             * 父级字典详情ID，用于查询指定父级下的子项
             */
            parentID?: number;
            /**
             * 层级路径，如 "1,2,3"
             */
            path?: string;
            /**
             * 排序标记
             */
            sort?: number;
            /**
             * 启用状态
             */
            status?: boolean;
            /**
             * 关联标记
             */
            sysDictionaryID?: number;
            /**
             * 更新时间
             */
            updatedAt?: string;
            /**
             * 字典值
             */
            value?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'SysDictionaryDetail.get_sysdictionarydetail_getsysdictionarydetaillist',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新SysDictionaryDetail
       *
       * **path:** /sysDictionaryDetail/updateSysDictionaryDetail
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 子字典详情
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   children?: System_sysdictionarydetail[]
       *   // 创建时间
       *   createdAt?: string
       *   // 禁用状态，根据status字段动态计算
       *   disabled?: boolean
       *   // 扩展值
       *   extend?: string
       *   // 展示值
       *   label?: string
       *   // 层级深度，从0开始
       *   level?: number
       *   // 父级字典详情ID
       *   parentID?: number
       *   // 层级路径，如 "1,2,3"
       *   path?: string
       *   // 排序标记
       *   sort?: number
       *   // 启用状态
       *   status?: boolean
       *   // 关联标记
       *   sysDictionaryID?: number
       *   // 更新时间
       *   updatedAt?: string
       *   // 字典值
       *   value?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_sysdictionarydetail_updatesysdictionarydetail<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysdictionarydetail;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysDictionaryDetail.put_sysdictionarydetail_updatesysdictionarydetail',
        Config
      >;
    };
    SysExportTemplate: {
      /**
       * ---
       *
       * [POST] 创建导出模板
       *
       * **path:** /sysExportTemplate/createSysExportTemplate
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // [items] start
       *   // [items] end
       *   conditions?: Array<{
       *     // 主键ID
       *     ID?: number
       *     column?: string
       *     // 创建时间
       *     createdAt?: string
       *     from?: string
       *     operator?: string
       *     templateID?: string
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 创建时间
       *   createdAt?: string
       *   // 数据库名称
       *   dbName?: string
       *   // [items] start
       *   // [items] end
       *   joinTemplate?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     joins?: string
       *     on?: string
       *     table?: string
       *     templateID?: string
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   limit?: number
       *   // 模板名称
       *   name?: string
       *   order?: string
       *   // 表名称
       *   tableName?: string
       *   // 模板标识
       *   templateID?: string
       *   // 模板信息
       *   templateInfo?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      post_sysexporttemplate_createsysexporttemplate<
        Config extends Alova2MethodConfig<string> & {
          data: System_sysexporttemplate;
        }
      >(
        config: Config
      ): Alova2Method<string, 'SysExportTemplate.post_sysexporttemplate_createsysexporttemplate', Config>;
      /**
       * ---
       *
       * [DELETE] 删除导出模板
       *
       * **path:** /sysExportTemplate/deleteSysExportTemplate
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // [items] start
       *   // [items] end
       *   conditions?: Array<{
       *     // 主键ID
       *     ID?: number
       *     column?: string
       *     // 创建时间
       *     createdAt?: string
       *     from?: string
       *     operator?: string
       *     templateID?: string
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 创建时间
       *   createdAt?: string
       *   // 数据库名称
       *   dbName?: string
       *   // [items] start
       *   // [items] end
       *   joinTemplate?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     joins?: string
       *     on?: string
       *     table?: string
       *     templateID?: string
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   limit?: number
       *   // 模板名称
       *   name?: string
       *   order?: string
       *   // 表名称
       *   tableName?: string
       *   // 模板标识
       *   templateID?: string
       *   // 模板信息
       *   templateInfo?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      delete_sysexporttemplate_deletesysexporttemplate<
        Config extends Alova2MethodConfig<string> & {
          data: System_sysexporttemplate;
        }
      >(
        config: Config
      ): Alova2Method<string, 'SysExportTemplate.delete_sysexporttemplate_deletesysexporttemplate', Config>;
      /**
       * ---
       *
       * [DELETE] 批量删除导出模板
       *
       * **path:** /sysExportTemplate/deleteSysExportTemplateByIds
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // [items] start
       *   // [items] end
       *   ids?: number[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      delete_sysexporttemplate_deletesysexporttemplatebyids<
        Config extends Alova2MethodConfig<string> & {
          data: Request_idsreq;
        }
      >(
        config: Config
      ): Alova2Method<string, 'SysExportTemplate.delete_sysexporttemplate_deletesysexporttemplatebyids', Config>;
      /**
       * ---
       *
       * [GET] 导出表格
       *
       * **path:** /sysExportTemplate/exportExcel
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      get_sysexporttemplate_exportexcel<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'SysExportTemplate.get_sysexporttemplate_exportexcel', Config>;
      /**
       * ---
       *
       * [GET] 导出表格模板
       *
       * **path:** /sysExportTemplate/exportTemplate
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      get_sysexporttemplate_exporttemplate<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'SysExportTemplate.get_sysexporttemplate_exporttemplate', Config>;
      /**
       * ---
       *
       * [GET] 用id查询导出模板
       *
       * **path:** /sysExportTemplate/findSysExportTemplate
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 数据库名称
       *   dbName?: string
       *   limit?: number
       *   // 模板名称
       *   name?: string
       *   order?: string
       *   // 表名称
       *   tableName?: string
       *   // 模板标识
       *   templateID?: string
       *   // 模板信息
       *   templateInfo?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      get_sysexporttemplate_findsysexporttemplate<
        Config extends Alova2MethodConfig<string> & {
          params: {
            /**
             * 主键ID
             */
            ID?: number;
            /**
             * 创建时间
             */
            createdAt?: string;
            /**
             * 数据库名称
             */
            dbName?: string;
            limit?: number;
            /**
             * 模板名称
             */
            name?: string;
            order?: string;
            /**
             * 表名称
             */
            tableName?: string;
            /**
             * 模板标识
             */
            templateID?: string;
            /**
             * 模板信息
             */
            templateInfo?: string;
            /**
             * 更新时间
             */
            updatedAt?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<string, 'SysExportTemplate.get_sysexporttemplate_findsysexporttemplate', Config>;
      /**
       * ---
       *
       * [GET] 分页获取导出模板列表
       *
       * **path:** /sysExportTemplate/getSysExportTemplateList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 数据库名称
       *   dbName?: string
       *   endCreatedAt?: string
       *   // 关键字
       *   keyword?: string
       *   limit?: number
       *   // 模板名称
       *   name?: string
       *   order?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   startCreatedAt?: string
       *   // 表名称
       *   tableName?: string
       *   // 模板标识
       *   templateID?: string
       *   // 模板信息
       *   templateInfo?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      get_sysexporttemplate_getsysexporttemplatelist<
        Config extends Alova2MethodConfig<string> & {
          params: {
            /**
             * 主键ID
             */
            ID?: number;
            /**
             * 创建时间
             */
            createdAt?: string;
            /**
             * 数据库名称
             */
            dbName?: string;
            endCreatedAt?: string;
            /**
             * 关键字
             */
            keyword?: string;
            limit?: number;
            /**
             * 模板名称
             */
            name?: string;
            order?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            startCreatedAt?: string;
            /**
             * 表名称
             */
            tableName?: string;
            /**
             * 模板标识
             */
            templateID?: string;
            /**
             * 模板信息
             */
            templateInfo?: string;
            /**
             * 更新时间
             */
            updatedAt?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<string, 'SysExportTemplate.get_sysexporttemplate_getsysexporttemplatelist', Config>;
      /**
       * ---
       *
       * [PUT] 更新导出模板
       *
       * **path:** /sysExportTemplate/updateSysExportTemplate
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // [items] start
       *   // [items] end
       *   conditions?: Array<{
       *     // 主键ID
       *     ID?: number
       *     column?: string
       *     // 创建时间
       *     createdAt?: string
       *     from?: string
       *     operator?: string
       *     templateID?: string
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 创建时间
       *   createdAt?: string
       *   // 数据库名称
       *   dbName?: string
       *   // [items] start
       *   // [items] end
       *   joinTemplate?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     joins?: string
       *     on?: string
       *     table?: string
       *     templateID?: string
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   limit?: number
       *   // 模板名称
       *   name?: string
       *   order?: string
       *   // 表名称
       *   tableName?: string
       *   // 模板标识
       *   templateID?: string
       *   // 模板信息
       *   templateInfo?: string
       *   // 更新时间
       *   updatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      put_sysexporttemplate_updatesysexporttemplate<
        Config extends Alova2MethodConfig<string> & {
          data: System_sysexporttemplate;
        }
      >(
        config: Config
      ): Alova2Method<string, 'SysExportTemplate.put_sysexporttemplate_updatesysexporttemplate', Config>;
    };
    ExportExcelByToken: {
      /**
       * ---
       *
       * [GET] 导出表格
       *
       * **path:** /sysExportTemplate/exportExcelByToken
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      get_sysexporttemplate_exportexcelbytoken<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'ExportExcelByToken.get_sysexporttemplate_exportexcelbytoken', Config>;
    };
    ExportTemplateByToken: {
      /**
       * ---
       *
       * [GET] 通过token导出表格模板
       *
       * **path:** /sysExportTemplate/exportTemplateByToken
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      get_sysexporttemplate_exporttemplatebytoken<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'ExportTemplateByToken.get_sysexporttemplate_exporttemplatebytoken', Config>;
    };
    SysImportTemplate: {
      /**
       * ---
       *
       * [POST] 导入表格
       *
       * **path:** /sysExportTemplate/importExcel
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      post_sysexporttemplate_importexcel<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'SysImportTemplate.post_sysexporttemplate_importexcel', Config>;
    };
    SysOperationRecord: {
      /**
       * ---
       *
       * [DELETE] 删除SysOperationRecord
       *
       * **path:** /sysOperationRecord/deleteSysOperationRecord
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 代理
       *   agent?: string
       *   // 请求Body
       *   body?: string
       *   // 创建时间
       *   createdAt?: string
       *   // 错误信息
       *   error_message?: string
       *   // 请求ip
       *   ip?: string
       *   // 延迟
       *   latency?: string
       *   // 请求方法
       *   method?: string
       *   // 请求路径
       *   path?: string
       *   // 响应Body
       *   resp?: string
       *   // 请求状态
       *   status?: number
       *   // 更新时间
       *   updatedAt?: string
       *   user?: {
       *     // 主键ID
       *     ID?: number
       *     // 多用户角色
       *     // [items] start
       *     // [items] end
       *     authorities?: Array<{
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.user.authorities.[]
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.user.authorities.[]
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.user.authorities.[]
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.user.authorities.[].menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 用户角色
       *     authority?: {
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.user.authority
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.user.authority
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [items] end
       *       menus?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // [items] start
       *         // [cycle] $.user.authority
       *         // [items] end
       *         authoritys?: System_sysauthority[]
       *         // [items] start
       *         // [cycle] $.user.authority.menus.[]
       *         // [items] end
       *         children?: System_sysbasemenu[]
       *         // 对应前端文件路径
       *         component?: string
       *         // 创建时间
       *         createdAt?: string
       *         // 是否在列表隐藏
       *         hidden?: boolean
       *         // [items] start
       *         // [items] end
       *         menuBtn?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           desc?: string
       *           name?: string
       *           sysBaseMenuID?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 附加属性
       *         meta?: {
       *           activeName?: string
       *           // 自动关闭tab
       *           closeTab?: boolean
       *           // 是否是基础路由（开发中）
       *           defaultMenu?: boolean
       *           // 菜单图标
       *           icon?: string
       *           // 是否缓存
       *           keepAlive?: boolean
       *           // 菜单名
       *           title?: string
       *           // 路由切换动画
       *           transitionType?: string
       *         }
       *         // 路由name
       *         name?: string
       *         // [items] start
       *         // [items] end
       *         parameters?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // 创建时间
       *           createdAt?: string
       *           // 地址栏携带参数的key
       *           key?: string
       *           sysBaseMenuID?: number
       *           // 地址栏携带参数为params还是query
       *           type?: string
       *           // 更新时间
       *           updatedAt?: string
       *           // 地址栏携带参数的值
       *           value?: string
       *         }>
       *         // 父菜单ID
       *         parentId?: number
       *         // 路由path
       *         path?: string
       *         // 排序标记
       *         sort?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }
       *     // 用户角色ID
       *     authorityId?: number
       *     // 创建时间
       *     createdAt?: string
       *     // 用户邮箱
       *     email?: string
       *     // 用户是否被冻结 1正常 2冻结
       *     enable?: number
       *     // 用户头像
       *     headerImg?: string
       *     // 用户昵称
       *     nickName?: string
       *     // 配置
       *     originSetting?: Record<string, any>
       *     // 用户手机号
       *     phone?: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 用户登录名
       *     userName?: string
       *     // 用户UUID
       *     uuid?: string
       *   }
       *   // 用户id
       *   user_id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_sysoperationrecord_deletesysoperationrecord<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysoperationrecord;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysOperationRecord.delete_sysoperationrecord_deletesysoperationrecord',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除SysOperationRecord
       *
       * **path:** /sysOperationRecord/deleteSysOperationRecordByIds
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // [items] start
       *   // [items] end
       *   ids?: number[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_sysoperationrecord_deletesysoperationrecordbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_idsreq;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysOperationRecord.delete_sysoperationrecord_deletesysoperationrecordbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询SysOperationRecord
       *
       * **path:** /sysOperationRecord/findSysOperationRecord
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 主键ID
       *   ID?: number
       *   // 代理
       *   agent?: string
       *   // 请求Body
       *   body?: string
       *   // 创建时间
       *   createdAt?: string
       *   // 错误信息
       *   error_message?: string
       *   // 请求ip
       *   ip?: string
       *   // 延迟
       *   latency?: string
       *   // 请求方法
       *   method?: string
       *   // 请求路径
       *   path?: string
       *   // 响应Body
       *   resp?: string
       *   // 请求状态
       *   status?: number
       *   // 更新时间
       *   updatedAt?: string
       *   // 用户id
       *   user_id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      get_sysoperationrecord_findsysoperationrecord<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 主键ID
             */
            ID?: number;
            /**
             * 代理
             */
            agent?: string;
            /**
             * 请求Body
             */
            body?: string;
            /**
             * 创建时间
             */
            createdAt?: string;
            /**
             * 错误信息
             */
            error_message?: string;
            /**
             * 请求ip
             */
            ip?: string;
            /**
             * 延迟
             */
            latency?: string;
            /**
             * 请求方法
             */
            method?: string;
            /**
             * 请求路径
             */
            path?: string;
            /**
             * 响应Body
             */
            resp?: string;
            /**
             * 请求状态
             */
            status?: number;
            /**
             * 更新时间
             */
            updatedAt?: string;
            /**
             * 用户id
             */
            user_id?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'SysOperationRecord.get_sysoperationrecord_findsysoperationrecord',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取SysOperationRecord列表
       *
       * **path:** /sysOperationRecord/getSysOperationRecordList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 主键ID
       *   ID?: number
       *   // 代理
       *   agent?: string
       *   // 请求Body
       *   body?: string
       *   // 创建时间
       *   createdAt?: string
       *   // 错误信息
       *   error_message?: string
       *   // 请求ip
       *   ip?: string
       *   // 关键字
       *   keyword?: string
       *   // 延迟
       *   latency?: string
       *   // 请求方法
       *   method?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   // 请求路径
       *   path?: string
       *   // 响应Body
       *   resp?: string
       *   // 请求状态
       *   status?: number
       *   // 更新时间
       *   updatedAt?: string
       *   // 用户id
       *   user_id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_sysoperationrecord_getsysoperationrecordlist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 主键ID
             */
            ID?: number;
            /**
             * 代理
             */
            agent?: string;
            /**
             * 请求Body
             */
            body?: string;
            /**
             * 创建时间
             */
            createdAt?: string;
            /**
             * 错误信息
             */
            error_message?: string;
            /**
             * 请求ip
             */
            ip?: string;
            /**
             * 关键字
             */
            keyword?: string;
            /**
             * 延迟
             */
            latency?: string;
            /**
             * 请求方法
             */
            method?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            /**
             * 请求路径
             */
            path?: string;
            /**
             * 响应Body
             */
            resp?: string;
            /**
             * 请求状态
             */
            status?: number;
            /**
             * 更新时间
             */
            updatedAt?: string;
            /**
             * 用户id
             */
            user_id?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'SysOperationRecord.get_sysoperationrecord_getsysoperationrecordlist',
        Config
      >;
    };
    SysParams: {
      /**
       * ---
       *
       * [POST] 创建参数
       *
       * **path:** /sysParams/createSysParams
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 参数说明
       *   desc?: string
       *   // 参数键
       *   key: string
       *   // 参数名称
       *   name: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 参数值
       *   value: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_sysparams_createsysparams<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysparams;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysParams.post_sysparams_createsysparams',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除参数
       *
       * **path:** /sysParams/deleteSysParams
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 参数说明
       *   desc?: string
       *   // 参数键
       *   key: string
       *   // 参数名称
       *   name: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 参数值
       *   value: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_sysparams_deletesysparams<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysparams;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysParams.delete_sysparams_deletesysparams',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除参数
       *
       * **path:** /sysParams/deleteSysParamsByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_sysparams_deletesysparamsbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysParams.delete_sysparams_deletesysparamsbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询参数
       *
       * **path:** /sysParams/findSysParams
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 参数说明
       *   desc?: string
       *   // 参数键
       *   key: string
       *   // 参数名称
       *   name: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 参数值
       *   value: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     // 参数说明
       *     desc?: string
       *     // 参数键
       *     key: string
       *     // 参数名称
       *     name: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 参数值
       *     value: string
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_sysparams_findsysparams<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: System_sysparams;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 主键ID
             */
            ID?: number;
            /**
             * 创建时间
             */
            createdAt?: string;
            /**
             * 参数说明
             */
            desc?: string;
            /**
             * 参数键
             */
            key: string;
            /**
             * 参数名称
             */
            name: string;
            /**
             * 更新时间
             */
            updatedAt?: string;
            /**
             * 参数值
             */
            value: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: System_sysparams;
          msg?: string;
        },
        'SysParams.get_sysparams_findsysparams',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据key获取参数value
       *
       * **path:** /sysParams/getSysParam
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // key
       *   key: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     // 参数说明
       *     desc?: string
       *     // 参数键
       *     key: string
       *     // 参数名称
       *     name: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 参数值
       *     value: string
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_sysparams_getsysparam<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: System_sysparams;
            msg?: string;
          }
        > & {
          params: {
            /**
             * key
             */
            key: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: System_sysparams;
          msg?: string;
        },
        'SysParams.get_sysparams_getsysparam',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取参数列表
       *
       * **path:** /sysParams/getSysParamsList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   endCreatedAt?: string
       *   key?: string
       *   // 关键字
       *   keyword?: string
       *   name?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   startCreatedAt?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_sysparams_getsysparamslist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            endCreatedAt?: string;
            key?: string;
            /**
             * 关键字
             */
            keyword?: string;
            name?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            startCreatedAt?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'SysParams.get_sysparams_getsysparamslist',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新参数
       *
       * **path:** /sysParams/updateSysParams
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 参数说明
       *   desc?: string
       *   // 参数键
       *   key: string
       *   // 参数名称
       *   name: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 参数值
       *   value: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_sysparams_updatesysparams<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysparams;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysParams.put_sysparams_updatesysparams',
        Config
      >;
    };
    SysVersion: {
      /**
       * ---
       *
       * [DELETE] 删除版本管理
       *
       * **path:** /sysVersion/deleteSysVersion
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 版本描述
       *   description?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 版本号
       *   versionCode: string
       *   // 版本数据
       *   versionData?: string
       *   // 版本名称
       *   versionName: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_sysversion_deletesysversion<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysversion;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysVersion.delete_sysversion_deletesysversion',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除版本管理
       *
       * **path:** /sysVersion/deleteSysVersionByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_sysversion_deletesysversionbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysVersion.delete_sysversion_deletesysversionbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 下载版本JSON数据
       *
       * **path:** /sysVersion/downloadVersionJson
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 版本ID
       *   ID: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_sysversion_downloadversionjson<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 版本ID
             */
            ID: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'SysVersion.get_sysversion_downloadversionjson',
        Config
      >;
      /**
       * ---
       *
       * [POST] 创建发版数据
       *
       * **path:** /sysVersion/exportVersion
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 选中的API ID列表
       *   // [items] start
       *   // [items] end
       *   apiIds?: number[]
       *   // 版本描述
       *   description?: string
       *   // 选中的字典ID列表
       *   // [items] start
       *   // [items] end
       *   dictIds?: number[]
       *   // 选中的菜单ID列表
       *   // [items] start
       *   // [items] end
       *   menuIds?: number[]
       *   // 版本号
       *   versionCode: string
       *   // 版本名称
       *   versionName: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_sysversion_exportversion<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_exportversionrequest;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysVersion.post_sysversion_exportversion',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询版本管理
       *
       * **path:** /sysVersion/findSysVersion
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 用id查询版本管理
       *   ID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     // 版本描述
       *     description?: string
       *     // 更新时间
       *     updatedAt?: string
       *     // 版本号
       *     versionCode: string
       *     // 版本数据
       *     versionData?: string
       *     // 版本名称
       *     versionName: string
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_sysversion_findsysversion<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: System_sysversion;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 用id查询版本管理
             */
            ID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: System_sysversion;
          msg?: string;
        },
        'SysVersion.get_sysversion_findsysversion',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取版本管理列表
       *
       * **path:** /sysVersion/getSysVersionList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   // 关键字
       *   keyword?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   versionCode?: string
       *   versionName?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_sysversion_getsysversionlist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            /**
             * 关键字
             */
            keyword?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            versionCode?: string;
            versionName?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'SysVersion.get_sysversion_getsysversionlist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的版本管理接口
       *
       * **path:** /sysVersion/getSysVersionPublic
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_sysversion_getsysversionpublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'SysVersion.get_sysversion_getsysversionpublic',
        Config
      >;
      /**
       * ---
       *
       * [POST] 导入版本数据
       *
       * **path:** /sysVersion/importVersion
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // API数据，直接复用SysApi
       *   // [items] start
       *   // [items] end
       *   apis?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // api组
       *     apiGroup?: string
       *     // 创建时间
       *     createdAt?: string
       *     // api中文描述
       *     description?: string
       *     // 方法:创建POST(默认)|查看GET|更新PUT|删除DELETE
       *     method?: string
       *     // api路径
       *     path?: string
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 字典数据，直接复用SysDictionary
       *   // [items] start
       *   // [items] end
       *   dictionaries?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // 子字典
       *     // [items] start
       *     // [cycle] $.dictionaries.[]
       *     // [items] end
       *     children?: System_sysdictionary[]
       *     // 创建时间
       *     createdAt?: string
       *     // 描述
       *     desc?: string
       *     // 字典名（中）
       *     name?: string
       *     // 父级字典ID
       *     parentID?: number
       *     // 状态
       *     status?: boolean
       *     // [items] start
       *     // [items] end
       *     sysDictionaryDetails?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 子字典详情
       *       // [items] start
       *       // [cycle] $.dictionaries.[].sysDictionaryDetails.[]
       *       // [items] end
       *       children?: System_sysdictionarydetail[]
       *       // 创建时间
       *       createdAt?: string
       *       // 禁用状态，根据status字段动态计算
       *       disabled?: boolean
       *       // 扩展值
       *       extend?: string
       *       // 展示值
       *       label?: string
       *       // 层级深度，从0开始
       *       level?: number
       *       // 父级字典详情ID
       *       parentID?: number
       *       // 层级路径，如 "1,2,3"
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 启用状态
       *       status?: boolean
       *       // 关联标记
       *       sysDictionaryID?: number
       *       // 更新时间
       *       updatedAt?: string
       *       // 字典值
       *       value?: string
       *     }>
       *     // 字典名（英）
       *     type?: string
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 菜单数据，直接复用SysBaseMenu
       *   // [items] start
       *   // [items] end
       *   menus?: Array<{
       *     // 主键ID
       *     ID?: number
       *     // [items] start
       *     // [items] end
       *     authoritys?: Array<{
       *       // 角色ID
       *       authorityId?: number
       *       // 角色名
       *       authorityName?: string
       *       // [items] start
       *       // [cycle] $.menus.[].authoritys.[]
       *       // [items] end
       *       children?: System_sysauthority[]
       *       // 创建时间
       *       createdAt?: string
       *       // [items] start
       *       // [cycle] $.menus.[].authoritys.[]
       *       // [items] end
       *       dataAuthorityId?: System_sysauthority[]
       *       // 默认菜单(默认dashboard)
       *       defaultRouter?: string
       *       deletedAt?: string
       *       // [items] start
       *       // [cycle] $.menus.[]
       *       // [items] end
       *       menus?: System_sysbasemenu[]
       *       // 父角色ID
       *       parentId?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // [items] start
       *     // [cycle] $.menus.[]
       *     // [items] end
       *     children?: System_sysbasemenu[]
       *     // 对应前端文件路径
       *     component?: string
       *     // 创建时间
       *     createdAt?: string
       *     // 是否在列表隐藏
       *     hidden?: boolean
       *     // [items] start
       *     // [items] end
       *     menuBtn?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       desc?: string
       *       name?: string
       *       sysBaseMenuID?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 附加属性
       *     meta?: {
       *       activeName?: string
       *       // 自动关闭tab
       *       closeTab?: boolean
       *       // 是否是基础路由（开发中）
       *       defaultMenu?: boolean
       *       // 菜单图标
       *       icon?: string
       *       // 是否缓存
       *       keepAlive?: boolean
       *       // 菜单名
       *       title?: string
       *       // 路由切换动画
       *       transitionType?: string
       *     }
       *     // 路由name
       *     name?: string
       *     // [items] start
       *     // [items] end
       *     parameters?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // 创建时间
       *       createdAt?: string
       *       // 地址栏携带参数的key
       *       key?: string
       *       sysBaseMenuID?: number
       *       // 地址栏携带参数为params还是query
       *       type?: string
       *       // 更新时间
       *       updatedAt?: string
       *       // 地址栏携带参数的值
       *       value?: string
       *     }>
       *     // 父菜单ID
       *     parentId?: number
       *     // 路由path
       *     path?: string
       *     // 排序标记
       *     sort?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 版本信息
       *   version: {
       *     // 版本号
       *     code: string
       *     // 版本描述
       *     description?: string
       *     // 导出时间
       *     exportTime?: string
       *     // 版本名称
       *     name: string
       *   }
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_sysversion_importversion<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_importversionrequest;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysVersion.post_sysversion_importversion',
        Config
      >;
    };
    TimeSlots: {
      /**
       * ---
       *
       * [POST] 创建时段设置
       *
       * **path:** /timeSlots/createTimeSlots
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 结束时间
       *   end_time?: string
       *   // 适用楼层 ID，NULL 表示全局时段
       *   floorId?: number
       *   // 时段标签，例如 10:00-11:00
       *   label?: string
       *   // 排序
       *   slot_order?: number
       *   // 起始时间
       *   startTime?: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_timeslots_createtimeslots<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_timeslots;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'TimeSlots.post_timeslots_createtimeslots',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除时段设置
       *
       * **path:** /timeSlots/deleteTimeSlots
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 结束时间
       *   end_time?: string
       *   // 适用楼层 ID，NULL 表示全局时段
       *   floorId?: number
       *   // 时段标签，例如 10:00-11:00
       *   label?: string
       *   // 排序
       *   slot_order?: number
       *   // 起始时间
       *   startTime?: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_timeslots_deletetimeslots<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_timeslots;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'TimeSlots.delete_timeslots_deletetimeslots',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除时段设置
       *
       * **path:** /timeSlots/deleteTimeSlotsByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_timeslots_deletetimeslotsbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'TimeSlots.delete_timeslots_deletetimeslotsbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询时段设置
       *
       * **path:** /timeSlots/findTimeSlots
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 用id查询时段设置
       *   ID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     deletedBy?: number
       *     // 结束时间
       *     end_time?: string
       *     // 适用楼层 ID，NULL 表示全局时段
       *     floorId?: number
       *     // 时段标签，例如 10:00-11:00
       *     label?: string
       *     // 排序
       *     slot_order?: number
       *     // 起始时间
       *     startTime?: string
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_timeslots_findtimeslots<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Miniprogram_timeslots;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 用id查询时段设置
             */
            ID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Miniprogram_timeslots;
          msg?: string;
        },
        'TimeSlots.get_timeslots_findtimeslots',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取时段设置列表
       *
       * **path:** /timeSlots/getTimeSlotsList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   // 关键字
       *   keyword?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_timeslots_gettimeslotslist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            /**
             * 关键字
             */
            keyword?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'TimeSlots.get_timeslots_gettimeslotslist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的时段设置接口
       *
       * **path:** /timeSlots/getTimeSlotsPublic
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_timeslots_gettimeslotspublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'TimeSlots.get_timeslots_gettimeslotspublic',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新时段设置
       *
       * **path:** /timeSlots/updateTimeSlots
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   deletedBy?: number
       *   // 结束时间
       *   end_time?: string
       *   // 适用楼层 ID，NULL 表示全局时段
       *   floorId?: number
       *   // 时段标签，例如 10:00-11:00
       *   label?: string
       *   // 排序
       *   slot_order?: number
       *   // 起始时间
       *   startTime?: string
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_timeslots_updatetimeslots<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_timeslots;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'TimeSlots.put_timeslots_updatetimeslots',
        Config
      >;
    };
    SysUser: {
      /**
       * ---
       *
       * [PUT] 设置用户信息
       *
       * **path:** /user/SetSelfInfo
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 多用户角色
       *   // [items] start
       *   // [items] end
       *   authorities?: Array<{
       *     // 角色ID
       *     authorityId?: number
       *     // 角色名
       *     authorityName?: string
       *     // [items] start
       *     // [cycle] $.authorities.[]
       *     // [items] end
       *     children?: System_sysauthority[]
       *     // 创建时间
       *     createdAt?: string
       *     // [items] start
       *     // [cycle] $.authorities.[]
       *     // [items] end
       *     dataAuthorityId?: System_sysauthority[]
       *     // 默认菜单(默认dashboard)
       *     defaultRouter?: string
       *     deletedAt?: string
       *     // [items] start
       *     // [items] end
       *     menus?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // [items] start
       *       // [cycle] $.authorities.[]
       *       // [items] end
       *       authoritys?: System_sysauthority[]
       *       // [items] start
       *       // [cycle] $.authorities.[].menus.[]
       *       // [items] end
       *       children?: System_sysbasemenu[]
       *       // 对应前端文件路径
       *       component?: string
       *       // 创建时间
       *       createdAt?: string
       *       // 是否在列表隐藏
       *       hidden?: boolean
       *       // [items] start
       *       // [items] end
       *       menuBtn?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         desc?: string
       *         name?: string
       *         sysBaseMenuID?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 附加属性
       *       meta?: {
       *         activeName?: string
       *         // 自动关闭tab
       *         closeTab?: boolean
       *         // 是否是基础路由（开发中）
       *         defaultMenu?: boolean
       *         // 菜单图标
       *         icon?: string
       *         // 是否缓存
       *         keepAlive?: boolean
       *         // 菜单名
       *         title?: string
       *         // 路由切换动画
       *         transitionType?: string
       *       }
       *       // 路由name
       *       name?: string
       *       // [items] start
       *       // [items] end
       *       parameters?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 地址栏携带参数的key
       *         key?: string
       *         sysBaseMenuID?: number
       *         // 地址栏携带参数为params还是query
       *         type?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 地址栏携带参数的值
       *         value?: string
       *       }>
       *       // 父菜单ID
       *       parentId?: number
       *       // 路由path
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 父角色ID
       *     parentId?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 用户角色
       *   authority?: {
       *     // 角色ID
       *     authorityId?: number
       *     // 角色名
       *     authorityName?: string
       *     // [items] start
       *     // [cycle] $.authority
       *     // [items] end
       *     children?: System_sysauthority[]
       *     // 创建时间
       *     createdAt?: string
       *     // [items] start
       *     // [cycle] $.authority
       *     // [items] end
       *     dataAuthorityId?: System_sysauthority[]
       *     // 默认菜单(默认dashboard)
       *     defaultRouter?: string
       *     deletedAt?: string
       *     // [items] start
       *     // [items] end
       *     menus?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // [items] start
       *       // [cycle] $.authority
       *       // [items] end
       *       authoritys?: System_sysauthority[]
       *       // [items] start
       *       // [cycle] $.authority.menus.[]
       *       // [items] end
       *       children?: System_sysbasemenu[]
       *       // 对应前端文件路径
       *       component?: string
       *       // 创建时间
       *       createdAt?: string
       *       // 是否在列表隐藏
       *       hidden?: boolean
       *       // [items] start
       *       // [items] end
       *       menuBtn?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         desc?: string
       *         name?: string
       *         sysBaseMenuID?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 附加属性
       *       meta?: {
       *         activeName?: string
       *         // 自动关闭tab
       *         closeTab?: boolean
       *         // 是否是基础路由（开发中）
       *         defaultMenu?: boolean
       *         // 菜单图标
       *         icon?: string
       *         // 是否缓存
       *         keepAlive?: boolean
       *         // 菜单名
       *         title?: string
       *         // 路由切换动画
       *         transitionType?: string
       *       }
       *       // 路由name
       *       name?: string
       *       // [items] start
       *       // [items] end
       *       parameters?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 地址栏携带参数的key
       *         key?: string
       *         sysBaseMenuID?: number
       *         // 地址栏携带参数为params还是query
       *         type?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 地址栏携带参数的值
       *         value?: string
       *       }>
       *       // 父菜单ID
       *       parentId?: number
       *       // 路由path
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 父角色ID
       *     parentId?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }
       *   // 用户角色ID
       *   authorityId?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 用户邮箱
       *   email?: string
       *   // 用户是否被冻结 1正常 2冻结
       *   enable?: number
       *   // 用户头像
       *   headerImg?: string
       *   // 用户昵称
       *   nickName?: string
       *   // 配置
       *   originSetting?: Record<string, any>
       *   // 用户手机号
       *   phone?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 用户登录名
       *   userName?: string
       *   // 用户UUID
       *   uuid?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      put_user_setselfinfo<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          data: System_sysuser;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'SysUser.put_user_setselfinfo',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 设置用户配置
       *
       * **path:** /user/SetSelfSetting
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = Record<string, any>
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      put_user_setselfsetting<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          data: Record<string, any>;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'SysUser.put_user_setselfsetting',
        Config
      >;
      /**
       * ---
       *
       * [POST] 用户注册账号
       *
       * **path:** /user/admin_register
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   authorityId?: string
       *   authorityIds?: string
       *   email?: string
       *   enable?: string
       *   headerImg?: string
       *   nickName?: string
       *   passWord?: string
       *   phone?: string
       *   userName?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     user?: {
       *       // 主键ID
       *       ID?: number
       *       // 多用户角色
       *       // [items] start
       *       // [items] end
       *       authorities?: Array<{
       *         // 角色ID
       *         authorityId?: number
       *         // 角色名
       *         authorityName?: string
       *         // [items] start
       *         // [cycle] $.data.user.authorities.[]
       *         // [items] end
       *         children?: System_sysauthority[]
       *         // 创建时间
       *         createdAt?: string
       *         // [items] start
       *         // [cycle] $.data.user.authorities.[]
       *         // [items] end
       *         dataAuthorityId?: System_sysauthority[]
       *         // 默认菜单(默认dashboard)
       *         defaultRouter?: string
       *         deletedAt?: string
       *         // [items] start
       *         // [items] end
       *         menus?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // [items] start
       *           // [cycle] $.data.user.authorities.[]
       *           // [items] end
       *           authoritys?: System_sysauthority[]
       *           // [items] start
       *           // [cycle] $.data.user.authorities.[].menus.[]
       *           // [items] end
       *           children?: System_sysbasemenu[]
       *           // 对应前端文件路径
       *           component?: string
       *           // 创建时间
       *           createdAt?: string
       *           // 是否在列表隐藏
       *           hidden?: boolean
       *           // [items] start
       *           // [items] end
       *           menuBtn?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // 创建时间
       *             createdAt?: string
       *             desc?: string
       *             name?: string
       *             sysBaseMenuID?: number
       *             // 更新时间
       *             updatedAt?: string
       *           }>
       *           // 附加属性
       *           meta?: {
       *             activeName?: string
       *             // 自动关闭tab
       *             closeTab?: boolean
       *             // 是否是基础路由（开发中）
       *             defaultMenu?: boolean
       *             // 菜单图标
       *             icon?: string
       *             // 是否缓存
       *             keepAlive?: boolean
       *             // 菜单名
       *             title?: string
       *             // 路由切换动画
       *             transitionType?: string
       *           }
       *           // 路由name
       *           name?: string
       *           // [items] start
       *           // [items] end
       *           parameters?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // 创建时间
       *             createdAt?: string
       *             // 地址栏携带参数的key
       *             key?: string
       *             sysBaseMenuID?: number
       *             // 地址栏携带参数为params还是query
       *             type?: string
       *             // 更新时间
       *             updatedAt?: string
       *             // 地址栏携带参数的值
       *             value?: string
       *           }>
       *           // 父菜单ID
       *           parentId?: number
       *           // 路由path
       *           path?: string
       *           // 排序标记
       *           sort?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 父角色ID
       *         parentId?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 用户角色
       *       authority?: {
       *         // 角色ID
       *         authorityId?: number
       *         // 角色名
       *         authorityName?: string
       *         // [items] start
       *         // [cycle] $.data.user.authority
       *         // [items] end
       *         children?: System_sysauthority[]
       *         // 创建时间
       *         createdAt?: string
       *         // [items] start
       *         // [cycle] $.data.user.authority
       *         // [items] end
       *         dataAuthorityId?: System_sysauthority[]
       *         // 默认菜单(默认dashboard)
       *         defaultRouter?: string
       *         deletedAt?: string
       *         // [items] start
       *         // [items] end
       *         menus?: Array<{
       *           // 主键ID
       *           ID?: number
       *           // [items] start
       *           // [cycle] $.data.user.authority
       *           // [items] end
       *           authoritys?: System_sysauthority[]
       *           // [items] start
       *           // [cycle] $.data.user.authority.menus.[]
       *           // [items] end
       *           children?: System_sysbasemenu[]
       *           // 对应前端文件路径
       *           component?: string
       *           // 创建时间
       *           createdAt?: string
       *           // 是否在列表隐藏
       *           hidden?: boolean
       *           // [items] start
       *           // [items] end
       *           menuBtn?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // 创建时间
       *             createdAt?: string
       *             desc?: string
       *             name?: string
       *             sysBaseMenuID?: number
       *             // 更新时间
       *             updatedAt?: string
       *           }>
       *           // 附加属性
       *           meta?: {
       *             activeName?: string
       *             // 自动关闭tab
       *             closeTab?: boolean
       *             // 是否是基础路由（开发中）
       *             defaultMenu?: boolean
       *             // 菜单图标
       *             icon?: string
       *             // 是否缓存
       *             keepAlive?: boolean
       *             // 菜单名
       *             title?: string
       *             // 路由切换动画
       *             transitionType?: string
       *           }
       *           // 路由name
       *           name?: string
       *           // [items] start
       *           // [items] end
       *           parameters?: Array<{
       *             // 主键ID
       *             ID?: number
       *             // 创建时间
       *             createdAt?: string
       *             // 地址栏携带参数的key
       *             key?: string
       *             sysBaseMenuID?: number
       *             // 地址栏携带参数为params还是query
       *             type?: string
       *             // 更新时间
       *             updatedAt?: string
       *             // 地址栏携带参数的值
       *             value?: string
       *           }>
       *           // 父菜单ID
       *           parentId?: number
       *           // 路由path
       *           path?: string
       *           // 排序标记
       *           sort?: number
       *           // 更新时间
       *           updatedAt?: string
       *         }>
       *         // 父角色ID
       *         parentId?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }
       *       // 用户角色ID
       *       authorityId?: number
       *       // 创建时间
       *       createdAt?: string
       *       // 用户邮箱
       *       email?: string
       *       // 用户是否被冻结 1正常 2冻结
       *       enable?: number
       *       // 用户头像
       *       headerImg?: string
       *       // 用户昵称
       *       nickName?: string
       *       // 配置
       *       originSetting?: Record<string, any>
       *       // 用户手机号
       *       phone?: string
       *       // 更新时间
       *       updatedAt?: string
       *       // 用户登录名
       *       userName?: string
       *       // 用户UUID
       *       uuid?: string
       *     }
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_user_admin_register<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_sysuserresponse;
            msg?: string;
          }
        > & {
          data: Request_register;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_sysuserresponse;
          msg?: string;
        },
        'SysUser.post_user_admin_register',
        Config
      >;
      /**
       * ---
       *
       * [POST] 用户修改密码
       *
       * **path:** /user/changePassword
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 新密码
       *   newPassword?: string
       *   // 密码
       *   password?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_user_changepassword<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_changepasswordreq;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysUser.post_user_changepassword',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除用户
       *
       * **path:** /user/deleteUser
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_user_deleteuser<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_getbyid;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysUser.delete_user_deleteuser',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取用户信息
       *
       * **path:** /user/getUserInfo
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      get_user_getuserinfo<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'SysUser.get_user_getuserinfo',
        Config
      >;
      /**
       * ---
       *
       * [POST] 分页获取用户列表
       *
       * **path:** /user/getUserList
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   email?: string
       *   // 关键字
       *   keyword?: string
       *   nickName?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   phone?: string
       *   username?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      post_user_getuserlist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          data: Request_getuserlist;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'SysUser.post_user_getuserlist',
        Config
      >;
      /**
       * ---
       *
       * [POST] 重置用户密码
       *
       * **path:** /user/resetPassword
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 多用户角色
       *   // [items] start
       *   // [items] end
       *   authorities?: Array<{
       *     // 角色ID
       *     authorityId?: number
       *     // 角色名
       *     authorityName?: string
       *     // [items] start
       *     // [cycle] $.authorities.[]
       *     // [items] end
       *     children?: System_sysauthority[]
       *     // 创建时间
       *     createdAt?: string
       *     // [items] start
       *     // [cycle] $.authorities.[]
       *     // [items] end
       *     dataAuthorityId?: System_sysauthority[]
       *     // 默认菜单(默认dashboard)
       *     defaultRouter?: string
       *     deletedAt?: string
       *     // [items] start
       *     // [items] end
       *     menus?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // [items] start
       *       // [cycle] $.authorities.[]
       *       // [items] end
       *       authoritys?: System_sysauthority[]
       *       // [items] start
       *       // [cycle] $.authorities.[].menus.[]
       *       // [items] end
       *       children?: System_sysbasemenu[]
       *       // 对应前端文件路径
       *       component?: string
       *       // 创建时间
       *       createdAt?: string
       *       // 是否在列表隐藏
       *       hidden?: boolean
       *       // [items] start
       *       // [items] end
       *       menuBtn?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         desc?: string
       *         name?: string
       *         sysBaseMenuID?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 附加属性
       *       meta?: {
       *         activeName?: string
       *         // 自动关闭tab
       *         closeTab?: boolean
       *         // 是否是基础路由（开发中）
       *         defaultMenu?: boolean
       *         // 菜单图标
       *         icon?: string
       *         // 是否缓存
       *         keepAlive?: boolean
       *         // 菜单名
       *         title?: string
       *         // 路由切换动画
       *         transitionType?: string
       *       }
       *       // 路由name
       *       name?: string
       *       // [items] start
       *       // [items] end
       *       parameters?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 地址栏携带参数的key
       *         key?: string
       *         sysBaseMenuID?: number
       *         // 地址栏携带参数为params还是query
       *         type?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 地址栏携带参数的值
       *         value?: string
       *       }>
       *       // 父菜单ID
       *       parentId?: number
       *       // 路由path
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 父角色ID
       *     parentId?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 用户角色
       *   authority?: {
       *     // 角色ID
       *     authorityId?: number
       *     // 角色名
       *     authorityName?: string
       *     // [items] start
       *     // [cycle] $.authority
       *     // [items] end
       *     children?: System_sysauthority[]
       *     // 创建时间
       *     createdAt?: string
       *     // [items] start
       *     // [cycle] $.authority
       *     // [items] end
       *     dataAuthorityId?: System_sysauthority[]
       *     // 默认菜单(默认dashboard)
       *     defaultRouter?: string
       *     deletedAt?: string
       *     // [items] start
       *     // [items] end
       *     menus?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // [items] start
       *       // [cycle] $.authority
       *       // [items] end
       *       authoritys?: System_sysauthority[]
       *       // [items] start
       *       // [cycle] $.authority.menus.[]
       *       // [items] end
       *       children?: System_sysbasemenu[]
       *       // 对应前端文件路径
       *       component?: string
       *       // 创建时间
       *       createdAt?: string
       *       // 是否在列表隐藏
       *       hidden?: boolean
       *       // [items] start
       *       // [items] end
       *       menuBtn?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         desc?: string
       *         name?: string
       *         sysBaseMenuID?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 附加属性
       *       meta?: {
       *         activeName?: string
       *         // 自动关闭tab
       *         closeTab?: boolean
       *         // 是否是基础路由（开发中）
       *         defaultMenu?: boolean
       *         // 菜单图标
       *         icon?: string
       *         // 是否缓存
       *         keepAlive?: boolean
       *         // 菜单名
       *         title?: string
       *         // 路由切换动画
       *         transitionType?: string
       *       }
       *       // 路由name
       *       name?: string
       *       // [items] start
       *       // [items] end
       *       parameters?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 地址栏携带参数的key
       *         key?: string
       *         sysBaseMenuID?: number
       *         // 地址栏携带参数为params还是query
       *         type?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 地址栏携带参数的值
       *         value?: string
       *       }>
       *       // 父菜单ID
       *       parentId?: number
       *       // 路由path
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 父角色ID
       *     parentId?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }
       *   // 用户角色ID
       *   authorityId?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 用户邮箱
       *   email?: string
       *   // 用户是否被冻结 1正常 2冻结
       *   enable?: number
       *   // 用户头像
       *   headerImg?: string
       *   // 用户昵称
       *   nickName?: string
       *   // 配置
       *   originSetting?: Record<string, any>
       *   // 用户手机号
       *   phone?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 用户登录名
       *   userName?: string
       *   // 用户UUID
       *   uuid?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_user_resetpassword<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: System_sysuser;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysUser.post_user_resetpassword',
        Config
      >;
      /**
       * ---
       *
       * [POST] 设置用户权限
       *
       * **path:** /user/setUserAuthorities
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 角色ID
       *   // [items] start
       *   // [items] end
       *   authorityIds?: number[]
       *   id?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_user_setuserauthorities<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_setuserauthorities;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysUser.post_user_setuserauthorities',
        Config
      >;
      /**
       * ---
       *
       * [POST] 更改用户权限
       *
       * **path:** /user/setUserAuthority
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 角色ID
       *   authorityId?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_user_setuserauthority<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Request_setuserauth;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'SysUser.post_user_setuserauthority',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 设置用户信息
       *
       * **path:** /user/setUserInfo
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 多用户角色
       *   // [items] start
       *   // [items] end
       *   authorities?: Array<{
       *     // 角色ID
       *     authorityId?: number
       *     // 角色名
       *     authorityName?: string
       *     // [items] start
       *     // [cycle] $.authorities.[]
       *     // [items] end
       *     children?: System_sysauthority[]
       *     // 创建时间
       *     createdAt?: string
       *     // [items] start
       *     // [cycle] $.authorities.[]
       *     // [items] end
       *     dataAuthorityId?: System_sysauthority[]
       *     // 默认菜单(默认dashboard)
       *     defaultRouter?: string
       *     deletedAt?: string
       *     // [items] start
       *     // [items] end
       *     menus?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // [items] start
       *       // [cycle] $.authorities.[]
       *       // [items] end
       *       authoritys?: System_sysauthority[]
       *       // [items] start
       *       // [cycle] $.authorities.[].menus.[]
       *       // [items] end
       *       children?: System_sysbasemenu[]
       *       // 对应前端文件路径
       *       component?: string
       *       // 创建时间
       *       createdAt?: string
       *       // 是否在列表隐藏
       *       hidden?: boolean
       *       // [items] start
       *       // [items] end
       *       menuBtn?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         desc?: string
       *         name?: string
       *         sysBaseMenuID?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 附加属性
       *       meta?: {
       *         activeName?: string
       *         // 自动关闭tab
       *         closeTab?: boolean
       *         // 是否是基础路由（开发中）
       *         defaultMenu?: boolean
       *         // 菜单图标
       *         icon?: string
       *         // 是否缓存
       *         keepAlive?: boolean
       *         // 菜单名
       *         title?: string
       *         // 路由切换动画
       *         transitionType?: string
       *       }
       *       // 路由name
       *       name?: string
       *       // [items] start
       *       // [items] end
       *       parameters?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 地址栏携带参数的key
       *         key?: string
       *         sysBaseMenuID?: number
       *         // 地址栏携带参数为params还是query
       *         type?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 地址栏携带参数的值
       *         value?: string
       *       }>
       *       // 父菜单ID
       *       parentId?: number
       *       // 路由path
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 父角色ID
       *     parentId?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }>
       *   // 用户角色
       *   authority?: {
       *     // 角色ID
       *     authorityId?: number
       *     // 角色名
       *     authorityName?: string
       *     // [items] start
       *     // [cycle] $.authority
       *     // [items] end
       *     children?: System_sysauthority[]
       *     // 创建时间
       *     createdAt?: string
       *     // [items] start
       *     // [cycle] $.authority
       *     // [items] end
       *     dataAuthorityId?: System_sysauthority[]
       *     // 默认菜单(默认dashboard)
       *     defaultRouter?: string
       *     deletedAt?: string
       *     // [items] start
       *     // [items] end
       *     menus?: Array<{
       *       // 主键ID
       *       ID?: number
       *       // [items] start
       *       // [cycle] $.authority
       *       // [items] end
       *       authoritys?: System_sysauthority[]
       *       // [items] start
       *       // [cycle] $.authority.menus.[]
       *       // [items] end
       *       children?: System_sysbasemenu[]
       *       // 对应前端文件路径
       *       component?: string
       *       // 创建时间
       *       createdAt?: string
       *       // 是否在列表隐藏
       *       hidden?: boolean
       *       // [items] start
       *       // [items] end
       *       menuBtn?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         desc?: string
       *         name?: string
       *         sysBaseMenuID?: number
       *         // 更新时间
       *         updatedAt?: string
       *       }>
       *       // 附加属性
       *       meta?: {
       *         activeName?: string
       *         // 自动关闭tab
       *         closeTab?: boolean
       *         // 是否是基础路由（开发中）
       *         defaultMenu?: boolean
       *         // 菜单图标
       *         icon?: string
       *         // 是否缓存
       *         keepAlive?: boolean
       *         // 菜单名
       *         title?: string
       *         // 路由切换动画
       *         transitionType?: string
       *       }
       *       // 路由name
       *       name?: string
       *       // [items] start
       *       // [items] end
       *       parameters?: Array<{
       *         // 主键ID
       *         ID?: number
       *         // 创建时间
       *         createdAt?: string
       *         // 地址栏携带参数的key
       *         key?: string
       *         sysBaseMenuID?: number
       *         // 地址栏携带参数为params还是query
       *         type?: string
       *         // 更新时间
       *         updatedAt?: string
       *         // 地址栏携带参数的值
       *         value?: string
       *       }>
       *       // 父菜单ID
       *       parentId?: number
       *       // 路由path
       *       path?: string
       *       // 排序标记
       *       sort?: number
       *       // 更新时间
       *       updatedAt?: string
       *     }>
       *     // 父角色ID
       *     parentId?: number
       *     // 更新时间
       *     updatedAt?: string
       *   }
       *   // 用户角色ID
       *   authorityId?: number
       *   // 创建时间
       *   createdAt?: string
       *   // 用户邮箱
       *   email?: string
       *   // 用户是否被冻结 1正常 2冻结
       *   enable?: number
       *   // 用户头像
       *   headerImg?: string
       *   // 用户昵称
       *   nickName?: string
       *   // 配置
       *   originSetting?: Record<string, any>
       *   // 用户手机号
       *   phone?: string
       *   // 更新时间
       *   updatedAt?: string
       *   // 用户登录名
       *   userName?: string
       *   // 用户UUID
       *   uuid?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: Record<string, any>
       *   msg?: string
       * }
       * ```
       */
      put_user_setuserinfo<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Record<string, any>;
            msg?: string;
          }
        > & {
          data: System_sysuser;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Record<string, any>;
          msg?: string;
        },
        'SysUser.put_user_setuserinfo',
        Config
      >;
    };
    WechatUser: {
      /**
       * ---
       *
       * [POST] 创建小程序用户
       *
       * **path:** /wechatuser/createWechatUser
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 头像URL
       *   avatar_url?: string
       *   bath_count?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   // 默认设备ID
       *   default_device_id?: number
       *   deletedBy?: number
       *   // 加解密密钥
       *   encryption_key?: string
       *   // 性别：0-未知，1-男，2-女
       *   gender: number
       *   // 身份证反面照URL
       *   id_car_back?: string
       *   // 身份证号
       *   id_card: string
       *   // 身份证正面照URL
       *   id_card_front: string
       *   // 手机号
       *   mobile: string
       *   // 用户昵称
       *   nick_name: string
       *   // 微信openid
       *   open_id?: string
       *   // 用户积分
       *   points?: number
       *   // 真实姓名
       *   real_name: string
       *   // 微信会话密钥
       *   session_key?: string
       *   // 状态：0-正常，1-禁用
       *   status: string
       *   // 累计消费
       *   total_consumption?: number
       *   // 累计充值
       *   total_recharge?: number
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 会员卡id
       *   vip_card?: string
       *   // 会员卡等级0无会员 1体验会员 2 vip会员 3 银卡会员 4金卡会员 5钻石会员
       *   vip_level?: number
       *   // 用户卡包余额
       *   wallet_balance?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      post_wechatuser_createwechatuser<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_wechatuser;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'WechatUser.post_wechatuser_createwechatuser',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除小程序用户
       *
       * **path:** /wechatuser/deleteWechatUser
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 头像URL
       *   avatar_url?: string
       *   bath_count?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   // 默认设备ID
       *   default_device_id?: number
       *   deletedBy?: number
       *   // 加解密密钥
       *   encryption_key?: string
       *   // 性别：0-未知，1-男，2-女
       *   gender: number
       *   // 身份证反面照URL
       *   id_car_back?: string
       *   // 身份证号
       *   id_card: string
       *   // 身份证正面照URL
       *   id_card_front: string
       *   // 手机号
       *   mobile: string
       *   // 用户昵称
       *   nick_name: string
       *   // 微信openid
       *   open_id?: string
       *   // 用户积分
       *   points?: number
       *   // 真实姓名
       *   real_name: string
       *   // 微信会话密钥
       *   session_key?: string
       *   // 状态：0-正常，1-禁用
       *   status: string
       *   // 累计消费
       *   total_consumption?: number
       *   // 累计充值
       *   total_recharge?: number
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 会员卡id
       *   vip_card?: string
       *   // 会员卡等级0无会员 1体验会员 2 vip会员 3 银卡会员 4金卡会员 5钻石会员
       *   vip_level?: number
       *   // 用户卡包余额
       *   wallet_balance?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_wechatuser_deletewechatuser<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_wechatuser;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'WechatUser.delete_wechatuser_deletewechatuser',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 批量删除小程序用户
       *
       * **path:** /wechatuser/deleteWechatUserByIds
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      delete_wechatuser_deletewechatuserbyids<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'WechatUser.delete_wechatuser_deletewechatuserbyids',
        Config
      >;
      /**
       * ---
       *
       * [GET] 用id查询小程序用户
       *
       * **path:** /wechatuser/findWechatUser
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 用id查询小程序用户
       *   ID: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     // 主键ID
       *     ID?: number
       *     // 头像URL
       *     avatar_url?: string
       *     bath_count?: number
       *     // 创建时间
       *     createdAt?: string
       *     createdBy?: number
       *     // 默认设备ID
       *     default_device_id?: number
       *     deletedBy?: number
       *     // 加解密密钥
       *     encryption_key?: string
       *     // 性别：0-未知，1-男，2-女
       *     gender: number
       *     // 身份证反面照URL
       *     id_car_back?: string
       *     // 身份证号
       *     id_card: string
       *     // 身份证正面照URL
       *     id_card_front: string
       *     // 手机号
       *     mobile: string
       *     // 用户昵称
       *     nick_name: string
       *     // 微信openid
       *     open_id?: string
       *     // 用户积分
       *     points?: number
       *     // 真实姓名
       *     real_name: string
       *     // 微信会话密钥
       *     session_key?: string
       *     // 状态：0-正常，1-禁用
       *     status: string
       *     // 累计消费
       *     total_consumption?: number
       *     // 累计充值
       *     total_recharge?: number
       *     // 更新时间
       *     updatedAt?: string
       *     updatedBy?: number
       *     // 会员卡id
       *     vip_card?: string
       *     // 会员卡等级0无会员 1体验会员 2 vip会员 3 银卡会员 4金卡会员 5钻石会员
       *     vip_level?: number
       *     // 用户卡包余额
       *     wallet_balance?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_wechatuser_findwechatuser<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Miniprogram_wechatuser;
            msg?: string;
          }
        > & {
          params: {
            /**
             * 用id查询小程序用户
             */
            ID: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Miniprogram_wechatuser;
          msg?: string;
        },
        'WechatUser.get_wechatuser_findwechatuser',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页获取小程序用户列表
       *
       * **path:** /wechatuser/getWechatUserList
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // [items] start
       *   // [items] end
       *   createdAtRange?: string[]
       *   gender?: number
       *   id_card?: string
       *   // 关键字
       *   keyword?: string
       *   mobile?: string
       *   nick_name?: string
       *   order?: string
       *   // 页码
       *   page?: number
       *   // 每页大小
       *   pageSize?: number
       *   real_name?: string
       *   sort?: string
       *   status?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: {
       *     list?: null
       *     page?: number
       *     pageSize?: number
       *     total?: number
       *   }
       *   msg?: string
       * }
       * ```
       */
      get_wechatuser_getwechatuserlist<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: Response_pageresult;
            msg?: string;
          }
        > & {
          params: {
            createdAtRange?: string[];
            gender?: number;
            id_card?: string;
            /**
             * 关键字
             */
            keyword?: string;
            mobile?: string;
            nick_name?: string;
            order?: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页大小
             */
            pageSize?: number;
            real_name?: string;
            sort?: string;
            status?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          data?: Response_pageresult;
          msg?: string;
        },
        'WechatUser.get_wechatuser_getwechatuserlist',
        Config
      >;
      /**
       * ---
       *
       * [GET] 不需要鉴权的小程序用户接口
       *
       * **path:** /wechatuser/getWechatUserPublic
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   data?: object
       *   msg?: string
       * }
       * ```
       */
      get_wechatuser_getwechatuserpublic<
        Config extends Alova2MethodConfig<
          Response_response & {
            data?: object;
            msg?: string;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        Response_response & {
          data?: object;
          msg?: string;
        },
        'WechatUser.get_wechatuser_getwechatuserpublic',
        Config
      >;
      /**
       * ---
       *
       * [PUT] 更新小程序用户
       *
       * **path:** /wechatuser/updateWechatUser
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 主键ID
       *   ID?: number
       *   // 头像URL
       *   avatar_url?: string
       *   bath_count?: number
       *   // 创建时间
       *   createdAt?: string
       *   createdBy?: number
       *   // 默认设备ID
       *   default_device_id?: number
       *   deletedBy?: number
       *   // 加解密密钥
       *   encryption_key?: string
       *   // 性别：0-未知，1-男，2-女
       *   gender: number
       *   // 身份证反面照URL
       *   id_car_back?: string
       *   // 身份证号
       *   id_card: string
       *   // 身份证正面照URL
       *   id_card_front: string
       *   // 手机号
       *   mobile: string
       *   // 用户昵称
       *   nick_name: string
       *   // 微信openid
       *   open_id?: string
       *   // 用户积分
       *   points?: number
       *   // 真实姓名
       *   real_name: string
       *   // 微信会话密钥
       *   session_key?: string
       *   // 状态：0-正常，1-禁用
       *   status: string
       *   // 累计消费
       *   total_consumption?: number
       *   // 累计充值
       *   total_recharge?: number
       *   // 更新时间
       *   updatedAt?: string
       *   updatedBy?: number
       *   // 会员卡id
       *   vip_card?: string
       *   // 会员卡等级0无会员 1体验会员 2 vip会员 3 银卡会员 4金卡会员 5钻石会员
       *   vip_level?: number
       *   // 用户卡包余额
       *   wallet_balance?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   code?: number
       *   data?: null
       *   msg?: string
       * } & {
       *   msg?: string
       * }
       * ```
       */
      put_wechatuser_updatewechatuser<
        Config extends Alova2MethodConfig<
          Response_response & {
            msg?: string;
          }
        > & {
          data: Miniprogram_wechatuser;
        }
      >(
        config: Config
      ): Alova2Method<
        Response_response & {
          msg?: string;
        },
        'WechatUser.put_wechatuser_updatewechatuser',
        Config
      >;
    };
  }

  var Apis: Apis;
}
